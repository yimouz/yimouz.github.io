# Apache JMeter 属性配置手册 (中文版)

> 本手册基于 `jmeter.properties` 文件生成，涵盖了 JMeter 的核心配置项。
> **注意**：不应直接修改 `jmeter.properties`，建议在 `user.properties` 中进行覆盖配置。

---

## 目录
- [XML 解析器配置](#xml-解析器配置)
- [SSL 配置](#ssl-配置)
- [外观配置 (Look and Feel)](#外观配置-look-and-feel)
- [JMX 备份配置](#jmx-备份配置)
- [远程主机和 RMI 配置](#远程主机和-rmi-配置)
- [HTTP 配置](#http-配置)
- [结果文件配置](#结果文件配置)
- [测试脚本录制器配置](#测试脚本录制器配置)
- [其他高级配置](#其他高级配置)

---

## XML 解析器配置

| 配置项 | 默认值 | 说明 |
| :--- | :--- | :--- |
| `xpath.namespace.config` | (空) | 包含命名空间映射的属性文件的路径 |
| `xpath2query.parser.cache.size` | `400` | XPath2 查询缓存大小 |

## SSL 配置

| 配置项 | 默认值 | 说明 |
| :--- | :--- | :--- |
| `https.sessioncontext.shared` | `false` | SSL 会话上下文是否共享（false 为按线程创建） |
| `https.default.protocol` | `TLS` | 默认 HTTPS 协议级别 |
| `https.socket.protocols` | (空) | 启用的协议列表（如 SSLv2Hello SSLv3 TLSv1） |
| `https.use.cached.ssl.context` | `true` | 是否在迭代之间重用缓存的 SSL 上下文（已弃用） |
| `httpclient.reset_state_on_thread_group_iteration` | `true` | 线程组迭代时是否重置 HTTP/SSL 状态 |

## 外观配置 (Look and Feel)

| 配置项 | 默认值 | 说明 |
| :--- | :--- | :--- |
| `jmeter.laf` | (自动) | 指定 GUI 的外观类名 (如 System, CrossPlatform) |
| `darklaf.decorations` | `false` | 为 Darklaf 启用自定义窗口装饰 |
| `jmeter.hidpi.mode` | `false` | 启用伪 HiDPI 模式（Windows/Linux 高分屏建议开启） |
| `jmeter.hidpi.scale.factor` | `1.0` | HiDPI 缩放因子 |
| `jmeter.toolbar.icons.size` | `22x22` | 工具栏图标大小 (22x22, 32x32, 48x48) |
| `jmeter.tree.icons.size` | `19x19` | 树图标大小 (19x19, 24x24, 32x32, 48x48) |

## JMX 备份配置

| 配置项 | 默认值 | 说明 |
| :--- | :--- | :--- |
| `jmeter.gui.action.save.backup_on_save` | `true` | 保存测试计划时自动备份 |
| `jmeter.gui.action.save.backup_directory` | `${JMETER_HOME}/backups` | 备份目录路径 |
| `jmeter.gui.action.save.keep_backup_max_hours` | `0` | 备份保留时间（小时），0 为永久 |
| `jmeter.gui.action.save.keep_backup_max_count` | `10` | 保留的最大备份文件数量 |

## 远程主机和 RMI 配置

| 配置项 | 默认值 | 说明 |
| :--- | :--- | :--- |
| `remote_hosts` | `127.0.0.1` | 远程主机列表（逗号分隔） |
| `server_port` | `1099` | RMI 服务器端口 |
| `client.rmi.localport` | `0` | 客户端 RMI 本地端口（0 为随机） |
| `server.rmi.ssl.disable` | `false` | 是否禁用 RMI 的 SSL 加密 |
| `server.rmi.ssl.keystore.file` | `rmi_keystore.jks` | RMI SSL 密钥库文件 |

## HTTP 配置

### 通用与 Java 实现
| 配置项 | 默认值 | 说明 |
| :--- | :--- | :--- |
| `post_add_content_type_if_missing` | `false` | POST 请求缺少 Content-Type 时是否自动补全 application/x-www-form-urlencoded |
| `http.java.sampler.retries` | `0` | HTTP Java 采样器重试次数 |

### Apache HttpClient 4
| 配置项 | 默认值 | 说明 |
| :--- | :--- | :--- |
| `httpclient.timeout` | `0` | 套接字超时（毫秒），0 为无超时 |
| `httpclient4.retrycount` | `0` | 请求重试次数 |
| `httpclient4.idletimeout` | `0` | 空闲连接超时 |
| `httpclient4.validate_after_inactivity` | `4900` | 检查连接有效性的空闲时间阈值 |
| `httpclient4.time_to_live` | `60000` | 连接存活时间 (TTL) |

## 结果文件配置 (Results Save Service)

| 配置项 | 默认值 | 说明 |
| :--- | :--- | :--- |
| `jmeter.save.saveservice.output_format` | `csv` | 输出格式 (csv / xml) |
| `jmeter.save.saveservice.timestamp_format` | `ms` | 时间戳格式 (ms 或 yyyy/MM/dd HH:mm:ss.SSS) |
| `jmeter.save.saveservice.default_delimiter` | `,` | CSV 分隔符 |
| `jmeter.save.saveservice.response_data` | `false` | 是否保存响应数据 (XML 模式下有效) |
| `jmeter.save.saveservice.response_data.on_error` | `false` | 是否仅在错误时保存响应数据 |

## 测试脚本录制器配置 (Proxy)

| 配置项 | 默认值 | 说明 |
| :--- | :--- | :--- |
| `proxy.pause` | `5000` | 视为新页面点击的最小间隔 (毫秒) |
| `proxy.number.requests` | `true` | 为采样器名称添加数字后缀 |
| `proxy.excludes.suggested` | (正则) | 建议排除的 URL 模式 (图片、CSS、JS 等) |
| `proxy.content_type_include` | `text/html...` | 包含的内容类型 |
| `proxy.content_type_exclude` | `image/.*...` | 排除的内容类型 |
| `proxy.cert.validity` | `7` | 生成证书的有效期 (天) |
| `proxy.cert.dynamic_keys` | `true` | 使用动态密钥生成 |

## 报告生成器配置

| 配置项 | 默认值 | 说明 |
| :--- | :--- | :--- |
| `jmeter.reportgenerator.apdex_satisfied_threshold` | `500` | APDEX 满意度阈值 (ms) |
| `jmeter.reportgenerator.apdex_tolerated_threshold` | `1500` | APDEX 容忍度阈值 (ms) |
| `jmeter.reportgenerator.exporter.html.series_filter` | (空) | 图表系列过滤正则 |
| `generate_report_ui.generation_timeout` | `300000` | 报告生成超时时间 |

## 其他高级配置

| 类别 | 配置项 | 默认值 | 说明 |
| :--- | :--- | :--- | :--- |
| **BackendListener** | `backend_metrics_window_mode` | `fixed` | 指标窗口模式 |
| **BackendListener** | `backend_influxdb.send_interval` | `5` | InfluxDB 发送间隔 (秒) |
| **BeanShell** | `beanshell.server.port` | `9000` | BeanShell 服务器端口 |
| **CookieManager** | `CookieManager.delete_null_cookies` | `true` | 是否删除空值 Cookie |
| **CookieManager** | `CookieManager.check.cookies` | `true` | 是否验证 Cookie 有效性 |
| **JDBC** | `jdbcsampler.max_retain_result_size` | `65536` | JDBC 结果最大保留大小 (字节) |
| **TCP** | `tcp.handler` | `TCPClientImpl` | TCP 默认处理类 |
| **Summariser** | `summariser.interval` | `30` | 摘要输出间隔 (秒) |
| **Thread Group** | `testplan_validation.nb_threads_per_thread_group` | `1` | 验证模式下的线程数 |

---
*Generated by Trae AI Assistant*
