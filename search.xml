<?xml version="1.0" encoding="utf-8"?>
<search> 
  
  
    
    <entry>
      <title></title>
      <link href="/2026/04/01/Linux%20NTP%20%E6%97%B6%E9%97%B4%E5%90%8C%E6%AD%A5%E9%97%AE%E9%A2%98%E8%A7%A3%E5%86%B3%E6%8C%87%E5%8D%97/"/>
      <url>/2026/04/01/Linux%20NTP%20%E6%97%B6%E9%97%B4%E5%90%8C%E6%AD%A5%E9%97%AE%E9%A2%98%E8%A7%A3%E5%86%B3%E6%8C%87%E5%8D%97/</url>
      
        <content type="html"><![CDATA[<h1 id="Linux-NTP-时间同步问题解决指南"><a href="#Linux-NTP-时间同步问题解决指南" class="headerlink" title="Linux NTP 时间同步问题解决指南"></a>Linux NTP 时间同步问题解决指南</h1><h2 id="问题描述"><a href="#问题描述" class="headerlink" title="问题描述"></a>问题描述</h2><p>错误提示：<code>"the NTP socket is in use, exiting"</code> 表示 NTP 套接字正在被使用，通常是因为系统中的 <code>ntpd</code> 服务已经在运行，而 <code>ntpdate</code> 命令和 <code>ntpd</code> 服务不能同时使用端口 123。</p><h2 id="解决步骤"><a href="#解决步骤" class="headerlink" title="解决步骤"></a>解决步骤</h2><h3 id="1-停止-ntpd-服务"><a href="#1-停止-ntpd-服务" class="headerlink" title="1. 停止 ntpd 服务"></a>1. 停止 ntpd 服务</h3><p>在执行 <code>ntpdate</code> 命令之前，确保 <code>ntpd</code> 服务已经停止。</p><p><strong>CentOS 7 及之前版本</strong>：</p><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash">sudo service ntpd stop</code></pre><p><strong>CentOS 8 及之后版本</strong>：</p><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash">sudo systemctl stop ntpd</code></pre><h3 id="2-使用-ntpdate-进行时间同步"><a href="#2-使用-ntpdate-进行时间同步" class="headerlink" title="2. 使用 ntpdate 进行时间同步"></a>2. 使用 ntpdate 进行时间同步</h3><p>执行 <code>ntpdate</code> 进行手动时间同步。以下是一些常用的 NTP 服务器地址：</p><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash"># 使用 pool.ntp.orgsudo ntpdate pool.ntp.org# 或使用 time.nist.govsudo ntpdate time.nist.gov# 或使用阿里云 NTPsudo ntpdate ntp.aliyun.com</code></pre><h3 id="3-禁用-ntpd-服务的自动启动"><a href="#3-禁用-ntpd-服务的自动启动" class="headerlink" title="3. 禁用 ntpd 服务的自动启动"></a>3. 禁用 ntpd 服务的自动启动</h3><p>如果不希望 <code>ntpd</code> 服务在系统启动时自动启动，可以禁用其开机自启：</p><p><strong>CentOS 7 及之前版本</strong>：</p><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash">sudo chkconfig ntpd off</code></pre><p><strong>CentOS 8 及之后版本</strong>：</p><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash">sudo systemctl disable ntpd</code></pre><h3 id="4-设置计划任务（Crontab）"><a href="#4-设置计划任务（Crontab）" class="headerlink" title="4. 设置计划任务（Crontab）"></a>4. 设置计划任务（Crontab）</h3><p>如果需要定期同步时间，可以将 <code>ntpdate</code> 命令加入到计划任务中。</p><p>编辑 crontab 文件：</p><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash">sudo crontab -e</code></pre><p>添加定时任务，例如每天凌晨 2 点同步时间：</p><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash">0 2 * * * /usr/sbin/ntpdate pool.ntp.org</code></pre><h2 id="现代时间同步服务（推荐）"><a href="#现代时间同步服务（推荐）" class="headerlink" title="现代时间同步服务（推荐）"></a>现代时间同步服务（推荐）</h2><p>为了实现持续的时间同步，建议使用以下现代时间同步服务：</p><h3 id="方案一：使用-chrony（推荐）"><a href="#方案一：使用-chrony（推荐）" class="headerlink" title="方案一：使用 chrony（推荐）"></a>方案一：使用 chrony（推荐）</h3><p><code>chrony</code> 是一种现代且高效的时间同步服务，比 <code>ntpd</code> 更快、更准确。</p><p><strong>安装 chrony</strong>：</p><p>CentOS 7：</p><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash">sudo yum install chrony</code></pre><p>CentOS 8/9：</p><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash">sudo dnf install chrony</code></pre><p>Ubuntu/Debian：</p><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash">sudo apt install chrony</code></pre><p><strong>启动服务</strong>：</p><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash">sudo systemctl start chronydsudo systemctl enable chronyd</code></pre><p><strong>查看同步状态</strong>：</p><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash">chronyc trackingchronyc sources</code></pre><p><strong>配置文件</strong>（/etc/chrony.conf）：</p><pre class="line-numbers language-conf" data-language="conf"><code class="language-conf"># 使用阿里云 NTP 服务器server ntp.aliyun.com iburstserver ntp1.aliyun.com iburst# 或使用国际 NTP 池server 0.pool.ntp.org iburstserver 1.pool.ntp.org iburst</code></pre><h3 id="方案二：使用-systemd-timesyncd"><a href="#方案二：使用-systemd-timesyncd" class="headerlink" title="方案二：使用 systemd-timesyncd"></a>方案二：使用 systemd-timesyncd</h3><p><code>systemd-timesyncd</code> 是 systemd 的一部分，提供简单的时间同步功能。</p><p><strong>启动服务</strong>：</p><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash">sudo systemctl start systemd-timesyncdsudo systemctl enable systemd-timesyncd</code></pre><p><strong>查看同步状态</strong>：</p><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash">timedatectl status</code></pre><p><strong>配置文件</strong>（/etc/systemd/timesyncd.conf）：</p><pre class="line-numbers language-conf" data-language="conf"><code class="language-conf">[Time]NTP=ntp.aliyun.com ntp1.aliyun.comFallbackNTP=0.pool.ntp.org 1.pool.ntp.org</code></pre><h2 id="常见问题（FAQ）"><a href="#常见问题（FAQ）" class="headerlink" title="常见问题（FAQ）"></a>常见问题（FAQ）</h2><p><strong>Q1: 防火墙阻止 NTP 通信</strong></p><p>确保防火墙允许 UDP 123 端口：</p><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash"># firewalldsudo firewall-cmd --add-service=ntp --permanentsudo firewall-cmd --reload# iptablessudo iptables -A INPUT -p udp --dport 123 -j ACCEPTsudo iptables -A OUTPUT -p udp --sport 123 -j ACCEPT</code></pre><p><strong>Q2: NTP 服务器不可达</strong></p><p>测试 NTP 服务器连通性：</p><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash"># 测试网络连通性ping ntp.aliyun.com# 测试 NTP 端口nc -u -v ntp.aliyun.com 123</code></pre><p><strong>Q3: 时间偏差过大无法同步</strong></p><p>如果时间偏差超过 1000 秒，NTP 可能拒绝同步。解决方法：</p><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash"># 先手动设置大致时间sudo date -s "2024-01-01 12:00:00"# 然后使用 ntpdate 同步sudo ntpdate -b pool.ntp.org</code></pre><p><strong>Q4: chrony 和 ntpd 冲突</strong></p><p>两者不能同时运行，选择其一：</p><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash"># 停止并禁用 ntpdsudo systemctl stop ntpdsudo systemctl disable ntpd# 启动 chronysudo systemctl start chronydsudo systemctl enable chronyd</code></pre><h2 id="推荐方案"><a href="#推荐方案" class="headerlink" title="推荐方案"></a>推荐方案</h2><p>*<em>推荐1</em>：使用 <code>chrony</code></p><ul><li>同步速度更快</li><li>适应网络变化能力强</li><li>资源占用少</li><li>支持更多场景（虚拟机、移动设备）</li></ul><p><strong>简单场景</strong>：使用 <code>systemd-timesyncd</code></p><ul><li>配置简单</li><li>系统自带</li><li>适合桌面和简单服务器</li></ul><p><strong>不推荐</strong>：定时执行 <code>ntpdate</code></p><ul><li>时间跳变可能影响应用</li><li>不如持续同步准确</li><li>已被标记为过时工具</li></ul>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>Rust 工具链迁移到 D 盘同级目录</title>
      <link href="/2026/04/01/Rust%20%E5%B7%A5%E5%85%B7%E9%93%BE%E8%BF%81%E7%A7%BB%E5%88%B0%20D%20%E7%9B%98%E5%90%8C%E7%BA%A7%E7%9B%AE%E5%BD%95/"/>
      <url>/2026/04/01/Rust%20%E5%B7%A5%E5%85%B7%E9%93%BE%E8%BF%81%E7%A7%BB%E5%88%B0%20D%20%E7%9B%98%E5%90%8C%E7%BA%A7%E7%9B%AE%E5%BD%95/</url>
      
        <content type="html"><![CDATA[<h1 id="Rust-工具链迁移到-D-盘同级目录"><a href="#Rust-工具链迁移到-D-盘同级目录" class="headerlink" title="Rust 工具链迁移到 D 盘同级目录"></a>Rust 工具链迁移到 D 盘同级目录</h1><h2 id="概述"><a href="#概述" class="headerlink" title="概述"></a>概述</h2><p>把 Rust 工具链从 C 盘的用户目录搬到 D 盘。Rust 在 Windows 上默认占用c盘的空间，包括：</p><ul><li>Rustup（Rust 版本管理器）</li><li>Rust 工具链（编译器、标准库这些）</li><li>Cargo 缓存</li><li>Cargo 包注册表</li></ul><h2 id="默认安装位置"><a href="#默认安装位置" class="headerlink" title="默认安装位置"></a>默认安装位置</h2><p>在 Windows 10 上，Rust 相关文件默认装在：</p><pre class="line-numbers language-none"><code class="language-none">C:\Users\cyz\.rustup\      # Rustup 和工具链C:\Users\cyz\.cargo\       # Cargo 缓存和注册表</code></pre><h2 id="迁移步骤"><a href="#迁移步骤" class="headerlink" title="迁移步骤"></a>迁移步骤</h2><h3 id="1-准备工作"><a href="#1-准备工作" class="headerlink" title="1. 准备工作"></a>1. 准备工作</h3><ol><li><p><strong>关闭所有 Rust 相关程序</strong></p><ul><li>把正在运行的 Rust 项目都关了</li><li>关闭 VS Code 或其他 IDE</li><li>确保没有 <code>cargo</code> 或 <code>rustc</code> 进程在运行</li></ul></li><li><p><strong>检查当前 Rust 安装状态</strong></p><pre class="line-numbers language-powershell" data-language="powershell"><code class="language-powershell">rustc --versioncargo --versionrustup show</code></pre></li><li><p><strong>记一下当前安装的工具链</strong></p><pre class="line-numbers language-powershell" data-language="powershell"><code class="language-powershell">rustup show</code></pre><p>把当前安装的工具链和默认工具链记下来，方便迁移后检查。</p></li></ol><h3 id="2-创建目标目录"><a href="#2-创建目标目录" class="headerlink" title="2. 创建目标目录"></a>2. 创建目标目录</h3><p>先在 D 盘创建好 Rust 相关的目录：</p><p><strong>PowerShell 命令：</strong></p><pre class="line-numbers language-powershell" data-language="powershell"><code class="language-powershell"># 创建 rustup 目录New-Item -ItemType Directory -Path "D:\.rustup" -Force# 创建 cargo 目录New-Item -ItemType Directory -Path "D:\.cargo" -Force</code></pre><p><strong>CMD 命令：</strong></p><pre class="line-numbers language-cmd" data-language="cmd"><code class="language-cmd">:: 创建 rustup 目录mkdir "D:\.rustup":: 创建 cargo 目录mkdir "D:\.cargo"</code></pre><h3 id="3-停止-Rustup-服务"><a href="#3-停止-Rustup-服务" class="headerlink" title="3. 停止 Rustup 服务"></a>3. 停止 Rustup 服务</h3><pre class="line-numbers language-powershell" data-language="powershell"><code class="language-powershell"># 如果有 Rustup 服务在运行，先停了它# 一般情况下不需要，但如果遇到问题可以执行：# taskkill /F /IM rustup.exe</code></pre><h3 id="4-迁移文件"><a href="#4-迁移文件" class="headerlink" title="4. 迁移文件"></a>4. 迁移文件</h3><p>用 PowerShell 或 CMD 把文件移过去（会保留文件权限和属性）：</p><p><strong>PowerShell 命令：</strong></p><pre class="line-numbers language-powershell" data-language="powershell"><code class="language-powershell"># 迁移 .rustup 目录robocopy "C:\Users\cyz\.rustup" "D:\.rustup" /E /COPYALL /R:0 /W:0# 迁移 .cargo 目录robocopy "C:\Users\cyz\.cargo" "D:\.cargo" /E /COPYALL /R:0 /W:0</code></pre><p><strong>CMD 命令：</strong></p><pre class="line-numbers language-cmd" data-language="cmd"><code class="language-cmd">:: 迁移 .rustup 目录robocopy "C:\Users\cyz\.rustup" "D:\.rustup" /E /COPYALL /R:0 /W:0:: 迁移 .cargo 目录robocopy "C:\Users\cyz\.cargo" "D:\.cargo" /E /COPYALL /R:0 /W:0</code></pre><p><strong>参数说明：</strong></p><ul><li><code>/E</code> - 复制所有子目录，包括空的</li><li><code>/COPYALL</code> - 复制所有文件信息（包括权限）</li><li><code>/R:0</code> - 失败了不重试</li><li><code>/W:0</code> - 重试等待时间为 0</li></ul><h3 id="5-验证迁移"><a href="#5-验证迁移" class="headerlink" title="5. 验证迁移"></a>5. 验证迁移</h3><p>检查一下文件是不是完整移过去了：</p><p><strong>PowerShell 命令：</strong></p><pre class="line-numbers language-powershell" data-language="powershell"><code class="language-powershell"># 检查目录大小Get-ChildItem "D:\.rustup" -Recurse | Measure-Object -Property Length -SumGet-ChildItem "D:\.cargo" -Recurse | Measure-Object -Property Length -Sum# 检查关键文件是否存在Test-Path "D:\.rustup\settings.toml"Test-Path "D:\.cargo\bin\rustc.exe"</code></pre><p><strong>CMD 命令：</strong></p><pre class="line-numbers language-cmd" data-language="cmd"><code class="language-cmd">:: 检查关键文件是否存在dir "D:\.rustup\settings.toml"dir "D:\.cargo\bin\rustc.exe"</code></pre><h3 id="6-删除原目录（可选）"><a href="#6-删除原目录（可选）" class="headerlink" title="6. 删除原目录（可选）"></a>6. 删除原目录（可选）</h3><p>确认迁移成功后，就可以把原目录删掉了：</p><p><strong>PowerShell 命令：</strong></p><pre class="line-numbers language-powershell" data-language="powershell"><code class="language-powershell"># 先备份一下（建议先重命名）Rename-Item "C:\Users\cyz\.rustup" "C:\Users\cyz\.rustup.backup"Rename-Item "C:\Users\cyz\.cargo" "C:\Users\cyz\.cargo.backup"# 确认没问题后删除Remove-Item "C:\Users\cyz\.rustup.backup" -Recurse -ForceRemove-Item "C:\Users\cyz\.cargo.backup" -Recurse -Force</code></pre><p><strong>CMD 命令：</strong></p><pre class="line-numbers language-cmd" data-language="cmd"><code class="language-cmd">:: 先备份一下（建议先重命名）ren "C:\Users\cyz\.rustup" ".rustup.backup"ren "C:\Users\cyz\.cargo" ".cargo.backup":: 确认没问题后删除rmdir /s /q "C:\Users\cyz\.rustup.backup"rmdir /s /q "C:\Users\cyz\.cargo.backup"</code></pre><h2 id="创建软链接"><a href="#创建软链接" class="headerlink" title="创建软链接"></a>创建软链接</h2><h3 id="方法：用-mklink-命令（需要管理员权限）"><a href="#方法：用-mklink-命令（需要管理员权限）" class="headerlink" title="方法：用 mklink 命令（需要管理员权限）"></a>方法：用 mklink 命令（需要管理员权限）</h3><ol><li><p><strong>以管理员身份打开命令提示符</strong></p><ul><li>右键点击「命令提示符」图标</li><li>选择「以管理员身份运行」</li></ul></li><li><p><strong>创建软链接</strong><br><strong>CMD 命令：</strong></p><pre class="line-numbers language-cmd" data-language="cmd"><code class="language-cmd">:: 删除原目录（如果还存在）rd /s /q "C:\Users\cyz\.rustup" 2&gt;nulrd /s /q "C:\Users\cyz\.cargo" 2&gt;nul:: 创建软链接mklink /D "C:\Users\cyz\.rustup" "D:\.rustup"mklink /D "C:\Users\cyz\.cargo" "D:\.cargo"</code></pre></li></ol><h3 id="方法二：用-PowerShell-命令"><a href="#方法二：用-PowerShell-命令" class="headerlink" title="方法二：用 PowerShell 命令"></a>方法二：用 PowerShell 命令</h3><ol><li><p><strong>以管理员身份打开 PowerShell</strong></p><ul><li>右键点击 PowerShell 图标</li><li>选择「以管理员身份运行」</li></ul></li><li><p><strong>创建软链接</strong><br><strong>PowerShell 命令：</strong></p><pre class="line-numbers language-powershell" data-language="powershell"><code class="language-powershell"># 删除原目录（如果还存在）Remove-Item "C:\Users\cyz\.rustup" -Recurse -Force -ErrorAction SilentlyContinueRemove-Item "C:\Users\cyz\.cargo" -Recurse -Force -ErrorAction SilentlyContinue# 创建符号链接New-Item -ItemType SymbolicLink -Path "C:\Users\cyz\.rustup" -Target "D:\.rustup"New-Item -ItemType SymbolicLink -Path "C:\Users\cyz\.cargo" -Target "D:\.cargo"</code></pre></li></ol><p><strong>注意：</strong> 在 Windows 10 上创建符号链接通常需要开发者模式或管理员权限。</p><h2 id="配置环境变量（可选）"><a href="#配置环境变量（可选）" class="headerlink" title="配置环境变量（可选）"></a>配置环境变量（可选）</h2><p>如果 Rustup 没能正确识别新位置，可以手动设置环境变量：</p><ol><li><p><strong>打开环境变量设置</strong></p><ul><li>右键点击「此电脑」→「属性」</li><li>点击「高级系统设置」</li><li>点击「环境变量」</li></ul></li><li><p><strong>添加或修改以下环境变量</strong>（在用户变量里）：</p><pre class="line-numbers language-none"><code class="language-none">RUSTUP_HOME = D:\.rustupCARGO_HOME = D:\.cargo</code></pre></li><li><p><strong>更新 PATH 环境变量</strong></p><ul><li>把 <code>C:\Users\cyz\.cargo\bin</code> 改成 <code>D:\.cargo\bin</code></li></ul></li></ol><h2 id="验证迁移结果"><a href="#验证迁移结果" class="headerlink" title="验证迁移结果"></a>验证迁移结果</h2><h3 id="1-验证软链接"><a href="#1-验证软链接" class="headerlink" title="1. 验证软链接"></a>1. 验证软链接</h3><p><strong>PowerShell 命令：</strong></p><pre class="line-numbers language-powershell" data-language="powershell"><code class="language-powershell"># 检查软链接是否创建成功Get-Item "C:\Users\cyz\.rustup" | Select-Object LinkType, TargetGet-Item "C:\Users\cyz\.cargo" | Select-Object LinkType, Target</code></pre><p><strong>CMD 命令：</strong></p><pre class="line-numbers language-cmd" data-language="cmd"><code class="language-cmd">:: 检查软链接是否创建成功dir "C:\Users\cyz" | findstr "rustup cargo"</code></pre><p>应该显示类似这样的结果：</p><pre class="line-numbers language-none"><code class="language-none">LinkType  Target-------  ------SymbolicLink D:\.rustup</code></pre><h3 id="2-验证-Rust-功能"><a href="#2-验证-Rust-功能" class="headerlink" title="2. 验证 Rust 功能"></a>2. 验证 Rust 功能</h3><p><strong>PowerShell 命令：</strong></p><pre class="line-numbers language-powershell" data-language="powershell"><code class="language-powershell"># 检查 Rust 版本rustc --versioncargo --version# 检查工具链rustup show# 检查 Rustup 主目录rustup which rustc# 测试编译一个简单项目cargo new test_projectcd test_projectcargo build</code></pre><p><strong>CMD 命令：</strong></p><pre class="line-numbers language-cmd" data-language="cmd"><code class="language-cmd">:: 检查 Rust 版本rustc --versioncargo --version:: 检查工具链rustup show:: 检查 Rustup 主目录rustup which rustc:: 测试编译一个简单项目cargo new test_projectcd test_projectcargo build</code></pre><h3 id="3-检查磁盘空间"><a href="#3-检查磁盘空间" class="headerlink" title="3. 检查磁盘空间"></a>3. 检查磁盘空间</h3><p><strong>PowerShell 命令：</strong></p><pre class="line-numbers language-powershell" data-language="powershell"><code class="language-powershell"># 检查 C 盘空间Get-PSDrive C# 检查 D 盘 Rust 目录大小Get-ChildItem "D:\.rustup" -Recurse | Measure-Object -Property Length -SumGet-ChildItem "D:\.cargo" -Recurse | Measure-Object -Property Length -Sum</code></pre><p><strong>CMD 命令：</strong></p><pre class="line-numbers language-cmd" data-language="cmd"><code class="language-cmd">:: 检查 C 盘空间fsutil volume diskfree C::: 检查 D 盘空间fsutil volume diskfree D:</code></pre><h2 id="常见问题"><a href="#常见问题" class="headerlink" title="常见问题"></a>常见问题</h2><h3 id="Q1-权限不足，没法创建软链接"><a href="#Q1-权限不足，没法创建软链接" class="headerlink" title="Q1: 权限不足，没法创建软链接"></a>Q1: 权限不足，没法创建软链接</h3><p><strong>解决办法：</strong></p><ul><li>以管理员身份运行命令提示符或 PowerShell</li><li>或者启用开发者模式：设置 → 更新和安全 → 开发者选项 → 开发者模式</li></ul><h3 id="Q2-Rust-命令找不到"><a href="#Q2-Rust-命令找不到" class="headerlink" title="Q2: Rust 命令找不到"></a>Q2: Rust 命令找不到</h3><p><strong>解决办法：</strong></p><ol><li>检查 PATH 环境变量里有没有 <code>D:\.cargo\bin</code></li><li>重新打开终端</li><li>如果还是不行，重新安装 Rustup：<pre class="line-numbers language-powershell" data-language="powershell"><code class="language-powershell"># 卸载 Rustuprustup self uninstall# 重新安装（指定安装目录）$env:RUSTUP_HOME="D:\.rustup"$env:CARGO_HOME="D:\.cargo"Invoke-WebRequest -Uri https://win.rustup.rs/x86_64 -OutFile rustup-init.exe.\rustup-init.exe</code></pre></li></ol><h3 id="Q3-Cargo-缓存还是占了很多空间"><a href="#Q3-Cargo-缓存还是占了很多空间" class="headerlink" title="Q3: Cargo 缓存还是占了很多空间"></a>Q3: Cargo 缓存还是占了很多空间</h3><p><strong>解决办法：</strong><br>清理 Cargo 缓存：</p><pre class="line-numbers language-powershell" data-language="powershell"><code class="language-powershell">cargo cache --autoclean</code></pre><p>或者手动清理：</p><pre class="line-numbers language-powershell" data-language="powershell"><code class="language-powershell">Remove-Item "D:\.cargo\registry\cache" -Recurse -Force</code></pre><h3 id="Q4-迁移后编译失败"><a href="#Q4-迁移后编译失败" class="headerlink" title="Q4: 迁移后编译失败"></a>Q4: 迁移后编译失败</h3><p><strong>解决办法：</strong></p><ol><li><p>清理项目构建缓存：</p><pre class="line-numbers language-powershell" data-language="powershell"><code class="language-powershell">cargo clean</code></pre></li><li><p>重新构建项目：</p><pre class="line-numbers language-powershell" data-language="powershell"><code class="language-powershell">cargo build</code></pre></li><li><p>如果还是有问题，检查工具链是否完整：</p><pre class="line-numbers language-powershell" data-language="powershell"><code class="language-powershell">rustup updaterustup component add rust-src</code></pre></li></ol><h2 id="维护建议"><a href="#维护建议" class="headerlink" title="维护建议"></a>维护建议</h2><h3 id="定期清理"><a href="#定期清理" class="headerlink" title="定期清理"></a>定期清理</h3><p><strong>PowerShell 命令：</strong></p><pre class="line-numbers language-powershell" data-language="powershell"><code class="language-powershell"># 清理 Cargo 缓存cargo cache --autoclean# 清理旧的工具链rustup self uninstallrustup update</code></pre><p><strong>CMD 命令：</strong></p><pre class="line-numbers language-cmd" data-language="cmd"><code class="language-cmd">:: 清理 Cargo 缓存cargo cache --autoclean:: 清理旧的工具链rustup self uninstallrustup update</code></pre><h3 id="监控空间使用"><a href="#监控空间使用" class="headerlink" title="监控空间使用"></a>监控空间使用</h3><p><strong>PowerShell 命令：</strong></p><pre class="line-numbers language-powershell" data-language="powershell"><code class="language-powershell"># 查看 Rust 相关目录大小Get-ChildItem "D:\.rustup" -Recurse | Measure-Object -Property Length -Sum | Select-Object @{Name="Size(GB)";Expression={[math]::Round($_.Sum / 1GB, 2)}}Get-ChildItem "D:\.cargo" -Recurse | Measure-Object -Property Length -Sum | Select-Object @{Name="Size(GB)";Expression={[math]::Round($_.Sum / 1GB, 2)}}# 查看 Cargo 缓存大小Get-ChildItem "D:\.cargo\registry" -Recurse | Measure-Object -Property Length -Sum | Select-Object @{Name="Size(GB)";Expression={[math]::Round($_.Sum / 1GB, 2)}}</code></pre><p><strong>CMD 命令：</strong></p><pre class="line-numbers language-cmd" data-language="cmd"><code class="language-cmd">:: 查看目录大小（用 dir 命令）dir "D:\.rustup" /sdir "D:\.cargo" /sdir "D:\.cargo\registry" /s</code></pre><h2 id="回滚步骤"><a href="#回滚步骤" class="headerlink" title="回滚步骤"></a>回滚步骤</h2><p>如果迁移后出问题了，可以按下面的步骤回滚：</p><ol><li><p><strong>删除软链接</strong><br><strong>PowerShell 命令：</strong></p><pre class="line-numbers language-powershell" data-language="powershell"><code class="language-powershell">Remove-Item "C:\Users\cyz\.rustup" -ForceRemove-Item "C:\Users\cyz\.cargo" -Force</code></pre><p><strong>CMD 命令：</strong></p><pre class="line-numbers language-cmd" data-language="cmd"><code class="language-cmd">:: 删除软链接rd "C:\Users\cyz\.rustup"rd "C:\Users\cyz\.cargo"</code></pre></li><li><p><strong>恢复备份</strong><br><strong>PowerShell 命令：</strong></p><pre class="line-numbers language-powershell" data-language="powershell"><code class="language-powershell">Move-Item "C:\Users\cyz\.rustup.backup" "C:\Users\cyz\.rustup"Move-Item "C:\Users\cyz\.cargo.backup" "C:\Users\cyz\.cargo"</code></pre><p><strong>CMD 命令：</strong></p><pre class="line-numbers language-cmd" data-language="cmd"><code class="language-cmd">:: 恢复备份ren "C:\Users\cyz\.rustup.backup" ".rustup"ren "C:\Users\cyz\.cargo.backup" ".cargo"</code></pre></li><li><p><strong>验证</strong><br><strong>PowerShell 命令：</strong></p><pre class="line-numbers language-powershell" data-language="powershell"><code class="language-powershell">rustc --versioncargo --version</code></pre><p><strong>CMD 命令：</strong></p><pre class="line-numbers language-cmd" data-language="cmd"><code class="language-cmd">:: 验证rustc --versioncargo --version</code></pre></li></ol><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>按照上面的步骤，你就能成功把 Rust 工具链从用户目录迁移到 D 盘，然后通过软链接保持原来的路径，这样既节省了 C 盘空间，又不影响 Rust 的正常使用。</p><p>迁移完成后，建议：</p><ol><li>定期清理 Cargo 缓存</li><li>注意监控磁盘空间使用情况</li><li>保持 Rust 工具链更新</li><li>定期备份重要配置文件</li></ol>]]></content>
      
      
      
        <tags>
            
            <tag> Rust </tag>
            
            <tag> Windows </tag>
            
            <tag> 迁移 </tag>
            
            <tag> 软链接 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>压测 TPS 波动排查逻辑test</title>
      <link href="/2026/04/01/testmermaid/"/>
      <url>/2026/04/01/testmermaid/</url>
      
        <content type="html"><![CDATA[<pre><code class="mermaid">graph TD    A[监控到TPS忽高忽低] --&gt; B{查看GC日志}    B -- 频繁Full GC --&gt; C[优化JVM堆内存]    B -- GC正常 --&gt; D[检查数据库连接池]</code></pre>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>Groovy (JSR223) 字符串语法</title>
      <link href="/2026/02/28/Groovy%20(JSR223)%20%E5%AD%97%E7%AC%A6%E4%B8%B2%E8%AF%AD%E6%B3%95/"/>
      <url>/2026/02/28/Groovy%20(JSR223)%20%E5%AD%97%E7%AC%A6%E4%B8%B2%E8%AF%AD%E6%B3%95/</url>
      
        <content type="html"><![CDATA[<h1 id="Groovy-JSR223-的字符串语法-⭐⭐⭐⭐⭐"><a href="#Groovy-JSR223-的字符串语法-⭐⭐⭐⭐⭐" class="headerlink" title="Groovy (JSR223) 的字符串语法 ⭐⭐⭐⭐⭐"></a>Groovy (JSR223) 的字符串语法 ⭐⭐⭐⭐⭐</h1><h2 id="1-单引号字符串（不支持插值）"><a href="#1-单引号字符串（不支持插值）" class="headerlink" title="1. 单引号字符串（不支持插值）"></a>1. 单引号字符串（不支持插值）</h2><pre class="line-numbers language-groovy" data-language="groovy"><code class="language-groovy">def str = 'Hello World'def name = 'Tom'def str2 = 'Hello ${name}'  // 输出: Hello ${name} (不会插值)</code></pre><h2 id="2-双引号字符串（支持插值）"><a href="#2-双引号字符串（支持插值）" class="headerlink" title="2. 双引号字符串（支持插值）"></a>2. 双引号字符串（支持插值）</h2><pre class="line-numbers language-groovy" data-language="groovy"><code class="language-groovy">def name = 'Tom'def str = "Hello ${name}"  // 输出: Hello Tomdef str2 = "Line1\nLine2"  // 支持转义</code></pre><h2 id="3-三单引号（多行字符串，不转义，不插值）⭐⭐⭐"><a href="#3-三单引号（多行字符串，不转义，不插值）⭐⭐⭐" class="headerlink" title="3. 三单引号（多行字符串，不转义，不插值）⭐⭐⭐"></a>3. 三单引号（多行字符串，不转义，不插值）⭐⭐⭐</h2><pre class="line-numbers language-groovy" data-language="groovy"><code class="language-groovy">def json = '''{  "name": "张三",  "path": "C:\\Users\\test\\file.txt",  "regex": "\\d+\\.\\d+",  "quote": "He said 'hello'"}'''// 所有字符都按原样输出，无需转义</code></pre><h2 id="4-三双引号（多行字符串，不转义，支持插值）⭐⭐⭐⭐⭐"><a href="#4-三双引号（多行字符串，不转义，支持插值）⭐⭐⭐⭐⭐" class="headerlink" title="4. 三双引号（多行字符串，不转义，支持插值）⭐⭐⭐⭐⭐"></a>4. 三双引号（多行字符串，不转义，支持插值）⭐⭐⭐⭐⭐</h2><pre class="line-numbers language-groovy" data-language="groovy"><code class="language-groovy">def name = "张三"def userId = "10001"def json = """{  "name": "${name}",  "userId": "${userId}",  "path": "C:\\Users\\test\\file.txt",  "regex": "\\d+\\.\\d+",  "message": "He said 'hello' and \"goodbye\""}"""// 支持变量插值，且无需转义反斜杠和引号</code></pre><h2 id="5-斜杠字符串（正则表达式专用）"><a href="#5-斜杠字符串（正则表达式专用）" class="headerlink" title="5. 斜杠字符串（正则表达式专用）"></a>5. 斜杠字符串（正则表达式专用）</h2><pre class="line-numbers language-groovy" data-language="groovy"><code class="language-groovy">def pattern = /\d+\.\d+/  // 无需转义反斜杠def path = /C:\Users\test\file.txt/  // 路径也很方便</code></pre><h1 id="二、BeanShell-的字符串语法（受限）"><a href="#二、BeanShell-的字符串语法（受限）" class="headerlink" title="二、BeanShell 的字符串语法（受限）"></a>二、BeanShell 的字符串语法（受限）</h1><h2 id="1-只支持双引号字符串"><a href="#1-只支持双引号字符串" class="headerlink" title="1. 只支持双引号字符串"></a>1. 只支持双引号字符串</h2><pre class="line-numbers language-java" data-language="java"><code class="language-java">String str = "Hello World";String name = "Tom";String str2 = "Hello " + name;  // 必须用 + 拼接</code></pre><h2 id="2-必须转义特殊字符"><a href="#2-必须转义特殊字符" class="headerlink" title="2. 必须转义特殊字符"></a>2. 必须转义特殊字符</h2><pre class="line-numbers language-java" data-language="java"><code class="language-java">// ❌ BeanShell 不支持三引号String json = """{  "name": "test"}""";  // 语法错误！// ✅ 必须这样写（痛苦）String json = "{\n" +              "  \"name\": \"test\",\n" +              "  \"path\": \"C:\\\\Users\\\\test\\\\file.txt\"\n" +              "}";</code></pre><h2 id="3-BeanShell-的多行字符串解决方案"><a href="#3-BeanShell-的多行字符串解决方案" class="headerlink" title="3. BeanShell 的多行字符串解决方案"></a>3. BeanShell 的多行字符串解决方案</h2><h3 id="方案A-字符串拼接（传统方式）"><a href="#方案A-字符串拼接（传统方式）" class="headerlink" title="方案A: 字符串拼接（传统方式）"></a>方案A: 字符串拼接（传统方式）</h3><pre class="line-numbers language-java" data-language="java"><code class="language-java">String json = "{" +    "\"code\": 0," +    "\"message\": \"success\"," +    "\"data\": {" +        "\"userId\": \"10001\"," +        "\"name\": \"张三\"," +        "\"path\": \"C:\\\\Users\\\\test\\\\file.txt\"" +    "}" +"}";</code></pre><h3 id="方案B-StringBuilder（稍好一点）"><a href="#方案B-StringBuilder（稍好一点）" class="headerlink" title="方案B: StringBuilder（稍好一点）"></a>方案B: StringBuilder（稍好一点）</h3><pre class="line-numbers language-java" data-language="java"><code class="language-java">StringBuilder sb = new StringBuilder();sb.append("{\n");sb.append("  \"code\": 0,\n");sb.append("  \"message\": \"success\",\n");sb.append("  \"data\": {\n");sb.append("    \"userId\": \"10001\",\n");sb.append("    \"name\": \"张三\",\n");sb.append("    \"path\": \"C:\\\\Users\\\\test\\\\file.txt\"\n");sb.append("  \n");sb.append("}");String json = sb.toString();</code></pre><h3 id="方案C-使用-JSON-库（推荐）⭐⭐⭐"><a href="#方案C-使用-JSON-库（推荐）⭐⭐⭐" class="headerlink" title="方案C: 使用 JSON 库（推荐）⭐⭐⭐"></a>方案C: 使用 JSON 库（推荐）⭐⭐⭐</h3><pre class="line-numbers language-java" data-language="java"><code class="language-java">import org.json.JSONObject;JSONObject json = new JSONObject();json.put("code", 0);json.put("message", "success");JSONObject data = new JSONObject();data.put("userId", "10001");data.put("name", "张三");data.put("path", "C:\Users\test\file.txt");  // 只需转义一次json.put("data", data);String jsonStr = json.toString(2);  // 2 表示缩进</code></pre><h1 id="三、实际对比示例"><a href="#三、实际对比示例" class="headerlink" title="三、实际对比示例"></a>三、实际对比示例</h1><h2 id="场景：返回包含特殊字符的-JSON"><a href="#场景：返回包含特殊字符的-JSON" class="headerlink" title="场景：返回包含特殊字符的 JSON"></a>场景：返回包含特殊字符的 JSON</h2><h3 id="Groovy-JSR223-优雅-⭐⭐⭐⭐⭐"><a href="#Groovy-JSR223-优雅-⭐⭐⭐⭐⭐" class="headerlink" title="Groovy (JSR223) - 优雅 ⭐⭐⭐⭐⭐"></a>Groovy (JSR223) - 优雅 ⭐⭐⭐⭐⭐</h3><pre class="line-numbers language-groovy" data-language="groovy"><code class="language-groovy">def userId = vars.get("userId") ?: "10001"def filePath = "C:\Users\test\data.txt"// 方式1: 三双引号（最推荐）def json = """{  "code": 0,  "message": "success",  "data": {    "userId": "${userId}",    "name": "张三",    "path": "${filePath}",    "regex": "\\d+\\.\\d+",    "quote": "He said 'hello' and \"goodbye\"",    "sql": "SELECT * FROM users WHERE name = 'Tom'"  }}"""SampleResult.setResponseData(json, "UTF-8")SampleResult.setSuccessful(true)// 方式2: 三单引号（无插值）def json = '''{  "code": 0,  "message": "success",  "data": {    "path": "C:\Users\test\data.txt",    "regex": "\\d+\\.\\d+",    "quote": "He said 'hello'"  }}'''</code></pre><h3 id="BeanShell-痛苦-⭐"><a href="#BeanShell-痛苦-⭐" class="headerlink" title="BeanShell - 痛苦 ⭐"></a>BeanShell - 痛苦 ⭐</h3><pre class="line-numbers language-java" data-language="java"><code class="language-java">String userId = vars.get("userId");if (userId == null) userId = "10001";// 方式1: 字符串拼接（痛苦）String json = "{" +    "\"code\": 0," +    "\"message\": \"success\"," +    "\"data\": {" +        "\"userId\": \"" + userId + "\"," +        "\"name\": \"张三\"," +        "\"path\": \"C:\\\\Users\\\\test\\\\data.txt\"," +        "\"regex\": \"\\\\d+\\\\.\\\\d+\"," +        "\"quote\": \"He said 'hello' and \\\"goodbye\\\"\"" +    "}" +"}";ResponseData = json;IsSuccess = true;// 方式2: 使用 JSON 库（推荐）import org.json.JSONObject;JSONObject response = new JSONObject();response.put("code", 0);response.put("message", "success");JSONObject data = new JSONObject();data.put("userId", userId);data.put("name", "张三");data.put("path", "C:\Users\test\data.txt");data.put("regex", "\\d+\\.\\d+");data.put("quote", "He said 'hello' and \"goodbye\"");response.put("data", data);ResponseData = response.toString(2);IsSuccess = true;</code></pre><h1 id="四、转义字符对比表"><a href="#四、转义字符对比表" class="headerlink" title="四、转义字符对比表"></a>四、转义字符对比表</h1><table><thead><tr><th>字符</th><th>Groovy 单/双引号</th><th>Groovy 三引号</th><th>BeanShell</th></tr></thead><tbody><tr><td>换行 \n</td><td>\n</td><td>直接换行</td><td>\n</td></tr><tr><td>反斜杠 \</td><td>\</td><td>\</td><td>\</td></tr><tr><td>双引号 “</td><td>"</td><td>“</td><td>\“</td></tr><tr><td>单引号 ‘</td><td>‘</td><td>‘</td><td>‘</td></tr><tr><td>制表符 \t</td><td>\t</td><td>直接 Tab</td><td>\t</td></tr><tr><td>路径 C:\test</td><td>C:\test</td><td>C:\test</td><td>C:\\test</td></tr><tr><td>正则 \d+</td><td>\d+</td><td>\d+</td><td>\\d+</td></tr></tbody></table><h1 id="五、完整实战对比"><a href="#五、完整实战对比" class="headerlink" title="五、完整实战对比"></a>五、完整实战对比</h1><h2 id="需求：返回包含-SQL、正则、路径的复杂-JSON"><a href="#需求：返回包含-SQL、正则、路径的复杂-JSON" class="headerlink" title="需求：返回包含 SQL、正则、路径的复杂 JSON"></a>需求：返回包含 SQL、正则、路径的复杂 JSON</h2><h3 id="Groovy-实现（简洁）"><a href="#Groovy-实现（简洁）" class="headerlink" title="Groovy 实现（简洁）"></a>Groovy 实现（简洁）</h3><pre class="line-numbers language-groovy" data-language="groovy"><code class="language-groovy">def userId = vars.get("userId") ?: "10001"def userName = vars.get("userName") ?: "张三"def response = """{  "code": 0,  "message": "查询成功",  "data": {    "userId": "${userId}",    "userName": "${userName}",    "sql": "SELECT * FROM users WHERE name = 'Tom' AND age &gt; 18",    "regex": "^\\d{3}-\\d{4}-\\d{4}$",    "filePath": "C:\Users\test\documents\report.xlsx",    "jsonPath": "$..books[?(@.price &lt; 10)]",    "xpath": "//div[@class='content']//p[1]",    "description": "He said: \"Hello, I'm Tom\" and she replied 'Hi!'"  },  "timestamp": ${System.currentTimeMillis()}}"""SampleResult.setResponseData(response, "UTF-8")SampleResult.setResponseCode("200")SampleResult.setSuccessful(true)</code></pre><h3 id="BeanShell-实现（复杂）"><a href="#BeanShell-实现（复杂）" class="headerlink" title="BeanShell 实现（复杂）"></a>BeanShell 实现（复杂）</h3><pre class="line-numbers language-java" data-language="java"><code class="language-java">import org.json.JSONObject;String userId = vars.get("userId");if (userId == null) userId = "10001";String userName = vars.get("userName");if (userName == null) userName = "张三";JSONObject response = new JSONObject();response.put("code", 0);response.put("message", "查询成功");JSONObject data = new JSONObject();data.put("userId", userId);data.put("userName", userName);data.put("sql", "SELECT * FROM users WHERE name = 'Tom' AND age &gt; 18");data.put("regex", "^\\d{3}-\\d{4}-\\d{4}$");data.put("filePath", "C:\Users\test\documents\report.xlsx");data.put("jsonPath", "$..books[?(@.price &lt; 10)]");data.put("xpath", "//div[@class='content']//p[1]");data.put("description", "He said: \"Hello, I'm Tom\" and she replied 'Hi!'");response.put("data", data);response.put("timestamp", System.currentTimeMillis());ResponseData = response.toString(2);ResponseCode = "200";IsSuccess = true;</code></pre><h1 id="六、对比"><a href="#六、对比" class="headerlink" title="六、对比"></a>六、对比</h1><table><thead><tr><th>特性</th><th>Groovy (JSR223)</th><th>BeanShell</th></tr></thead><tbody><tr><td>三引号支持</td><td>✅ 支持 ‘’’ 和 “””</td><td>❌ 不支持</td></tr><tr><td>字符串插值</td><td>✅ “Hello ${name}”</td><td>❌ 必须用 + 拼接</td></tr><tr><td>多行字符串</td><td>✅ 原生支持</td><td>❌ 需要 + 或 StringBuilder</td></tr><tr><td>转义复杂度</td><td>⭐ 简单</td><td>⭐⭐⭐⭐⭐ 复杂</td></tr><tr><td>代码可读性</td><td>⭐⭐⭐⭐⭐ 优秀</td><td>⭐⭐ 较差</td></tr><tr><td>推荐度</td><td>⭐⭐⭐⭐⭐</td><td>⭐ 不推荐</td></tr></tbody></table><h2 id="建议"><a href="#建议" class="headerlink" title="建议"></a>建议</h2><ul><li><strong>新项目</strong>: 强烈推荐使用 JSR223 + Groovy 的三引号语法</li><li><strong>旧项目</strong>: 如果必须用 BeanShell，使用 JSON 库而不是字符串拼接</li><li><strong>迁移</strong>: 将 BeanShell 脚本迁移到 JSR223，可以大幅简化代码</li></ul>]]></content>
      
      
      <categories>
          
          <category> groove </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Groovy </tag>
            
            <tag> JSR223 </tag>
            
            <tag> 字符串语法 </tag>
            
            <tag> BeanShell </tag>
            
            <tag> 对比 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>数据库性能瓶颈初步诊断</title>
      <link href="/2026/02/26/%E6%95%B0%E6%8D%AE%E5%BA%93%E6%80%A7%E8%83%BD%E7%93%B6%E9%A2%88%E5%88%9D%E6%AD%A5%E8%AF%8A%E6%96%AD/"/>
      <url>/2026/02/26/%E6%95%B0%E6%8D%AE%E5%BA%93%E6%80%A7%E8%83%BD%E7%93%B6%E9%A2%88%E5%88%9D%E6%AD%A5%E8%AF%8A%E6%96%AD/</url>
      
        <content type="html"><![CDATA[<ul><li><h1 id="数据库性能瓶颈初步诊断"><a href="#数据库性能瓶颈初步诊断" class="headerlink" title="数据库性能瓶颈初步诊断"></a>数据库性能瓶颈初步诊断</h1><h2 id="概述"><a href="#概述" class="headerlink" title="概述"></a>概述</h2><p>数据库性能瓶颈通常表现在以下几个方面：</p><ol><li><strong>慢查询</strong> - SQL 执行时间过长</li><li><strong>连接数过多</strong> - 连接池耗尽</li><li><strong>锁等待</strong> - 事务锁竞争</li><li><strong>I/O 瓶颈</strong> - 磁盘读写慢</li><li><strong>CPU 瓶颈</strong> - 复杂计算或全表扫描</li><li><strong>内存不足</strong> - 缓存命中率低</li></ol><p>本章涵盖 MySQL、PostgreSQL、Redis、MongoDB 等主流数据库。</p><h2 id="一、MySQL-性能诊断"><a href="#一、MySQL-性能诊断" class="headerlink" title="一、MySQL 性能诊断"></a>一、MySQL 性能诊断</h2><h3 id="1-1-快速诊断-查看数据库状态"><a href="#1-1-快速诊断-查看数据库状态" class="headerlink" title="1.1 快速诊断 - 查看数据库状态"></a>1.1 快速诊断 - 查看数据库状态</h3><pre class="line-numbers language-sql" data-language="sql"><code class="language-sql">-- 查看数据库运行状态SHOW STATUS;-- 查看当前连接数SHOW STATUS LIKE 'Threads_connected';SHOW STATUS LIKE 'Max_used_connections';-- 查看慢查询数量SHOW STATUS LIKE 'Slow_queries';-- 查看 QPS (每秒查询数)SHOW GLOBAL STATUS LIKE 'Questions';SHOW GLOBAL STATUS LIKE 'Uptime';-- QPS = Questions / Uptime-- 查看 TPS (每秒事务数)SHOW GLOBAL STATUS LIKE 'Com_commit';SHOW GLOBAL STATUS LIKE 'Com_rollback';</code></pre><h3 id="1-2-慢查询诊断"><a href="#1-2-慢查询诊断" class="headerlink" title="1.2 慢查询诊断"></a>1.2 慢查询诊断</h3><p><strong>开启慢查询日志</strong>：</p><pre class="line-numbers language-sql" data-language="sql"><code class="language-sql">-- 查看慢查询配置SHOW VARIABLES LIKE 'slow_query%';SHOW VARIABLES LIKE 'long_query_time';-- 开启慢查询日志（临时生效）SET GLOBAL slow_query_log = 'ON';SET GLOBAL long_query_time = 2;  -- 超过2秒的查询记录SET GLOBAL log_queries_not_using_indexes = 'ON';  -- 记录未使用索引的查询</code></pre><p><strong>永久配置（my.cnf）</strong>：</p><pre class="line-numbers language-ini" data-language="ini"><code class="language-ini">[mysqld]slow_query_log = 1slow_query_log_file = /var/log/mysql/slow.loglong_query_time = 2log_queries_not_using_indexes = 1</code></pre><p><strong>分析慢查询日志</strong>：</p><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash"># 使用 mysqldumpslow 分析mysqldumpslow -s t -t 10 /var/log/mysql/slow.log# -s t: 按查询时间排序# -t 10: 显示前10条# 按查询次数排序mysqldumpslow -s c -t 10 /var/log/mysql/slow.log# 使用 pt-query-digest（更强大）pt-query-digest /var/log/mysql/slow.log &gt; slow_report.txt</code></pre><h3 id="1-3-实时查看正在执行的查询"><a href="#1-3-实时查看正在执行的查询" class="headerlink" title="1.3 实时查看正在执行的查询"></a>1.3 实时查看正在执行的查询</h3><pre class="line-numbers language-sql" data-language="sql"><code class="language-sql">-- 查看当前所有连接和正在执行的查询SHOW PROCESSLIST;-- 查看完整 SQL（不截断）SHOW FULL PROCESSLIST;-- 查看执行时间超过2秒的查询SELECT * FROM information_schema.PROCESSLIST WHERE COMMAND != 'Sleep' AND TIME &gt; 2 ORDER BY TIME DESC;-- 杀死慢查询KILL &lt;process_id&gt;;</code></pre><h3 id="1-4-SQL-执行计划分析"><a href="#1-4-SQL-执行计划分析" class="headerlink" title="1.4 SQL 执行计划分析"></a>1.4 SQL 执行计划分析</h3><pre class="line-numbers language-sql" data-language="sql"><code class="language-sql">-- 使用 EXPLAIN 分析查询EXPLAIN SELECT * FROM users WHERE email = 'test@example.com';-- 更详细的分析EXPLAIN FORMAT=JSON SELECT * FROM users WHERE email = 'test@example.com';-- 查看实际执行统计（MySQL 8.0+）EXPLAIN ANALYZE SELECT * FROM users WHERE email = 'test@example.com';</code></pre><p><strong>EXPLAIN 关键字段解读</strong>：</p><table><thead><tr><th>字段</th><th>含义</th><th>优化建议</th></tr></thead><tbody><tr><td><code>type</code></td><td>连接类型</td><td>ALL(全表扫描)最差，const/eq_ref最好</td></tr><tr><td><code>possible_keys</code></td><td>可能使用的索引</td><td>NULL 说明没有可用索引</td></tr><tr><td><code>key</code></td><td>实际使用的索引</td><td>NULL 说明未使用索引</td></tr><tr><td><code>rows</code></td><td>扫描的行数</td><td>越少越好</td></tr><tr><td><code>Extra</code></td><td>额外信息</td><td>Using filesort/Using temporary 需要优化</td></tr></tbody></table><p><strong>type 类型从好到坏</strong>：</p><pre class="line-numbers language-none"><code class="language-none">system &gt; const &gt; eq_ref &gt; ref &gt; range &gt; index &gt; ALL</code></pre><h3 id="1-5-索引分析"><a href="#1-5-索引分析" class="headerlink" title="1.5 索引分析"></a>1.5 索引分析</h3><pre class="line-numbers language-sql" data-language="sql"><code class="language-sql">-- 查看表的索引SHOW INDEX FROM table_name;-- 查看索引使用情况SELECT * FROM sys.schema_unused_indexes;-- 查看冗余索引SELECT * FROM sys.schema_redundant_indexes;-- 查看索引统计信息SHOW TABLE STATUS LIKE 'table_name';-- 分析表（更新索引统计信息）ANALYZE TABLE table_name;</code></pre><h3 id="1-6-锁等待诊断"><a href="#1-6-锁等待诊断" class="headerlink" title="1.6 锁等待诊断"></a>1.6 锁等待诊断</h3><pre class="line-numbers language-sql" data-language="sql"><code class="language-sql">-- 查看当前锁等待情况（MySQL 5.7+）SELECT * FROM performance_schema.data_locks;SELECT * FROM performance_schema.data_lock_waits;-- 查看 InnoDB 锁等待SHOW ENGINE INNODB STATUS\G-- 查看事务信息SELECT * FROM information_schema.INNODB_TRX;-- 查看锁等待超时设置SHOW VARIABLES LIKE 'innodb_lock_wait_timeout';</code></pre><h3 id="1-7-连接数诊断"><a href="#1-7-连接数诊断" class="headerlink" title="1.7 连接数诊断"></a>1.7 连接数诊断</h3><pre class="line-numbers language-sql" data-language="sql"><code class="language-sql">-- 查看最大连接数配置SHOW VARIABLES LIKE 'max_connections';-- 查看当前连接数SHOW STATUS LIKE 'Threads_connected';-- 查看历史最大连接数SHOW STATUS LIKE 'Max_used_connections';-- 查看连接来源SELECT USER, HOST, COUNT(*) FROM information_schema.PROCESSLIST GROUP BY USER, HOST;-- 调整最大连接数（临时）SET GLOBAL max_connections = 500;</code></pre><h3 id="1-8-缓冲池（Buffer-Pool）分析"><a href="#1-8-缓冲池（Buffer-Pool）分析" class="headerlink" title="1.8 缓冲池（Buffer Pool）分析"></a>1.8 缓冲池（Buffer Pool）分析</h3><pre class="line-numbers language-sql" data-language="sql"><code class="language-sql">-- 查看缓冲池大小SHOW VARIABLES LIKE 'innodb_buffer_pool_size';-- 查看缓冲池使用情况SHOW STATUS LIKE 'Innodb_buffer_pool%';-- 计算缓冲池命中率-- 命中率 = (1 - Innodb_buffer_pool_reads / Innodb_buffer_pool_read_requests) * 100-- 正常应该 &gt; 99%-- 查看缓冲池详细信息SELECT * FROM information_schema.INNODB_BUFFER_POOL_STATS;</code></pre><h3 id="1-9-MySQL-性能监控工具"><a href="#1-9-MySQL-性能监控工具" class="headerlink" title="1.9 MySQL 性能监控工具"></a>1.9 MySQL 性能监控工具</h3><p><strong>使用 mysqladmin</strong>：</p><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash"># 查看状态mysqladmin -u root -p status# 持续监控mysqladmin -u root -p -i 1 extended-status | grep -E "Threads_connected|Questions|Slow_queries"# 查看变量mysqladmin -u root -p variables</code></pre><p><strong>使用 mytop</strong>：</p><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash"># 安装yum install mytop  # CentOSapt install mytop  # Ubuntu# 运行（类似 top 的界面）mytop -u root -p password</code></pre><p><strong>使用 Percona Toolkit</strong>：</p><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash"># 安装yum install percona-toolkit# 分析慢查询pt-query-digest /var/log/mysql/slow.log# 查找重复索引pt-duplicate-key-checker# 在线修改表结构（不锁表）pt-online-schema-change --alter "ADD INDEX idx_name (name)" D=db,t=table --execute</code></pre><h2 id="二、PostgreSQL-性能诊断"><a href="#二、PostgreSQL-性能诊断" class="headerlink" title="二、PostgreSQL 性能诊断"></a>二、PostgreSQL 性能诊断</h2><h3 id="2-1-查看数据库状态"><a href="#2-1-查看数据库状态" class="headerlink" title="2.1 查看数据库状态"></a>2.1 查看数据库状态</h3><pre class="line-numbers language-sql" data-language="sql"><code class="language-sql">-- 查看当前连接数SELECT count(*) FROM pg_stat_activity;-- 查看最大连接数SHOW max_connections;-- 查看数据库大小SELECT pg_database.datname,        pg_size_pretty(pg_database_size(pg_database.datname)) AS sizeFROM pg_database;-- 查看表大小SELECT schemaname, tablename,       pg_size_pretty(pg_total_relation_size(schemaname||'.'||tablename)) AS sizeFROM pg_tablesORDER BY pg_total_relation_size(schemaname||'.'||tablename) DESCLIMIT 10;</code></pre><h3 id="2-2-慢查询诊断"><a href="#2-2-慢查询诊断" class="headerlink" title="2.2 慢查询诊断"></a>2.2 慢查询诊断</h3><p><strong>开启慢查询日志（postgresql.conf）</strong>：</p><pre class="line-numbers language-ini" data-language="ini"><code class="language-ini">log_min_duration_statement = 1000  # 记录超过1秒的查询log_line_prefix = '%t [%p]: [%l-1] user=%u,db=%d,app=%a,client=%h 'log_statement = 'all'  # 记录所有SQL</code></pre><p><strong>查看正在执行的查询</strong>：</p><pre class="line-numbers language-sql" data-language="sql"><code class="language-sql">-- 查看当前活动查询SELECT pid, usename, datname, state, query,        now() - query_start AS durationFROM pg_stat_activityWHERE state != 'idle'ORDER BY duration DESC;-- 终止慢查询SELECT pg_terminate_backend(pid);-- 查看长时间运行的事务SELECT pid, usename, datname, state,        now() - xact_start AS durationFROM pg_stat_activityWHERE xact_start IS NOT NULLORDER BY duration DESC;</code></pre><h3 id="2-3-执行计划分析"><a href="#2-3-执行计划分析" class="headerlink" title="2.3 执行计划分析"></a>2.3 执行计划分析</h3><pre class="line-numbers language-sql" data-language="sql"><code class="language-sql">-- 查看执行计划EXPLAIN SELECT * FROM users WHERE email = 'test@example.com';-- 查看详细执行计划EXPLAIN (ANALYZE, BUFFERS, VERBOSE) SELECT * FROM users WHERE email = 'test@example.com';-- 查看 JSON 格式EXPLAIN (FORMAT JSON) SELECT * FROM users WHERE email = 'test@example.com';</code></pre><h3 id="2-4-索引分析"><a href="#2-4-索引分析" class="headerlink" title="2.4 索引分析"></a>2.4 索引分析</h3><pre class="line-numbers language-sql" data-language="sql"><code class="language-sql">-- 查看表的索引SELECT * FROM pg_indexes WHERE tablename = 'users';-- 查看索引使用情况SELECT schemaname, tablename, indexname, idx_scan, idx_tup_read, idx_tup_fetchFROM pg_stat_user_indexesORDER BY idx_scan ASC;-- 查看未使用的索引SELECT schemaname, tablename, indexnameFROM pg_stat_user_indexesWHERE idx_scan = 0 AND indexrelname NOT LIKE '%_pkey';-- 查看索引大小SELECT schemaname, tablename, indexname,       pg_size_pretty(pg_relation_size(indexrelid)) AS sizeFROM pg_stat_user_indexesORDER BY pg_relation_size(indexrelid) DESC;</code></pre><h3 id="2-5-锁等待诊断"><a href="#2-5-锁等待诊断" class="headerlink" title="2.5 锁等待诊断"></a>2.5 锁等待诊断</h3><pre class="line-numbers language-sql" data-language="sql"><code class="language-sql">-- 查看锁等待SELECT blocked_locks.pid AS blocked_pid,       blocked_activity.usename AS blocked_user,       blocking_locks.pid AS blocking_pid,       blocking_activity.usename AS blocking_user,       blocked_activity.query AS blocked_statement,       blocking_activity.query AS blocking_statementFROM pg_catalog.pg_locks blocked_locksJOIN pg_catalog.pg_stat_activity blocked_activity ON blocked_activity.pid = blocked_locks.pidJOIN pg_catalog.pg_locks blocking_locks     ON blocking_locks.locktype = blocked_locks.locktype    AND blocking_locks.database IS NOT DISTINCT FROM blocked_locks.database    AND blocking_locks.relation IS NOT DISTINCT FROM blocked_locks.relation    AND blocking_locks.page IS NOT DISTINCT FROM blocked_locks.page    AND blocking_locks.tuple IS NOT DISTINCT FROM blocked_locks.tuple    AND blocking_locks.virtualxid IS NOT DISTINCT FROM blocked_locks.virtualxid    AND blocking_locks.transactionid IS NOT DISTINCT FROM blocked_locks.transactionid    AND blocking_locks.classid IS NOT DISTINCT FROM blocked_locks.classid    AND blocking_locks.objid IS NOT DISTINCT FROM blocked_locks.objid    AND blocking_locks.objsubid IS NOT DISTINCT FROM blocked_locks.objsubid    AND blocking_locks.pid != blocked_locks.pidJOIN pg_catalog.pg_stat_activity blocking_activity ON blocking_activity.pid = blocking_locks.pidWHERE NOT blocked_locks.granted;</code></pre><h3 id="2-6-缓存命中率"><a href="#2-6-缓存命中率" class="headerlink" title="2.6 缓存命中率"></a>2.6 缓存命中率</h3><pre class="line-numbers language-sql" data-language="sql"><code class="language-sql">-- 查看缓存命中率SELECT     sum(heap_blks_read) as heap_read,    sum(heap_blks_hit) as heap_hit,    sum(heap_blks_hit) / (sum(heap_blks_hit) + sum(heap_blks_read)) as ratioFROM pg_statio_user_tables;-- 命中率应该 &gt; 0.99-- 查看索引缓存命中率SELECT     sum(idx_blks_read) as idx_read,    sum(idx_blks_hit) as idx_hit,    sum(idx_blks_hit) / (sum(idx_blks_hit) + sum(idx_blks_read)) as ratioFROM pg_statio_user_indexes;</code></pre><h3 id="2-7-VACUUM-和-ANALYZE"><a href="#2-7-VACUUM-和-ANALYZE" class="headerlink" title="2.7 VACUUM 和 ANALYZE"></a>2.7 VACUUM 和 ANALYZE</h3><pre class="line-numbers language-sql" data-language="sql"><code class="language-sql">-- 查看表的膨胀情况SELECT schemaname, tablename,        pg_size_pretty(pg_total_relation_size(schemaname||'.'||tablename)) AS size,       n_dead_tup, n_live_tup,       round(n_dead_tup * 100.0 / NULLIF(n_live_tup + n_dead_tup, 0), 2) AS dead_ratioFROM pg_stat_user_tablesWHERE n_dead_tup &gt; 1000ORDER BY n_dead_tup DESC;-- 手动 VACUUMVACUUM ANALYZE table_name;-- 查看自动 VACUUM 配置SHOW autovacuum;SHOW autovacuum_naptime;</code></pre><h2 id="三、Redis-性能诊断"><a href="#三、Redis-性能诊断" class="headerlink" title="三、Redis 性能诊断"></a>三、Redis 性能诊断</h2><h3 id="3-1-查看-Redis-状态"><a href="#3-1-查看-Redis-状态" class="headerlink" title="3.1 查看 Redis 状态"></a>3.1 查看 Redis 状态</h3><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash"># 连接 Redisredis-cli# 查看服务器信息INFO# 查看特定部分INFO stats        # 统计信息INFO memory       # 内存信息INFO replication  # 复制信息INFO cpu          # CPU信息INFO clients      # 客户端信息</code></pre><h3 id="3-2-慢查询诊断"><a href="#3-2-慢查询诊断" class="headerlink" title="3.2 慢查询诊断"></a>3.2 慢查询诊断</h3><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash"># 查看慢查询配置CONFIG GET slowlog-log-slower-than  # 微秒，默认10000（10ms）CONFIG GET slowlog-max-len          # 慢查询日志长度# 设置慢查询阈值（临时）CONFIG SET slowlog-log-slower-than 1000  # 1ms# 查看慢查询日志SLOWLOG GET 10  # 获取最近10条慢查询# 查看慢查询日志长度SLOWLOG LEN# 清空慢查询日志SLOWLOG RESET</code></pre><h3 id="3-3-实时监控命令"><a href="#3-3-实时监控命令" class="headerlink" title="3.3 实时监控命令"></a>3.3 实时监控命令</h3><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash"># 实时监控所有命令MONITOR# 查看客户端连接CLIENT LIST# 查看当前连接数INFO clients | grep connected_clients# 查看阻塞的客户端CLIENT LIST | grep blocked</code></pre><h3 id="3-4-内存分析"><a href="#3-4-内存分析" class="headerlink" title="3.4 内存分析"></a>3.4 内存分析</h3><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash"># 查看内存使用INFO memory# 查看大 keyredis-cli --bigkeys# 查看内存使用详情（Redis 4.0+）MEMORY STATS# 查看某个 key 的内存占用MEMORY USAGE key_name# 查看内存碎片率INFO memory | grep mem_fragmentation_ratio# 正常值：1.0 - 1.5# &gt; 1.5：内存碎片较多# &lt; 1.0：可能发生了 swap</code></pre><h3 id="3-5-性能测试"><a href="#3-5-性能测试" class="headerlink" title="3.5 性能测试"></a>3.5 性能测试</h3><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash"># 使用 redis-benchmark 测试redis-benchmark -h 127.0.0.1 -p 6379 -c 100 -n 100000# -c: 并发连接数# -n: 请求总数# 测试特定命令redis-benchmark -t set,get -n 100000 -q# 测试 pipelineredis-benchmark -n 100000 -P 16</code></pre><h3 id="3-6-持久化影响"><a href="#3-6-持久化影响" class="headerlink" title="3.6 持久化影响"></a>3.6 持久化影响</h3><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash"># 查看 RDB 配置CONFIG GET save# 查看 AOF 配置CONFIG GET appendonlyCONFIG GET appendfsync# 查看最后一次 save 时间INFO persistence | grep rdb_last_save_time# 手动触发 save（会阻塞）SAVE# 后台 save（不阻塞）BGSAVE</code></pre><h2 id="四、MongoDB-性能诊断"><a href="#四、MongoDB-性能诊断" class="headerlink" title="四、MongoDB 性能诊断"></a>四、MongoDB 性能诊断</h2><h3 id="4-1-查看数据库状态"><a href="#4-1-查看数据库状态" class="headerlink" title="4.1 查看数据库状态"></a>4.1 查看数据库状态</h3><pre class="line-numbers language-javascript" data-language="javascript"><code class="language-javascript">// 查看服务器状态db.serverStatus()// 查看数据库统计db.stats()// 查看集合统计db.collection_name.stats()// 查看当前操作db.currentOp()// 查看连接数db.serverStatus().connections</code></pre><h3 id="4-2-慢查询诊断"><a href="#4-2-慢查询诊断" class="headerlink" title="4.2 慢查询诊断"></a>4.2 慢查询诊断</h3><pre class="line-numbers language-javascript" data-language="javascript"><code class="language-javascript">// 开启 profiling（级别0-2）// 0: 关闭// 1: 记录慢查询// 2: 记录所有查询db.setProfilingLevel(1, { slowms: 100 })  // 记录超过100ms的查询// 查看 profiling 状态db.getProfilingStatus()// 查看慢查询db.system.profile.find().limit(10).sort({ ts: -1 }).pretty()// 查看最慢的查询db.system.profile.find({ millis: { $gt: 100 } }).sort({ millis: -1 }).limit(10)</code></pre><h3 id="4-3-执行计划分析"><a href="#4-3-执行计划分析" class="headerlink" title="4.3 执行计划分析"></a>4.3 执行计划分析</h3><pre class="line-numbers language-javascript" data-language="javascript"><code class="language-javascript">// 查看执行计划db.collection.find({ field: value }).explain("executionStats")// 查看详细执行计划db.collection.find({ field: value }).explain("allPlansExecution")// 关键指标：// - executionTimeMillis: 执行时间// - totalDocsExamined: 扫描的文档数// - totalKeysExamined: 扫描的索引键数// - stage: COLLSCAN（全表扫描）需要优化</code></pre><h3 id="4-4-索引分析"><a href="#4-4-索引分析" class="headerlink" title="4.4 索引分析"></a>4.4 索引分析</h3><pre class="line-numbers language-javascript" data-language="javascript"><code class="language-javascript">// 查看集合的索引db.collection.getIndexes()// 查看索引使用情况db.collection.aggregate([{ $indexStats: {} }])// 创建索引db.collection.createIndex({ field: 1 })// 删除索引db.collection.dropIndex("index_name")// 重建索引db.collection.reIndex()</code></pre><h3 id="4-5-锁等待诊断"><a href="#4-5-锁等待诊断" class="headerlink" title="4.5 锁等待诊断"></a>4.5 锁等待诊断</h3><pre class="line-numbers language-javascript" data-language="javascript"><code class="language-javascript">// 查看当前锁db.currentOp({ $or: [{ waitingForLock: true }, { lockStats: { $exists: true } }] })// 杀死慢操作db.killOp(opid)// 查看锁统计db.serverStatus().locks</code></pre><h2 id="五、数据库性能优化通用策略"><a href="#五、数据库性能优化通用策略" class="headerlink" title="五、数据库性能优化通用策略"></a>五、数据库性能优化通用策略</h2><h3 id="5-1-SQL-优化"><a href="#5-1-SQL-优化" class="headerlink" title="5.1 SQL 优化"></a>5.1 SQL 优化</h3><p><strong>避免全表扫描</strong>：</p><pre class="line-numbers language-sql" data-language="sql"><code class="language-sql">-- 不好：全表扫描SELECT * FROM users WHERE YEAR(created_at) = 2024;-- 好：使用索引SELECT * FROM users WHERE created_at &gt;= '2024-01-01' AND created_at &lt; '2025-01-01';</code></pre><p>**避免 SELECT ***：</p><pre class="line-numbers language-sql" data-language="sql"><code class="language-sql">-- 不好：查询所有字段SELECT * FROM users WHERE id = 1;-- 好：只查询需要的字段SELECT id, name, email FROM users WHERE id = 1;</code></pre><p><strong>使用 LIMIT</strong>：</p><pre class="line-numbers language-sql" data-language="sql"><code class="language-sql">-- 不好：返回所有结果SELECT * FROM users WHERE status = 'active';-- 好：限制返回数量SELECT * FROM users WHERE status = 'active' LIMIT 100;</code></pre><p><strong>批量操作</strong>：</p><pre class="line-numbers language-sql" data-language="sql"><code class="language-sql">-- 不好：逐条插入INSERT INTO users (name) VALUES ('user1');INSERT INTO users (name) VALUES ('user2');-- 好：批量插入INSERT INTO users (name) VALUES ('user1'), ('user2'), ('user3');</code></pre><h3 id="5-2-索引优化"><a href="#5-2-索引优化" class="headerlink" title="5.2 索引优化"></a>5.2 索引优化</h3><p><strong>创建合适的索引</strong>：</p><pre class="line-numbers language-sql" data-language="sql"><code class="language-sql">-- 单列索引CREATE INDEX idx_email ON users(email);-- 复合索引（注意顺序）CREATE INDEX idx_status_created ON users(status, created_at);-- 覆盖索引（包含查询所需的所有字段）CREATE INDEX idx_cover ON users(status, id, name);</code></pre><p><strong>索引使用原则</strong>：</p><ul><li>在 WHERE、JOIN、ORDER BY 的字段上建索引</li><li>选择性高的字段优先（值分布均匀）</li><li>复合索引遵循最左前缀原则</li><li>避免在低选择性字段建索引（如性别）</li><li>定期删除未使用的索引</li></ul><h3 id="5-3-连接池优化"><a href="#5-3-连接池优化" class="headerlink" title="5.3 连接池优化"></a>5.3 连接池优化</h3><p><strong>连接池配置建议</strong>：</p><pre class="line-numbers language-properties" data-language="properties"><code class="language-properties"># 最小连接数minPoolSize=10# 最大连接数（根据数据库 max_connections 设置）maxPoolSize=50# 连接超时时间connectionTimeout=30000# 空闲连接超时时间idleTimeout=600000# 最大生命周期maxLifetime=1800000</code></pre><h3 id="5-4-缓存策略"><a href="#5-4-缓存策略" class="headerlink" title="5.4 缓存策略"></a>5.4 缓存策略</h3><p><strong>多级缓存架构</strong>：</p><pre class="line-numbers language-none"><code class="language-none">应用层  ↓本地缓存（Caffeine/Guava）  ↓分布式缓存（Redis）  ↓数据库</code></pre><p><strong>缓存更新策略</strong>：</p><ul><li>Cache Aside（旁路缓存）：先更新数据库，再删除缓存</li><li>Write Through（写穿）：同时更新缓存和数据库</li><li>Write Behind（写回）：先更新缓存，异步更新数据库</li></ul><h3 id="5-5-分库分表"><a href="#5-5-分库分表" class="headerlink" title="5.5 分库分表"></a>5.5 分库分表</h3><p><strong>垂直拆分</strong>：</p><ul><li>按业务模块拆分（用户库、订单库、商品库）</li><li>将大字段拆分到独立表</li></ul><p><strong>水平拆分</strong>：</p><ul><li>按范围分片（如按日期）</li><li>按哈希分片（如按用户ID）</li><li>按地理位置分片</li></ul><h3 id="5-6-读写分离"><a href="#5-6-读写分离" class="headerlink" title="5.6 读写分离"></a>5.6 读写分离</h3><pre class="line-numbers language-none"><code class="language-none">应用  ↓写操作 → 主库（Master）           ↓ 复制读操作 → 从库（Slave1, Slave2, Slave3）</code></pre><p><strong>注意事项</strong>：</p><ul><li>主从延迟问题</li><li>强一致性要求的读操作走主库</li><li>使用中间件（如 MyCat、ShardingSphere）</li></ul><h2 id="六、数据库监控工具"><a href="#六、数据库监控工具" class="headerlink" title="六、数据库监控工具"></a>六、数据库监控工具</h2><h3 id="6-1-MySQL-监控工具"><a href="#6-1-MySQL-监控工具" class="headerlink" title="6.1 MySQL 监控工具"></a>6.1 MySQL 监控工具</h3><p>**Percona Monitoring and Management (PMM)**：</p><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash"># 安装 PMM Serverdocker run -d -p 80:80 --name pmm-server percona/pmm-server:2# 安装 PMM Clientwget https://www.percona.com/downloads/pmm2/pmm-client-2.x.x.tar.gztar -xzf pmm-client-2.x.x.tar.gz./pmm-admin config --server-url=http://pmm-server:80./pmm-admin add mysql --username=root --password=password</code></pre><p><strong>Prometheus + Grafana</strong>：</p><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash"># 使用 mysqld_exporterdocker run -d -p 9104:9104 \  -e DATA_SOURCE_NAME="user:password@(hostname:3306)/" \  prom/mysqld-exporter</code></pre><h3 id="6-2-PostgreSQL-监控工具"><a href="#6-2-PostgreSQL-监控工具" class="headerlink" title="6.2 PostgreSQL 监控工具"></a>6.2 PostgreSQL 监控工具</h3><p><strong>pg_stat_statements</strong>：</p><pre class="line-numbers language-sql" data-language="sql"><code class="language-sql">-- 安装扩展CREATE EXTENSION pg_stat_statements;-- 查看 SQL 统计SELECT query, calls, total_time, mean_time, rowsFROM pg_stat_statementsORDER BY total_time DESCLIMIT 10;</code></pre><p><strong>pgBadger</strong>：</p><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash"># 分析 PostgreSQL 日志pgbadger /var/log/postgresql/postgresql.log -o report.html</code></pre><h3 id="6-3-Redis-监控工具"><a href="#6-3-Redis-监控工具" class="headerlink" title="6.3 Redis 监控工具"></a>6.3 Redis 监控工具</h3><p><strong>RedisInsight</strong>：</p><ul><li>官方 GUI 工具</li><li>支持实时监控、慢查询分析、内存分析</li></ul><p><strong>redis-stat</strong>：</p><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash"># 安装gem install redis-stat# 运行redis-stat --server</code></pre><h2 id="七、数据库性能诊断检查清单"><a href="#七、数据库性能诊断检查清单" class="headerlink" title="七、数据库性能诊断检查清单"></a>七、数据库性能诊断检查清单</h2><h3 id="快速检查（5分钟）"><a href="#快速检查（5分钟）" class="headerlink" title="快速检查（5分钟）"></a>快速检查（5分钟）</h3><ul><li><input disabled="" type="checkbox"> 查看当前连接数是否接近上限</li><li><input disabled="" type="checkbox"> 查看是否有慢查询</li><li><input disabled="" type="checkbox"> 查看是否有锁等待</li><li><input disabled="" type="checkbox"> 查看 CPU 和内存使用率</li><li><input disabled="" type="checkbox"> 查看磁盘 I/O 是否繁忙</li></ul><h3 id="深入分析（30分钟）"><a href="#深入分析（30分钟）" class="headerlink" title="深入分析（30分钟）"></a>深入分析（30分钟）</h3><ul><li><input disabled="" type="checkbox"> 分析慢查询日志，找出 TOP 10 慢查询</li><li><input disabled="" type="checkbox"> 检查慢查询的执行计划</li><li><input disabled="" type="checkbox"> 检查索引使用情况，找出未使用的索引</li><li><input disabled="" type="checkbox"> 检查缓存命中率</li><li><input disabled="" type="checkbox"> 检查表和索引的大小</li><li><input disabled="" type="checkbox"> 检查是否有表锁或行锁竞争</li><li><input disabled="" type="checkbox"> 检查连接池配置是否合理</li></ul><h3 id="长期优化（持续进行）"><a href="#长期优化（持续进行）" class="headerlink" title="长期优化（持续进行）"></a>长期优化（持续进行）</h3><ul><li><input disabled="" type="checkbox"> 建立性能监控系统</li><li><input disabled="" type="checkbox"> 定期分析慢查询趋势</li><li><input disabled="" type="checkbox"> 优化高频查询的 SQL</li><li><input disabled="" type="checkbox"> 定期清理无用数据和索引</li><li><input disabled="" type="checkbox"> 规划分库分表策略</li><li><input disabled="" type="checkbox"> 实施读写分离</li><li><input disabled="" type="checkbox"> 建立数据库备份和恢复机制</li></ul><h2 id="八、常见数据库性能问题"><a href="#八、常见数据库性能问题" class="headerlink" title="八、常见数据库性能问题"></a>八、常见数据库性能问题</h2><h3 id="问题1：查询突然变慢"><a href="#问题1：查询突然变慢" class="headerlink" title="问题1：查询突然变慢"></a>问题1：查询突然变慢</h3><p><strong>可能原因</strong>：</p><ul><li>数据量增长，索引失效</li><li>统计信息过期</li><li>锁等待</li><li>缓存失效</li></ul><p><strong>排查步骤</strong>：</p><pre class="line-numbers language-sql" data-language="sql"><code class="language-sql">-- 1. 查看执行计划EXPLAIN SELECT ...;-- 2. 更新统计信息ANALYZE TABLE table_name;-- 3. 查看锁等待SHOW ENGINE INNODB STATUS;-- 4. 查看缓存命中率SHOW STATUS LIKE 'Innodb_buffer_pool%';</code></pre><h3 id="问题2：连接数耗尽"><a href="#问题2：连接数耗尽" class="headerlink" title="问题2：连接数耗尽"></a>问题2：连接数耗尽</h3><p><strong>可能原因</strong>：</p><ul><li>连接未正确释放</li><li>连接池配置过小</li><li>慢查询占用连接</li></ul><p><strong>解决方案</strong>：</p><pre class="line-numbers language-sql" data-language="sql"><code class="language-sql">-- 查看连接来源SELECT USER, HOST, COUNT(*) FROM information_schema.PROCESSLIST GROUP BY USER, HOST;-- 杀死空闲连接KILL &lt;process_id&gt;;-- 调整最大连接数SET GLOBAL max_connections = 500;</code></pre><h3 id="问题3：死锁"><a href="#问题3：死锁" class="headerlink" title="问题3：死锁"></a>问题3：死锁</h3><p><strong>排查</strong>：</p><pre class="line-numbers language-sql" data-language="sql"><code class="language-sql">-- 查看最近的死锁信息SHOW ENGINE INNODB STATUS;-- 开启死锁日志SET GLOBAL innodb_print_all_deadlocks = ON;</code></pre><p><strong>预防</strong>：</p><ul><li>保持事务简短</li><li>按相同顺序访问表和行</li><li>使用合适的隔离级别</li><li>添加合适的索引减少锁范围</li></ul><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>数据库性能诊断的关键步骤：</p><ol><li><strong>监控指标</strong>：QPS、TPS、连接数、慢查询数</li><li><strong>定位问题</strong>：慢查询日志、执行计划、锁等待</li><li><strong>分析原因</strong>：索引、SQL、配置、硬件</li><li><strong>优化方案</strong>：SQL优化、索引优化、架构优化</li><li><strong>持续改进</strong>：监控系统、定期分析、容量规划</li></ol><p>以下：</p><ul><li><strong>80% 的性能问题来自 20% 的 SQL</strong></li><li><strong>索引是性能优化的第一选择</strong></li><li><strong>监控和分析比优化更重要</strong></li></ul></li></ul>]]></content>
      
      
      <categories>
          
          <category> 杂记 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 杂记 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Linux 性能瓶颈诊断</title>
      <link href="/2026/02/25/Linux%20%E6%80%A7%E8%83%BD%E7%93%B6%E9%A2%88%E5%88%9D%E6%AD%A5%E5%88%86%E6%9E%90/"/>
      <url>/2026/02/25/Linux%20%E6%80%A7%E8%83%BD%E7%93%B6%E9%A2%88%E5%88%9D%E6%AD%A5%E5%88%86%E6%9E%90/</url>
      
        <content type="html"><![CDATA[<h1 id="Linux-性能瓶颈诊断"><a href="#Linux-性能瓶颈诊断" class="headerlink" title="Linux 性能瓶颈诊断"></a>Linux 性能瓶颈诊断</h1><h2 id="概述"><a href="#概述" class="headerlink" title="概述"></a>概述</h2><p>性能瓶颈通常出现在以下四个方面：</p><ol><li><strong>CPU</strong> - 计算密集型任务</li><li><strong>内存</strong> - 内存不足或泄漏</li><li><strong>磁盘 I/O</strong> - 读写频繁</li><li><strong>网络 I/O</strong> - 网络传输瓶颈</li></ol><h2 id="快速诊断流程"><a href="#快速诊断流程" class="headerlink" title="快速诊断流程"></a>快速诊断流程</h2><h3 id="第一步：整体系统负载查看"><a href="#第一步：整体系统负载查看" class="headerlink" title="第一步：整体系统负载查看"></a>第一步：整体系统负载查看</h3><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash"># 查看系统整体负载（最常用）top# 更友好的交互式工具htop# 查看系统平均负载uptime# 输出示例：up 10 days, 2 users, load average: 0.50, 0.40, 0.35# 三个数字分别代表 1分钟、5分钟、15分钟 的平均负载# 负载值 &gt; CPU核心数 表示系统过载</code></pre><p><strong>load average 解读</strong>：</p><ul><li>单核 CPU：负载 &gt; 1.0 表示过载</li><li>4核 CPU：负载 &gt; 4.0 表示过载</li><li>负载持续增长说明系统压力在增大</li></ul><h3 id="第二步：确定瓶颈类型"><a href="#第二步：确定瓶颈类型" class="headerlink" title="第二步：确定瓶颈类型"></a>第二步：确定瓶颈类型</h3><p>使用 <code>vmstat</code> 快速判断：</p><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash">vmstat 1 5# 每秒输出一次，共5次# 输出示例：# procs -----------memory---------- ---swap-- -----io---- -system-- ------cpu-----#  r  b   swpd   free   buff  cache   si   so    bi    bo   in   cs us sy id wa st#  2  0      0 500000  50000 800000    0    0    10    20  100  200 25  5 70  0  0</code></pre><p><strong>关键指标解读</strong>：</p><table><thead><tr><th>列</th><th>含义</th><th>瓶颈判断</th></tr></thead><tbody><tr><td><code>r</code></td><td>运行队列长度（等待CPU的进程数）</td><td>&gt; CPU核心数 → CPU瓶颈</td></tr><tr><td><code>b</code></td><td>不可中断睡眠的进程数（等待I/O）</td><td>&gt; 0 持续存在 → I/O瓶颈</td></tr><tr><td><code>si/so</code></td><td>换入/换出内存（KB/s）</td><td>&gt; 0 持续存在 → 内存不足</td></tr><tr><td><code>bi/bo</code></td><td>块设备读入/写出（块/s）</td><td>很高 → 磁盘I/O瓶颈</td></tr><tr><td><code>us</code></td><td>用户态CPU使用率</td><td>&gt; 70% → CPU密集型</td></tr><tr><td><code>sy</code></td><td>系统态CPU使用率</td><td>&gt; 30% → 系统调用频繁</td></tr><tr><td><code>wa</code></td><td>I/O等待CPU占比</td><td>&gt; 20% → I/O瓶颈</td></tr><tr><td><code>id</code></td><td>空闲CPU</td><td>&lt; 10% → CPU瓶颈</td></tr></tbody></table><h2 id="一、CPU-瓶颈诊断"><a href="#一、CPU-瓶颈诊断" class="headerlink" title="一、CPU 瓶颈诊断"></a>一、CPU 瓶颈诊断</h2><h3 id="1-1-查看-CPU-使用率"><a href="#1-1-查看-CPU-使用率" class="headerlink" title="1.1 查看 CPU 使用率"></a>1.1 查看 CPU 使用率</h3><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash"># 实时查看 CPU 使用情况top# 按 '1' 显示每个CPU核心# 按 'P' 按CPU使用率排序# 查看每个CPU核心使用率mpstat -P ALL 1# 输出每个核心的 %usr, %sys, %iowait, %idle# 查看进程级别的CPU使用ps aux --sort=-%cpu | head -10# 显示CPU使用率最高的10个进程</code></pre><h3 id="1-2-查找CPU密集型进程"><a href="#1-2-查找CPU密集型进程" class="headerlink" title="1.2 查找CPU密集型进程"></a>1.2 查找CPU密集型进程</h3><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash"># 找出CPU占用最高的进程top -b -n 1 | head -20# 查看具体进程的线程CPU使用情况top -H -p &lt;PID&gt;# 使用 pidstat 查看进程CPU统计pidstat -u 1 5# -u: CPU使用率# 1: 每秒采样# 5: 采样5次</code></pre><h3 id="1-3-分析CPU瓶颈原因"><a href="#1-3-分析CPU瓶颈原因" class="headerlink" title="1.3 分析CPU瓶颈原因"></a>1.3 分析CPU瓶颈原因</h3><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash"># 查看进程的系统调用strace -c -p &lt;PID&gt;# -c: 统计系统调用次数和时间# -p: 指定进程ID# 查看进程的函数调用栈（需要安装 perf）perf top -p &lt;PID&gt;# 生成火焰图（需要安装 FlameGraph）perf record -F 99 -p &lt;PID&gt; -g -- sleep 30perf script | stackcollapse-perf.pl | flamegraph.pl &gt; cpu-flamegraph.svg</code></pre><p><strong>CPU瓶颈判断标准</strong>：</p><ul><li><code>us + sy &gt; 80%</code> 且 <code>id &lt; 20%</code> → CPU瓶颈</li><li><code>us</code> 很高 → 应用程序计算密集</li><li><code>sy</code> 很高 → 系统调用频繁，可能是大量小文件操作或网络连接</li></ul><h3 id="1-4-CPU-上下文切换分析"><a href="#1-4-CPU-上下文切换分析" class="headerlink" title="1.4 CPU 上下文切换分析"></a>1.4 CPU 上下文切换分析</h3><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash"># 查看上下文切换次数vmstat 1# cs 列：每秒上下文切换次数# 正常值：&lt; 10000# 异常值：&gt; 100000 说明线程/进程切换频繁# 查看每个进程的上下文切换pidstat -w 1 5# cswch/s: 自愿上下文切换（进程主动放弃CPU）# nvcswch/s: 非自愿上下文切换（时间片用完被强制切换）</code></pre><h2 id="二、内存瓶颈诊断"><a href="#二、内存瓶颈诊断" class="headerlink" title="二、内存瓶颈诊断"></a>二、内存瓶颈诊断</h2><h3 id="2-1-查看内存使用情况"><a href="#2-1-查看内存使用情况" class="headerlink" title="2.1 查看内存使用情况"></a>2.1 查看内存使用情况</h3><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash"># 查看内存概况free -h# 输出示例：#               total        used        free      shared  buff/cache   available# Mem:           15Gi       8.0Gi       2.0Gi       500Mi       5.0Gi       6.5Gi# Swap:         2.0Gi       100Mi       1.9Gi# 详细内存信息cat /proc/meminfo# 实时监控内存watch -n 1 free -h</code></pre><p><strong>内存指标解读</strong>：</p><ul><li><code>available</code> &lt; 总内存的 10% → 内存紧张</li><li><code>Swap used</code> &gt; 0 且持续增长 → 内存不足</li><li><code>buff/cache</code> 很高是正常的（Linux会用空闲内存做缓存）</li></ul><h3 id="2-2-查找内存占用高的进程"><a href="#2-2-查找内存占用高的进程" class="headerlink" title="2.2 查找内存占用高的进程"></a>2.2 查找内存占用高的进程</h3><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash"># 按内存使用率排序ps aux --sort=-%mem | head -10# 使用 top 查看top# 按 'M' 按内存使用率排序# 查看进程内存详细信息pmap -x &lt;PID&gt;# 查看进程内存映射cat /proc/&lt;PID&gt;/smaps</code></pre><h3 id="2-3-内存泄漏检测"><a href="#2-3-内存泄漏检测" class="headerlink" title="2.3 内存泄漏检测"></a>2.3 内存泄漏检测</h3><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash"># 持续监控进程内存增长while true; do    ps aux | grep &lt;进程名&gt; | grep -v grep    sleep 5done# 使用 valgrind 检测内存泄漏（需要重启应用）valgrind --leak-check=full --log-file=valgrind.log ./your_program# 使用 pidstat 监控内存pidstat -r 1# VSZ: 虚拟内存大小# RSS: 物理内存大小# %MEM: 内存使用百分比</code></pre><h3 id="2-4-查看-Swap-使用情况"><a href="#2-4-查看-Swap-使用情况" class="headerlink" title="2.4 查看 Swap 使用情况"></a>2.4 查看 Swap 使用情况</h3><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash"># 查看 swap 使用swapon --show# 查看哪些进程在使用 swapfor file in /proc/*/status ; do    awk '/VmSwap|Name/{printf $2 " " $3}END{ print ""}' $filedone | sort -k 2 -n -r | head -10# 清理 swap（谨慎使用）# swapoff -a &amp;&amp; swapon -a</code></pre><p><strong>内存瓶颈判断标准</strong>：</p><ul><li>可用内存 &lt; 10% → 内存不足</li><li>Swap 使用率 &gt; 50% → 严重内存不足</li><li>进程频繁被 OOM Killer 杀死 → 内存耗尽</li></ul><h2 id="三、磁盘-I-O-瓶颈诊断"><a href="#三、磁盘-I-O-瓶颈诊断" class="headerlink" title="三、磁盘 I/O 瓶颈诊断"></a>三、磁盘 I/O 瓶颈诊断</h2><h3 id="3-1-查看磁盘-I-O-统计"><a href="#3-1-查看磁盘-I-O-统计" class="headerlink" title="3.1 查看磁盘 I/O 统计"></a>3.1 查看磁盘 I/O 统计</h3><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash"># 查看磁盘I/O统计（最常用）iostat -x 1 5# -x: 显示扩展统计# 1: 每秒采样# 5: 采样5次# 输出示例：# Device    r/s   w/s   rkB/s   wkB/s  %util# sda      10.0  20.0   100.0   200.0   85.0</code></pre><p><strong>关键指标解读</strong>：</p><table><thead><tr><th>指标</th><th>含义</th><th>瓶颈判断</th></tr></thead><tbody><tr><td><code>r/s</code></td><td>每秒读请求数</td><td>机械硬盘 &gt; 100, SSD &gt; 10000</td></tr><tr><td><code>w/s</code></td><td>每秒写请求数</td><td>机械硬盘 &gt; 100, SSD &gt; 10000</td></tr><tr><td><code>await</code></td><td>平均I/O等待时间(ms)</td><td>&gt; 20ms 说明I/O慢</td></tr><tr><td><code>svctm</code></td><td>平均服务时间(ms)</td><td>&gt; 10ms 说明磁盘慢</td></tr><tr><td><code>%util</code></td><td>磁盘繁忙程度</td><td>&gt; 80% → I/O瓶颈</td></tr></tbody></table><h3 id="3-2-查找磁盘-I-O-密集型进程"><a href="#3-2-查找磁盘-I-O-密集型进程" class="headerlink" title="3.2 查找磁盘 I/O 密集型进程"></a>3.2 查找磁盘 I/O 密集型进程</h3><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash"># 查看进程I/O统计（需要root权限）iotop# 按 'o' 只显示有I/O的进程# 按 'a' 显示累计I/O# 使用 pidstat 查看进程I/Opidstat -d 1 5# kB_rd/s: 每秒读取KB数# kB_wr/s: 每秒写入KB数# 查看进程打开的文件lsof -p &lt;PID&gt;# 查看进程的I/O统计cat /proc/&lt;PID&gt;/io</code></pre><h3 id="3-3-分析磁盘性能"><a href="#3-3-分析磁盘性能" class="headerlink" title="3.3 分析磁盘性能"></a>3.3 分析磁盘性能</h3><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash"># 测试磁盘读性能hdparm -t /dev/sda# 测试磁盘写性能dd if=/dev/zero of=/tmp/test bs=1M count=1024 oflag=direct# 注意：会创建1GB文件# 查看磁盘队列深度cat /sys/block/sda/queue/nr_requests# 查看文件系统统计df -hdf -i  # 查看 inode 使用情况</code></pre><h3 id="3-4-查看具体文件的-I-O"><a href="#3-4-查看具体文件的-I-O" class="headerlink" title="3.4 查看具体文件的 I/O"></a>3.4 查看具体文件的 I/O</h3><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash"># 实时监控文件访问inotifywait -m /path/to/directory# 查看哪些进程在访问某个文件lsof /path/to/file# 查看目录下文件大小du -sh /path/to/directory/*</code></pre><p><strong>磁盘I/O瓶颈判断标准</strong>：</p><ul><li><code>%util &gt; 80%</code> → 磁盘繁忙</li><li><code>await &gt; 20ms</code> → I/O响应慢</li><li><code>vmstat</code> 中 <code>wa &gt; 20%</code> → CPU等待I/O时间过长</li></ul><h2 id="四、网络-I-O-瓶颈诊断"><a href="#四、网络-I-O-瓶颈诊断" class="headerlink" title="四、网络 I/O 瓶颈诊断"></a>四、网络 I/O 瓶颈诊断</h2><h3 id="4-1-查看网络流量"><a href="#4-1-查看网络流量" class="headerlink" title="4.1 查看网络流量"></a>4.1 查看网络流量</h3><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash"># 实时查看网络流量（需要安装）iftop# 查看网络接口统计ifconfig# 或ip -s link# 持续监控网络流量watch -n 1 'cat /proc/net/dev'# 使用 sar 查看网络统计sar -n DEV 1 5# rxkB/s: 每秒接收KB数# txkB/s: 每秒发送KB数</code></pre><h3 id="4-2-查看网络连接"><a href="#4-2-查看网络连接" class="headerlink" title="4.2 查看网络连接"></a>4.2 查看网络连接</h3><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash"># 查看所有网络连接netstat -antp# 或使用更快的 ssss -antp# 查看连接数统计netstat -n | awk '/^tcp/ {++S[$NF]} END {for(a in S) print a, S[a]}'# 输出各种状态的连接数：ESTABLISHED, TIME_WAIT, CLOSE_WAIT 等# 查看监听端口ss -lntp# 查看某个端口的连接ss -antp | grep :80</code></pre><h3 id="4-3-查找网络密集型进程"><a href="#4-3-查找网络密集型进程" class="headerlink" title="4.3 查找网络密集型进程"></a>4.3 查找网络密集型进程</h3><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash"># 查看进程的网络连接lsof -i -P -n# -i: 网络连接# -P: 显示端口号# -n: 不解析主机名# 查看特定进程的网络连接lsof -i -P -n -p &lt;PID&gt;# 使用 nethogs 查看进程网络使用（需要安装）nethogs# 查看进程的网络统计cat /proc/&lt;PID&gt;/net/dev</code></pre><h3 id="4-4-网络性能测试"><a href="#4-4-网络性能测试" class="headerlink" title="4.4 网络性能测试"></a>4.4 网络性能测试</h3><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash"># 测试网络带宽（需要在两台机器上安装 iperf3）# 服务端：iperf3 -s# 客户端：iperf3 -c &lt;服务器IP&gt;# 测试网络延迟ping -c 10 &lt;目标IP&gt;# 追踪路由traceroute &lt;目标IP&gt;# 查看网络丢包和错误netstat -i# RX-ERR: 接收错误# TX-ERR: 发送错误# RX-DRP: 接收丢包# TX-DRP: 发送丢包</code></pre><h3 id="4-5-TCP-连接状态分析"><a href="#4-5-TCP-连接状态分析" class="headerlink" title="4.5 TCP 连接状态分析"></a>4.5 TCP 连接状态分析</h3><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash"># 查看 TCP 连接状态统计ss -s# 查看 TIME_WAIT 连接数ss -ant | grep TIME_WAIT | wc -l# 查看 CLOSE_WAIT 连接数（如果很多说明应用没有正确关闭连接）ss -ant | grep CLOSE_WAIT | wc -l# 查看 TCP 重传netstat -s | grep retransmit</code></pre><p><strong>网络瓶颈判断标准</strong>：</p><ul><li>网络流量接近带宽上限 → 带宽瓶颈</li><li><code>TIME_WAIT</code> 连接数 &gt; 10000 → 短连接过多</li><li><code>CLOSE_WAIT</code> 连接数持续增长 → 应用未正确关闭连接</li><li>丢包率 &gt; 1% → 网络质量问题</li></ul><h2 id="五、综合诊断工具"><a href="#五、综合诊断工具" class="headerlink" title="五、综合诊断工具"></a>五、综合诊断工具</h2><h3 id="5-1-dstat-综合监控工具"><a href="#5-1-dstat-综合监控工具" class="headerlink" title="5.1 dstat - 综合监控工具"></a>5.1 dstat - 综合监控工具</h3><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash"># 安装yum install dstat  # CentOS/RHELapt install dstat  # Ubuntu/Debian# 综合查看 CPU、内存、磁盘、网络dstat -cdngy# -c: CPU# -d: 磁盘# -n: 网络# -g: 页面统计# -y: 系统统计# 每秒更新，显示最繁忙的进程dstat -c --top-cpu -d --top-bio</code></pre><h3 id="5-2-sar-系统活动报告"><a href="#5-2-sar-系统活动报告" class="headerlink" title="5.2 sar - 系统活动报告"></a>5.2 sar - 系统活动报告</h3><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash"># 查看历史CPU使用率sar -u 1 10# 查看历史内存使用sar -r 1 10# 查看历史磁盘I/Osar -d 1 10# 查看历史网络流量sar -n DEV 1 10# 查看今天的所有统计sar -A</code></pre><h3 id="5-3-perf-性能分析工具"><a href="#5-3-perf-性能分析工具" class="headerlink" title="5.3 perf - 性能分析工具"></a>5.3 perf - 性能分析工具</h3><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash"># 查看系统整体性能事件perf stat -a sleep 10# 记录性能数据perf record -a -g -- sleep 10# 查看性能报告perf report# 实时查看热点函数perf top</code></pre><h3 id="5-4-nmon-综合性能监控"><a href="#5-4-nmon-综合性能监控" class="headerlink" title="5.4 nmon - 综合性能监控"></a>5.4 nmon - 综合性能监控</h3><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash"># 安装wget http://sourceforge.net/projects/nmon/files/nmon_linux_x86_64.tar.gztar -xvf nmon_linux_x86_64.tar.gz# 运行./nmon# 交互式按键：# c: CPU# m: 内存# d: 磁盘# n: 网络# t: 进程</code></pre><h2 id="六、性能瓶颈诊断流程图"><a href="#六、性能瓶颈诊断流程图" class="headerlink" title="六、性能瓶颈诊断流程图"></a>六、性能瓶颈诊断流程图</h2><pre class="line-numbers language-mermaid" data-language="mermaid"><code class="language-mermaid">flowchart TD    Start([开始性能诊断]) --&gt; Step1[运行 top/htop&lt;br/&gt;查看整体负载]    Step1 --&gt; Step2[运行 vmstat 1 5&lt;br/&gt;判断瓶颈类型]        Step2 --&gt; Decision{判断瓶颈类型}        Decision --&gt;|us+sy高, id低| CPU[CPU瓶颈]    Decision --&gt;|free低, swap高| Memory[内存瓶颈]    Decision --&gt;|wa高, %util高| Disk[磁盘I/O瓶颈]    Decision --&gt;|网络流量高| Network[网络I/O瓶颈]        CPU --&gt; CPU1[top -H&lt;br/&gt;查找CPU密集进程]    CPU1 --&gt; CPU2[perf top&lt;br/&gt;分析热点函数]    CPU2 --&gt; CPU3[优化代码或扩容]    CPU3 --&gt; End1([完成])        Memory --&gt; Mem1[ps aux --sort=-%mem&lt;br/&gt;查找内存占用高的进程]    Mem1 --&gt; Mem2[pmap/valgrind&lt;br/&gt;检查内存泄漏]    Mem2 --&gt; Mem3[优化内存使用&lt;br/&gt;或增加内存]    Mem3 --&gt; End2([完成])        Disk --&gt; Disk1[iostat -x&lt;br/&gt;查看磁盘负载]    Disk1 --&gt; Disk2[iotop&lt;br/&gt;查找I/O密集进程]    Disk2 --&gt; Disk3[优化I/O&lt;br/&gt;或使用SSD]    Disk3 --&gt; End3([完成])        Network --&gt; Net1[iftop/nethogs&lt;br/&gt;查看网络流量]    Net1 --&gt; Net2[ss -s&lt;br/&gt;查看连接状态]    Net2 --&gt; Net3[优化网络&lt;br/&gt;或增加带宽]    Net3 --&gt; End4([完成])        style Start fill:#e1f5e1    style End1 fill:#e1f5e1    style End2 fill:#e1f5e1    style End3 fill:#e1f5e1    style End4 fill:#e1f5e1    style Decision fill:#fff4e1    style CPU fill:#ffe1e1    style Memory fill:#e1e1ff    style Disk fill:#ffe1ff    style Network fill:#e1ffff</code></pre><h2 id="七、常见性能问题和解决方案"><a href="#七、常见性能问题和解决方案" class="headerlink" title="七、常见性能问题和解决方案"></a>七、常见性能问题和解决方案</h2><h3 id="7-1-CPU-100-问题"><a href="#7-1-CPU-100-问题" class="headerlink" title="7.1 CPU 100% 问题"></a>7.1 CPU 100% 问题</h3><p><strong>诊断</strong>：</p><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash">top  # 找到CPU占用高的进程top -H -p &lt;PID&gt;  # 查看线程级别perf top -p &lt;PID&gt;  # 查看热点函数</code></pre><p><strong>常见原因</strong>：</p><ul><li>死循环</li><li>正则表达式回溯</li><li>大量计算</li><li>频繁的 GC（Java应用）</li></ul><h3 id="7-2-内存泄漏问题"><a href="#7-2-内存泄漏问题" class="headerlink" title="7.2 内存泄漏问题"></a>7.2 内存泄漏问题</h3><p><strong>诊断</strong>：</p><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash"># 持续监控内存增长watch -n 5 'ps aux | grep &lt;进程名&gt;'# 查看进程内存映射pmap -x &lt;PID&gt;# 使用 valgrindvalgrind --leak-check=full ./program</code></pre><p><strong>常见原因</strong>：</p><ul><li>未释放的对象引用</li><li>缓存无限增长</li><li>文件句柄未关闭</li></ul><h3 id="7-3-磁盘-I-O-慢问题"><a href="#7-3-磁盘-I-O-慢问题" class="headerlink" title="7.3 磁盘 I/O 慢问题"></a>7.3 磁盘 I/O 慢问题</h3><p><strong>诊断</strong>：</p><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash">iostat -x 1  # 查看 %util 和 awaitiotop  # 找到I/O密集进程</code></pre><p><strong>常见原因</strong>：</p><ul><li>频繁的小文件读写</li><li>没有使用缓存</li><li>磁盘碎片化</li><li>RAID 配置不当</li></ul><h3 id="7-4-网络延迟问题"><a href="#7-4-网络延迟问题" class="headerlink" title="7.4 网络延迟问题"></a>7.4 网络延迟问题</h3><p><strong>诊断</strong>：</p><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash">ping &lt;目标IP&gt;  # 测试延迟traceroute &lt;目标IP&gt;  # 追踪路由ss -s  # 查看连接状态</code></pre><p><strong>常见原因</strong>：</p><ul><li>网络拥塞</li><li>DNS 解析慢</li><li>连接池配置不当</li><li>TIME_WAIT 连接过多</li></ul><h2 id="八、性能优化建议"><a href="#八、性能优化建议" class="headerlink" title="八、性能优化建议"></a>八、性能优化建议</h2><h3 id="8-1-CPU-优化"><a href="#8-1-CPU-优化" class="headerlink" title="8.1 CPU 优化"></a>8.1 CPU 优化</h3><ul><li>减少不必要的计算</li><li>使用缓存避免重复计算</li><li>多线程/多进程并行处理</li><li>使用更高效的算法和数据结构</li></ul><h3 id="8-2-内存优化"><a href="#8-2-内存优化" class="headerlink" title="8.2 内存优化"></a>8.2 内存优化</h3><ul><li>及时释放不用的对象</li><li>使用对象池减少分配</li><li>限制缓存大小</li><li>使用内存映射文件处理大文件</li></ul><h3 id="8-3-磁盘-I-O-优化"><a href="#8-3-磁盘-I-O-优化" class="headerlink" title="8.3 磁盘 I/O 优化"></a>8.3 磁盘 I/O 优化</h3><ul><li>使用缓存减少磁盘访问</li><li>批量读写代替频繁小I/O</li><li>使用 SSD 代替机械硬盘</li><li>使用异步 I/O</li></ul><h3 id="8-4-网络-I-O-优化"><a href="#8-4-网络-I-O-优化" class="headerlink" title="8.4 网络 I/O 优化"></a>8.4 网络 I/O 优化</h3><ul><li>使用连接池</li><li>启用 TCP keepalive</li><li>调整 TCP 参数（窗口大小、队列长度）</li><li>使用 CDN 加速静态资源</li></ul><h2 id="九、快速参考命令"><a href="#九、快速参考命令" class="headerlink" title="九、快速参考命令"></a>九、快速参考命令</h2><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash"># 一键查看系统性能概况echo "=== CPU ===" &amp;&amp; top -b -n 1 | head -20 &amp;&amp; \echo "=== Memory ===" &amp;&amp; free -h &amp;&amp; \echo "=== Disk ===" &amp;&amp; df -h &amp;&amp; \echo "=== Network ===" &amp;&amp; ss -s &amp;&amp; \echo "=== Load ===" &amp;&amp; uptime# 保存性能数据用于分析sar -A &gt; sar_report.txt# 生成性能报告perf record -a -g -- sleep 30perf report &gt; perf_report.txt</code></pre><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>性能瓶颈诊断关键：</p><ol><li><strong>先整体后局部</strong>：先用 top/vmstat 看整体，再用专项工具深入</li><li><strong>看趋势不看瞬时值</strong>：持续监控一段时间，观察趋势</li><li><strong>结合业务场景</strong>：性能指标要结合实际业务负载分析</li><li><strong>量化指标</strong>：用具体数据说话，凭感觉容易事倍功半</li></ol><p>记住这个口诀：</p><ul><li><strong>CPU看 top</strong></li><li><strong>内存看 free</strong></li><li><strong>磁盘看 iostat</strong></li><li><strong>网络看 iftop</strong></li><li><strong>综合看 vmstat</strong></li></ul>]]></content>
      
      
      <categories>
          
          <category> 性能笔记 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> linux </tag>
            
            <tag> 性能笔记 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>JMeter Bin 目录结构详解与配置指南</title>
      <link href="/2026/02/10/JMeter%20Bin%20%E7%9B%AE%E5%BD%95%E7%BB%93%E6%9E%84%E8%AF%A6%E8%A7%A3%E4%B8%8E%E9%85%8D%E7%BD%AE/"/>
      <url>/2026/02/10/JMeter%20Bin%20%E7%9B%AE%E5%BD%95%E7%BB%93%E6%9E%84%E8%AF%A6%E8%A7%A3%E4%B8%8E%E9%85%8D%E7%BD%AE/</url>
      
        <content type="html"><![CDATA[<h1 id="JMeter-Bin-目录结构详解与配置指南"><a href="#JMeter-Bin-目录结构详解与配置指南" class="headerlink" title="JMeter Bin 目录结构详解与配置指南"></a>JMeter Bin 目录结构详解与配置指南</h1><p>Apache JMeter 的 <code>bin</code> 目录是其核心运行环境，包含了启动脚本、关键配置文件、依赖库及调试工具。本文档详细梳理了该目录下的文件分类、作用及操作建议。</p><h2 id="1-核心启动与运行脚本"><a href="#1-核心启动与运行脚本" class="headerlink" title="1. 核心启动与运行脚本"></a>1. 核心启动与运行脚本</h2><p>这些脚本负责启动 JMeter 的不同模式（GUI、命令行、分布式）。</p><table><thead><tr><th align="left">文件名 (Windows/Linux)</th><th align="left">作用</th><th align="left">备注</th></tr></thead><tbody><tr><td align="left"><code>jmeter.bat</code> / <code>jmeter.sh</code></td><td align="left"><strong>主启动脚本</strong></td><td align="left">默认启动 GUI 模式。<strong>不建议</strong>直接修改此文件中的内存配置。</td></tr><tr><td align="left"><code>jmeter-n.cmd</code></td><td align="left"><strong>无界面模式启动</strong></td><td align="left">快捷启动 CLI (Non-GUI) 模式，用于正式压测。</td></tr><tr><td align="left"><code>jmeter-server.bat</code> / <code>jmeter-server</code></td><td align="left"><strong>分布式从机启动</strong></td><td align="left">用于启动分布式测试中的 Server (Slave) 节点。</td></tr><tr><td align="left"><code>shutdown.cmd</code> / <code>shutdown.sh</code></td><td align="left"><strong>平滑停止</strong></td><td align="left">停止测试，但会等待当前线程运行结束（推荐）。</td></tr><tr><td align="left"><code>stoptest.cmd</code> / <code>stoptest.sh</code></td><td align="left"><strong>强制停止</strong></td><td align="left">立即终止所有线程（可能导致数据不完整）。</td></tr><tr><td align="left"><code>ApacheJMeter.jar</code></td><td align="left"><strong>核心程序包</strong></td><td align="left">所有脚本最终调用的主程序 Jar 包。<strong>严禁修改</strong>。</td></tr></tbody></table><h2 id="2-关键配置文件-properties"><a href="#2-关键配置文件-properties" class="headerlink" title="2. 关键配置文件 (.properties)"></a>2. 关键配置文件 (.properties)</h2><p>控制 JMeter 行为的核心配置。</p><table><thead><tr><th align="left">文件名</th><th align="left">作用</th><th align="left">修改建议</th></tr></thead><tbody><tr><td align="left"><code>jmeter.properties</code></td><td align="left"><strong>全局主配置</strong></td><td align="left">包含所有默认设置。<strong>建议只读</strong>，修改项请移至 <code>user.properties</code>。</td></tr><tr><td align="left"><code>user.properties</code></td><td align="left"><strong>用户自定义配置</strong></td><td align="left"><strong>推荐修改处</strong>。优先级高于主配置，升级时可直接备份迁移。</td></tr><tr><td align="left"><code>system.properties</code></td><td align="left"><strong>系统属性</strong></td><td align="left">用于设置 JVM 系统属性（如 SSL 路径、代理设置）。</td></tr><tr><td align="left"><code>log4j2.xml</code></td><td align="left"><strong>日志配置</strong></td><td align="left">控制日志级别（DEBUG/INFO/ERROR）和输出格式。</td></tr><tr><td align="left"><code>saveservice.properties</code></td><td align="left"><strong>保存服务映射</strong></td><td align="left">定义 JMX/JTL 文件的保存格式。<strong>严禁修改</strong>，否则脚本无法读取。</td></tr><tr><td align="left"><code>upgrade.properties</code></td><td align="left"><strong>升级兼容配置</strong></td><td align="left">处理版本升级时的属性映射。<strong>无需关注</strong>。</td></tr></tbody></table><h2 id="3-报告与图表-Reporting"><a href="#3-报告与图表-Reporting" class="headerlink" title="3. 报告与图表 (Reporting)"></a>3. 报告与图表 (Reporting)</h2><p>控制 HTML 测试报告（Dashboard）的生成。</p><table><thead><tr><th align="left">文件名</th><th align="left">作用</th></tr></thead><tbody><tr><td align="left"><code>reportgenerator.properties</code></td><td align="left">配置报告生成参数（如 Apdex 阈值、时间粒度）。可修改以调整报告标准。</td></tr><tr><td align="left"><code>report-template/</code></td><td align="left">存放 HTML 报告的模板文件（CSS, JS, FreeMarker）。高级用户可定制报告样式。</td></tr></tbody></table><h2 id="4-调试与诊断-Diagnostics"><a href="#4-调试与诊断-Diagnostics" class="headerlink" title="4. 调试与诊断 (Diagnostics)"></a>4. 调试与诊断 (Diagnostics)</h2><p>当 JMeter 运行异常（卡死、OOM）时的急救工具。</p><table><thead><tr><th align="left">文件名</th><th align="left">作用</th><th align="left">场景</th></tr></thead><tbody><tr><td align="left"><code>heapdump.cmd</code> / <code>sh</code></td><td align="left"><strong>导出内存快照</strong></td><td align="left">分析 OutOfMemoryError (OOM) 原因。</td></tr><tr><td align="left"><code>threaddump.cmd</code> / <code>sh</code></td><td align="left"><strong>导出线程堆栈</strong></td><td align="left">分析死锁、线程阻塞或测试无法停止的原因。</td></tr></tbody></table><h2 id="5-安全与认证-Security-amp-Auth"><a href="#5-安全与认证-Security-amp-Auth" class="headerlink" title="5. 安全与认证 (Security &amp; Auth)"></a>5. 安全与认证 (Security &amp; Auth)</h2><table><thead><tr><th align="left">文件名</th><th align="left">作用</th></tr></thead><tbody><tr><td align="left"><code>create-rmi-keystore.bat</code></td><td align="left">生成 RMI SSL 密钥库。用于分布式测试的安全通信。</td></tr><tr><td align="left"><code>krb5.conf</code></td><td align="left">Kerberos 认证配置。用于测试需 Kerberos 登录的系统。</td></tr><tr><td align="left"><code>jaas.conf</code></td><td align="left">Java 认证与授权服务配置。</td></tr></tbody></table><h2 id="6-扩展与自定义建议"><a href="#6-扩展与自定义建议" class="headerlink" title="6. 扩展与自定义建议"></a>6. 扩展与自定义建议</h2><h3 id="✅-推荐添加的文件"><a href="#✅-推荐添加的文件" class="headerlink" title="✅ 推荐添加的文件"></a>✅ 推荐添加的文件</h3><ul><li><strong><code>setenv.bat</code> (Windows) / <code>setenv.sh</code> (Linux)</strong><ul><li>  <strong>作用</strong>：官方预留的环境变量设置文件。</li><li>  <strong>用途</strong>：设置 JVM 堆内存 (<code>HEAP</code>)、GC 策略、语言环境。</li><li>  <strong>优势</strong>：与核心脚本解耦，升级 JMeter 时不会丢失配置。</li></ul></li></ul><h3 id="📝-修改原则"><a href="#📝-修改原则" class="headerlink" title="📝 修改原则"></a>📝 修改原则</h3><ol><li> **优先使用 <code>user.properties</code>**：覆盖 <code>jmeter.properties</code> 中的默认值（如 <code>sampleresult.default.encoding=UTF-8</code>）。</li><li> <strong>使用 <code>setenv</code> 管理内存</strong>：不要去改 <code>jmeter.bat</code> 里的 <code>HEAP</code> 变量。</li><li> <strong>不动核心</strong>：<code>ApacheJMeter.jar</code> 和 <code>saveservice.properties</code> 是禁区。</li></ol><h2 id="7-常用目录说明"><a href="#7-常用目录说明" class="headerlink" title="7. 常用目录说明"></a>7. 常用目录说明</h2><ul><li>  <code>templates/</code>：存放测试计划模板，可添加自定义模板。</li><li>  <code>examples/</code>：官方提供的示例脚本（CSV, JDBC 等）。</li><li>  <code>lib/</code> (位于 bin 上级)：存放第三方 Jar 包插件和 JDBC 驱动。</li></ul>]]></content>
      
      
      <categories>
          
          <category> JMeter </category>
          
      </categories>
      
      
        <tags>
            
            <tag> JMeter </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>JMeter 启动脚本 (jmeter.bat) 详解</title>
      <link href="/2026/02/09/JMeter%20%E5%90%AF%E5%8A%A8%E8%84%9A%E6%9C%AC%20(jmeter.bat)%20%E8%AF%A6%E8%A7%A3/"/>
      <url>/2026/02/09/JMeter%20%E5%90%AF%E5%8A%A8%E8%84%9A%E6%9C%AC%20(jmeter.bat)%20%E8%AF%A6%E8%A7%A3/</url>
      
        <content type="html"><![CDATA[<h1 id="JMeter-启动脚本-jmeter-bat-详解"><a href="#JMeter-启动脚本-jmeter-bat-详解" class="headerlink" title="JMeter 启动脚本 (jmeter.bat) 详解"></a>JMeter 启动脚本 (jmeter.bat) 详解</h1><p>本文档基于 <code>jmeter.bat</code>  整理，详细解析了 JMeter Windows 启动脚本的核心逻辑、关键环境变量及配置实践。</p><h2 id="1-核心功能概述"><a href="#1-核心功能概述" class="headerlink" title="1. 核心功能概述"></a>1. 核心功能概述</h2><p><code>jmeter.bat</code> 是 JMeter 在 Windows 环境下的主启动脚本。它的主要职责包括：</p><ol><li> <strong>环境检测</strong>：自动定位 <code>JMETER_HOME</code> 和 Java 运行环境。</li><li> <strong>加载配置</strong>：加载用户自定义配置 (<code>setenv.bat</code>)默认没有这个文件，按需添加。</li><li> <strong>参数组装</strong>：设置 JVM 内存 (<code>HEAP</code>)、垃圾回收 (<code>GC_ALGO</code>) 和其他启动参数 (<code>JVM_ARGS</code>)。</li><li> <strong>启动程序</strong>：最终调用 <code>java.exe</code> 启动 <code>ApacheJMeter.jar</code>。</li></ol><hr><h2 id="2-关键环境变量"><a href="#2-关键环境变量" class="headerlink" title="2. 关键环境变量"></a>2. 关键环境变量</h2><p>以下变量可以在系统环境变量中设置，或者更推荐在 <code>bin/setenv.bat</code> 中定义：</p><table><thead><tr><th align="left">变量名</th><th align="left">默认值</th><th align="left">说明</th></tr></thead><tbody><tr><td align="left"><strong><code>HEAP</code></strong></td><td align="left"><code>-Xms1g -Xmx1g -XX:MaxMetaspaceSize=256m</code></td><td align="left"><strong>最常用</strong>。JVM 堆内存设置。生产环境压测建议调大（如 4G+）。</td></tr><tr><td align="left"><strong><code>JVM_ARGS</code></strong></td><td align="left"><code>-Duser.language="en" -Duser.region="EN"</code></td><td align="left">Java 启动参数。常用于设置界面语言、代理服务器等。</td></tr><tr><td align="left"><strong><code>GC_ALGO</code></strong></td><td align="left"><code>-XX:+UseG1GC ...</code></td><td align="left">垃圾回收算法。默认使用 G1GC，通常无需修改。</td></tr><tr><td align="left"><code>JMETER_HOME</code></td><td align="left">自动猜测</td><td align="left">JMeter 安装根目录。</td></tr><tr><td align="left"><code>JMETER_BIN</code></td><td align="left"><code>%JMETER_HOME%\bin</code></td><td align="left">JMeter bin 目录。</td></tr><tr><td align="left"><code>JM_LAUNCH</code></td><td align="left"><code>java.exe</code></td><td align="left">Java 执行程序路径。</td></tr></tbody></table><hr><h2 id="3-脚本执行流程解析"><a href="#3-脚本执行流程解析" class="headerlink" title="3. 脚本执行流程解析"></a>3. 脚本执行流程解析</h2><h3 id="3-1-初始化与路径探测"><a href="#3-1-初始化与路径探测" class="headerlink" title="3.1 初始化与路径探测"></a>3.1 初始化与路径探测</h3><p>脚本首先尝试自动定位安装目录：</p><pre class="line-numbers language-batch" data-language="batch"><code class="language-batch">if not "%JMETER_HOME%" == "" goto gotHomeset "JMETER_HOME=%CURRENT_DIR%"if exist "%JMETER_HOME%\bin\jmeter.bat" goto okHome...</code></pre><h3 id="3-2-加载用户自定义配置-关键"><a href="#3-2-加载用户自定义配置-关键" class="headerlink" title="3.2 加载用户自定义配置 (关键)"></a>3.2 加载用户自定义配置 (关键)</h3><p>这是配置 JMeter 的<strong>入口</strong>。脚本会检查是否存在 <code>setenv.bat</code> 并调用它：</p><pre class="line-numbers language-batch" data-language="batch"><code class="language-batch">rem 获取标准环境变量if exist "%JMETER_HOME%\bin\setenv.bat" call "%JMETER_HOME%\bin\setenv.bat"</code></pre><blockquote><p><strong>提示</strong>：不要直接修改 <code>jmeter.bat</code>，而是创建 <code>setenv.bat</code> 来覆盖变量。这样升级 JMeter 时配置不会丢失。</p></blockquote><h3 id="3-3-语言设置"><a href="#3-3-语言设置" class="headerlink" title="3.3 语言设置"></a>3.3 语言设置</h3><p>如果未定义 <code>JMETER_LANGUAGE</code>，默认强制为英文环境：</p><pre class="line-numbers language-batch" data-language="batch"><code class="language-batch">if not defined JMETER_LANGUAGE (    set JMETER_LANGUAGE=-Duser.language="en" -Duser.region="EN")</code></pre><p><em>在 <code>setenv.bat</code> 中设置 <code>JVM_ARGS</code> 可覆盖此行为。</em></p><h3 id="3-4-Java-版本检查"><a href="#3-4-Java-版本检查" class="headerlink" title="3.4 Java 版本检查"></a>3.4 Java 版本检查</h3><p>脚本包含复杂的逻辑来解析 <code>java -version</code> 输出，确保 Java 版本符合要求（最低 1.8.0）：</p><pre class="line-numbers language-batch" data-language="batch"><code class="language-batch">for /f "tokens=3" %%g in ('java -version 2^&gt;^&amp;1 ^| findstr /i "version"') do (    set JAVAVER=%%g)</code></pre><h3 id="3-5-内存与-GC-配置"><a href="#3-5-内存与-GC-配置" class="headerlink" title="3.5 内存与 GC 配置"></a>3.5 内存与 GC 配置</h3><p>如果用户未在 <code>setenv.bat</code> 中定义，脚本将使用默认值：</p><pre class="line-numbers language-batch" data-language="batch"><code class="language-batch">if not defined HEAP (    set HEAP=-Xms1g -Xmx1g -XX:MaxMetaspaceSize=256m)if not defined GC_ALGO (    set GC_ALGO=-XX:+UseG1GC ...)</code></pre><hr><h2 id="4-最终启动命令"><a href="#4-最终启动命令" class="headerlink" title="4. 最终启动命令"></a>4. 最终启动命令</h2><p>脚本最后构建完整的启动命令：</p><pre class="line-numbers language-batch" data-language="batch"><code class="language-batch">%JM_START% "%JM_LAUNCH%" %ARGS% %JVM_ARGS% -jar "%JMETER_BIN%ApacheJMeter.jar" %JMETER_CMD_LINE_ARGS%</code></pre><ul><li>  <code>%ARGS%</code>：包含 <code>HEAP</code> + <code>GC_ALGO</code> + <code>DDRAW</code> 等核心参数。</li><li>  <code>%JVM_ARGS%</code>：包含用户自定义的额外 Java 参数。</li><li>  <code>%JMETER_CMD_LINE_ARGS%</code>：透传用户在命令行输入的参数（如 <code>-n -t test.jmx</code>）。</li></ul><hr><h2 id="5-常见配置场景"><a href="#5-常见配置场景" class="headerlink" title="5. 常见配置场景"></a>5. 常见配置场景</h2><h3 id="场景一：增加内存防止-OOM"><a href="#场景一：增加内存防止-OOM" class="headerlink" title="场景一：增加内存防止 OOM"></a>场景一：增加内存防止 OOM</h3><p>在 <code>setenv.bat</code> 中添加：</p><pre class="line-numbers language-batch" data-language="batch"><code class="language-batch">set HEAP=-Xms2g -Xmx4g -XX:MaxMetaspaceSize=512m</code></pre><h3 id="场景二：设置中文界面"><a href="#场景二：设置中文界面" class="headerlink" title="场景二：设置中文界面"></a>场景二：设置中文界面</h3><p>在 <code>setenv.bat</code> 中添加：</p><pre class="line-numbers language-batch" data-language="batch"><code class="language-batch">set JVM_ARGS=%JVM_ARGS% -Duser.language=zh -Duser.region=CN</code></pre><h3 id="场景三：配置-HTTP-代理"><a href="#场景三：配置-HTTP-代理" class="headerlink" title="场景三：配置 HTTP 代理"></a>场景三：配置 HTTP 代理</h3><p>在 <code>setenv.bat</code> 中添加：</p><pre class="line-numbers language-batch" data-language="batch"><code class="language-batch">set JVM_ARGS=%JVM_ARGS% -Dhttp.proxyHost=127.0.0.1 -Dhttp.proxyPort=8888</code></pre><h3 id="场景四：指定特定-JDK-版本"><a href="#场景四：指定特定-JDK-版本" class="headerlink" title="场景四：指定特定 JDK 版本"></a>场景四：指定特定 JDK 版本</h3><p>如果需要使用特定版本的 JDK 运行 JMeter（而非系统默认的 <code>JAVA_HOME</code>），可以在 <code>setenv.bat</code> 中临时修改环境变量：</p><pre class="line-numbers language-batch" data-language="batch"><code class="language-batch">rem 设置特定的 JDK 路径set JAVA_HOME=C:\Program Files\Java\jdk-17rem 将该 JDK 的 bin 目录添加到 PATH 最前面，确保优先使用set PATH=%JAVA_HOME%\bin;%PATH%</code></pre><p><em>(注：这种修改仅对当前 JMeter 启动过程生效，不会影响系统的全局环境变量。)</em></p><hr><h2 id="6-附录：Linux-macOS-配置指南-setenv-sh"><a href="#6-附录：Linux-macOS-配置指南-setenv-sh" class="headerlink" title="6. 附录：Linux/macOS 配置指南 (setenv.sh)"></a>6. 附录：Linux/macOS 配置指南 (setenv.sh)</h2><p>在 Linux 或 macOS 环境下，对应的启动脚本是 <code>jmeter</code> (Shell 脚本)，且配套的配置文件为 <code>bin/setenv.sh</code>。</p><h3 id="6-1-关键差异对照表"><a href="#6-1-关键差异对照表" class="headerlink" title="6.1 关键差异对照表"></a>6.1 关键差异对照表</h3><table><thead><tr><th align="left">配置项</th><th align="left">Windows (<code>setenv.bat</code>)</th><th align="left">Linux/macOS (<code>setenv.sh</code>)</th></tr></thead><tbody><tr><td align="left"><strong>赋值语法</strong></td><td align="left"><code>set VAR=VAL</code></td><td align="left"><code>export VAR="VAL"</code></td></tr><tr><td align="left"><strong>变量引用</strong></td><td align="left"><code>%VAR%</code></td><td align="left"><code>$VAR</code></td></tr><tr><td align="left"><strong>路径分隔符</strong></td><td align="left">反斜杠 <code>\</code></td><td align="left">正斜杠 <code>/</code></td></tr><tr><td align="left"><strong>路径列表分隔符</strong></td><td align="left">分号 <code>;</code></td><td align="left">冒号 <code>:</code></td></tr></tbody></table><h3 id="6-2-常用配置示例-Linux"><a href="#6-2-常用配置示例-Linux" class="headerlink" title="6.2 常用配置示例 (Linux)"></a>6.2 常用配置示例 (Linux)</h3><p><strong>1. 增加内存</strong></p><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash">export HEAP="-Xms4g -Xmx4g -XX:MaxMetaspaceSize=512m"</code></pre><p><strong>2. 设置中文界面</strong></p><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash">export JVM_ARGS="$JVM_ARGS -Duser.language=zh -Duser.region=CN"</code></pre><p><strong>3. 指定特定 JDK 版本</strong></p><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash"># 设置特定的 JDK 路径export JAVA_HOME=/usr/lib/jvm/java-17-openjdk-amd64# 将该 JDK 的 bin 目录添加到 PATH 最前面export PATH=$JAVA_HOME/bin:$PATH</code></pre>]]></content>
      
      
      <categories>
          
          <category> JMeter </category>
          
      </categories>
      
      
        <tags>
            
            <tag> JMeter </tag>
            
            <tag> 性能测试 </tag>
            
            <tag> jmeter.bat </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>JMeter vars.put 操作详细指南</title>
      <link href="/2026/02/03/JMeter%E4%B8%AD%E7%9A%84%E2%80%98vars%E2%80%99%E5%AF%B9%E8%B1%A1/"/>
      <url>/2026/02/03/JMeter%E4%B8%AD%E7%9A%84%E2%80%98vars%E2%80%99%E5%AF%B9%E8%B1%A1/</url>
      
        <content type="html"><![CDATA[<h1 id="JMeter-vars-put-操作详细指南"><a href="#JMeter-vars-put-操作详细指南" class="headerlink" title="JMeter vars.put 操作详细指南"></a>JMeter vars.put 操作详细指南</h1><h2 id="1-概述"><a href="#1-概述" class="headerlink" title="1. 概述"></a>1. 概述</h2><p><code>vars</code> 是 JMeter 内置的<strong>线程级变量操作对象</strong>，用于在当前线程的不同元件间传递数据（如 PreProcessor → Sampler → PostProcessor）。<code>vars.put()</code> 是其核心方法之一，用于将数据存储为线程级变量。</p><blockquote><p>**tags: [作用域]**：<code>vars</code> 仅对当前线程可见，不同线程的 <code>vars</code> 是完全独立的（线程隔离）。</p></blockquote><h2 id="2-vars-put-基本用法"><a href="#2-vars-put-基本用法" class="headerlink" title="2. vars.put() 基本用法"></a>2. vars.put() 基本用法</h2><h3 id="2-1-语法"><a href="#2-1-语法" class="headerlink" title="2.1 语法"></a>2.1 语法</h3><pre class="line-numbers language-java" data-language="java"><code class="language-java">// 语法：vars.put(变量名, 变量值);// 说明：变量值必须为字符串类型（非字符串类型需先转换为字符串）vars.put("key", "value");</code></pre><h3 id="2-2-示例：存储不同类型的数据"><a href="#2-2-示例：存储不同类型的数据" class="headerlink" title="2.2 示例：存储不同类型的数据"></a>2.2 示例：存储不同类型的数据</h3><h4 id="示例-1：存储字符串"><a href="#示例-1：存储字符串" class="headerlink" title="示例 1：存储字符串"></a>示例 1：存储字符串</h4><pre class="line-numbers language-groovy" data-language="groovy"><code class="language-groovy">// 存储字符串变量vars.put("username", "testuser");vars.put("password", "testpass");// 验证存储结果log.info("用户名：" + vars.get("username"));  // 输出：用户名：testuser</code></pre><h4 id="示例-2：存储数值"><a href="#示例-2：存储数值" class="headerlink" title="示例 2：存储数值"></a>示例 2：存储数值</h4><blockquote><p>**tags: [类型转换]**：<code>vars.put()</code> 仅支持字符串，存储数值时需先转换为字符串。</p></blockquote><pre class="line-numbers language-groovy" data-language="groovy"><code class="language-groovy">// 方法 1：使用 toString() 转换int age = 25;vars.put("userAge", age.toString());// 方法 2：使用字符串拼接转换int score = 95;vars.put("userScore", "" + score);// 方法 3：使用 String.valueOf() 转换long timestamp = System.currentTimeMillis();vars.put("timestamp", String.valueOf(timestamp));</code></pre><h4 id="示例-3：存储布尔值"><a href="#示例-3：存储布尔值" class="headerlink" title="示例 3：存储布尔值"></a>示例 3：存储布尔值</h4><pre class="line-numbers language-groovy" data-language="groovy"><code class="language-groovy">boolean isActive = true;vars.put("isUserActive", String.valueOf(isActive));  // 存储为 "true"</code></pre><h2 id="3-vars-相关方法"><a href="#3-vars-相关方法" class="headerlink" title="3. vars 相关方法"></a>3. vars 相关方法</h2><h3 id="3-1-存储对象：vars-putObject"><a href="#3-1-存储对象：vars-putObject" class="headerlink" title="3.1 存储对象：vars.putObject()"></a>3.1 存储对象：vars.putObject()</h3><p>当需要存储<strong>非字符串类型</strong>的数据（如数组、列表、Map 等）时，使用 <code>vars.putObject()</code>。</p><pre class="line-numbers language-java" data-language="java"><code class="language-java">// 语法：vars.putObject(变量名, 对象);vars.putObject("key", object);</code></pre><h4 id="示例：存储数组和-Map"><a href="#示例：存储数组和-Map" class="headerlink" title="示例：存储数组和 Map"></a>示例：存储数组和 Map</h4><pre class="line-numbers language-groovy" data-language="groovy"><code class="language-groovy">// 存储字符串数组def ipArray = ["192.168.1.1", "192.168.1.2", "192.168.1.3"];vars.putObject("ipList", ipArray);// 存储 Map 对象def userInfo = ["id": 1, "name": "testuser", "age": 25];vars.putObject("userMap", userInfo);// 验证存储结果log.info("IP 列表长度：" + vars.getObject("ipList").size());  // 输出：3</code></pre><h3 id="3-2-获取变量：vars-get"><a href="#3-2-获取变量：vars-get" class="headerlink" title="3.2 获取变量：vars.get()"></a>3.2 获取变量：vars.get()</h3><p>用于获取之前存储的<strong>字符串变量</strong>，若变量不存在返回 <code>null</code>。</p><pre class="line-numbers language-java" data-language="java"><code class="language-java">// 语法：vars.get(变量名);String value = vars.get("key");</code></pre><h4 id="示例"><a href="#示例" class="headerlink" title="示例"></a>示例</h4><pre class="line-numbers language-groovy" data-language="groovy"><code class="language-groovy">// 获取之前存储的变量def username = vars.get("username");def age = vars.get("userAge");// 使用获取的值if (username != null) {    log.info("用户：" + username + "，年龄：" + age);}</code></pre><h3 id="3-3-获取对象：vars-getObject"><a href="#3-3-获取对象：vars-getObject" class="headerlink" title="3.3 获取对象：vars.getObject()"></a>3.3 获取对象：vars.getObject()</h3><p>用于获取之前存储的<strong>对象</strong>，需进行类型强转。</p><pre class="line-numbers language-java" data-language="java"><code class="language-java">// 语法：vars.getObject(变量名);Object value = vars.getObject("key");</code></pre><h4 id="示例-1"><a href="#示例-1" class="headerlink" title="示例"></a>示例</h4><pre class="line-numbers language-groovy" data-language="groovy"><code class="language-groovy">// 获取数组对象def ipList = (List&lt;String&gt;) vars.getObject("ipList");log.info("第一个 IP：" + ipList[0]);  // 输出：192.168.1.1// 获取 Map 对象def userMap = (Map&lt;String, Object&gt;) vars.getObject("userMap");log.info("用户名：" + userMap.get("name"));  // 输出：testuser</code></pre><h3 id="3-4-移除变量：vars-remove"><a href="#3-4-移除变量：vars-remove" class="headerlink" title="3.4 移除变量：vars.remove()"></a>3.4 移除变量：vars.remove()</h3><p>从当前线程的变量空间中移除指定变量。</p><pre class="line-numbers language-java" data-language="java"><code class="language-java">// 语法：vars.remove(变量名);vars.remove("key");</code></pre><h4 id="示例-2"><a href="#示例-2" class="headerlink" title="示例"></a>示例</h4><pre class="line-numbers language-groovy" data-language="groovy"><code class="language-groovy">// 移除变量vars.remove("username");// 验证是否移除成功log.info("用户名是否存在：" + (vars.get("username") == null));  // 输出：true</code></pre><h3 id="3-5-清空所有变量：vars-clear"><a href="#3-5-清空所有变量：vars-clear" class="headerlink" title="3.5 清空所有变量：vars.clear()"></a>3.5 清空所有变量：vars.clear()</h3><p>清空当前线程的所有变量（谨慎使用，可能影响后续元件）。</p><pre class="line-numbers language-java" data-language="java"><code class="language-java">// 语法：vars.clear();vars.clear();</code></pre><h4 id="示例-3"><a href="#示例-3" class="headerlink" title="示例"></a>示例</h4><pre class="line-numbers language-groovy" data-language="groovy"><code class="language-groovy">// 清空所有变量vars.clear();// 验证变量数量log.info("变量数量：" + vars.entrySet().size());  // 输出：0</code></pre><h2 id="4-常见使用场景"><a href="#4-常见使用场景" class="headerlink" title="4. 常见使用场景"></a>4. 常见使用场景</h2><h3 id="场景-1：PreProcessor-中生成数据传递给-Sampler"><a href="#场景-1：PreProcessor-中生成数据传递给-Sampler" class="headerlink" title="场景 1：PreProcessor 中生成数据传递给 Sampler"></a>场景 1：PreProcessor 中生成数据传递给 Sampler</h3><blockquote><p>**tags: [典型应用]**：在请求前生成动态数据（如唯一 ID、时间戳），并在 Sampler 中使用。</p></blockquote><p><strong>示例</strong>：生成唯一请求 ID 并在 HTTP Request 中使用</p><ol><li><strong>添加 JSR223 PreProcessor</strong>（HTTP Request 的子元件）：</li></ol><pre class="line-numbers language-groovy" data-language="groovy"><code class="language-groovy">// 生成唯一请求 IDdef requestId = "REQ_" + System.currentTimeMillis();vars.put("requestId", requestId);// 生成时间戳def timestamp = String.valueOf(System.currentTimeMillis());vars.put("timestamp", timestamp);</code></pre><ol start="2"><li><strong>在 HTTP Request 中引用</strong>：<ul><li><strong>Path</strong> 字段填写：<code>/api/test?requestId=${requestId}&amp;timestamp=${timestamp}</code></li><li>实际请求路径会被替换为：<code>/api/test?requestId=REQ_1678901234567&amp;timestamp=1678901234567</code></li></ul></li></ol><h3 id="场景-2：PostProcessor-中提取响应数据传递给后续-Sampler"><a href="#场景-2：PostProcessor-中提取响应数据传递给后续-Sampler" class="headerlink" title="场景 2：PostProcessor 中提取响应数据传递给后续 Sampler"></a>场景 2：PostProcessor 中提取响应数据传递给后续 Sampler</h3><blockquote><p>**tags: [数据流转]**：从响应中提取数据（如 token、用户 ID），供后续请求使用。</p></blockquote><p><strong>示例</strong>：提取响应中的 token 并在后续请求的 Header 中使用</p><ol><li><strong>添加 JSR223 PostProcessor</strong>（第一个 HTTP Request 的子元件）：</li></ol><pre class="line-numbers language-groovy" data-language="groovy"><code class="language-groovy">// 获取响应体String response = prev.getResponseDataAsString();// 正则提取 tokendef tokenMatcher = response =~ /"token":"([^"]+)"/;if (tokenMatcher.find()) {    String token = tokenMatcher.group(1);    vars.put("authToken", token);    log.info("已提取 token：" + token);}</code></pre><ol start="2"><li><strong>在后续 HTTP Request 的 Header 中引用</strong>：<ul><li>添加 Header：<code>Authorization</code> → 值填写：<code>Bearer ${authToken}</code></li></ul></li></ol><h3 id="场景-3：存储复杂数据结构"><a href="#场景-3：存储复杂数据结构" class="headerlink" title="场景 3：存储复杂数据结构"></a>场景 3：存储复杂数据结构</h3><blockquote><p>**tags: [数据结构]**：对于数组、列表等复杂数据，使用 <code>vars.putObject()</code> 存储更高效。</p></blockquote><p><strong>示例</strong>：存储用户列表并在后续元件中遍历使用</p><ol><li><strong>在 PreProcessor 中存储用户列表</strong>：</li></ol><pre class="line-numbers language-groovy" data-language="groovy"><code class="language-groovy">// 定义用户列表def users = [    ["id": 1, "name": "user1", "age": 20],    ["id": 2, "name": "user2", "age": 25],    ["id": 3, "name": "user3", "age": 30]];// 存储为对象vars.putObject("userList", users);</code></pre><ol start="2"><li><strong>在后续 PostProcessor 中使用</strong>：</li></ol><pre class="line-numbers language-groovy" data-language="groovy"><code class="language-groovy">// 获取用户列表List&lt;Map&lt;String, Object&gt;&gt; userList = (List&lt;Map&lt;String, Object&gt;&gt;) vars.getObject("userList");// 遍历用户列表for (int i = 0; i &lt; userList.size(); i++) {    Map&lt;String, Object&gt; user = userList.get(i);    log.info("用户 " + (i+1) + "：" + user.get("name") + "，年龄：" + user.get("age"));}</code></pre><h2 id="5-注意事项"><a href="#5-注意事项" class="headerlink" title="5. 注意事项"></a>5. 注意事项</h2><blockquote><p>**tags: [警告]**：以下事项需特别注意，避免使用错误。</p></blockquote><h3 id="5-1-字符串必须加引号"><a href="#5-1-字符串必须加引号" class="headerlink" title="5.1 字符串必须加引号"></a>5.1 字符串必须加引号</h3><p>存储字符串时，必须用引号包围（双引号或单引号），否则 Groovy 会将其视为变量名（未定义会报错）。</p><pre class="line-numbers language-groovy" data-language="groovy"><code class="language-groovy">// 错误：test 被视为变量名（未定义会抛出异常）// vars.put("var", test);  // 抛出 MissingPropertyException// 正确：用引号包围字符串vars.put("var", "test");</code></pre><h3 id="5-2-跨线程共享需使用-props"><a href="#5-2-跨线程共享需使用-props" class="headerlink" title="5.2 跨线程共享需使用 props"></a>5.2 跨线程共享需使用 props</h3><p><code>vars</code> 仅对当前线程可见，若需跨线程共享数据，应使用 <code>props</code>（全局属性）。</p><pre class="line-numbers language-groovy" data-language="groovy"><code class="language-groovy">// 存储为全局属性（所有线程可见）props.put("globalVar", "value");// 在其他线程中获取String value = props.get("globalVar");</code></pre><h3 id="5-3-变量替换仅在支持的字段中生效"><a href="#5-3-变量替换仅在支持的字段中生效" class="headerlink" title="5.3 变量替换仅在支持的字段中生效"></a>5.3 变量替换仅在支持的字段中生效</h3><p><code>${变量名}</code> 语法仅在 JMeter 支持变量替换的字段中生效（如 HTTP Request 的路径、参数值、Header 等）。</p><h3 id="5-4-性能考虑"><a href="#5-4-性能考虑" class="headerlink" title="5.4 性能考虑"></a>5.4 性能考虑</h3><ul><li>避免在高频执行的元件（如循环中的 Sampler）中频繁调用 <code>vars.put()</code>/<code>vars.get()</code>，可能影响性能。</li><li>对于复杂数据结构，优先使用 <code>vars.putObject()</code> 存储，减少类型转换开销。</li></ul><h2 id="6-与其他变量操作对象的对比"><a href="#6-与其他变量操作对象的对比" class="headerlink" title="6. 与其他变量操作对象的对比"></a>6. 与其他变量操作对象的对比</h2><table><thead><tr><th>操作对象</th><th>作用域</th><th>生命周期</th><th>适用场景</th></tr></thead><tbody><tr><td><code>vars</code></td><td>当前线程</td><td>线程执行期间</td><td>线程内数据传递</td></tr><tr><td><code>props</code></td><td>全局（所有线程）</td><td>测试计划执行期间</td><td>跨线程数据共享</td></tr><tr><td><code>ctx</code></td><td>当前线程</td><td>线程执行期间</td><td>访问 JMeter 上下文（如采样器结果）</td></tr><tr><td><code>prev</code></td><td>当前线程</td><td>当前采样器执行后</td><td>访问上一个采样器的结果</td></tr></tbody></table><h2 id="7-完整示例：IP-轮询场景"><a href="#7-完整示例：IP-轮询场景" class="headerlink" title="7. 完整示例：IP 轮询场景"></a>7. 完整示例：IP 轮询场景</h2><h3 id="7-1-需求"><a href="#7-1-需求" class="headerlink" title="7.1 需求"></a>7.1 需求</h3><p>定义 IP 列表 <code>["192.168.1.1", "192.168.1.2", "192.168.1.3"]</code>，并在每次请求时按顺序轮询使用。</p><h3 id="7-2-实现步骤"><a href="#7-2-实现步骤" class="headerlink" title="7.2 实现步骤"></a>7.2 实现步骤</h3><ol><li><strong>添加 JSR223 PreProcessor</strong>（线程组级别）：</li></ol><pre class="line-numbers language-groovy" data-language="groovy"><code class="language-groovy">// 初始化 IP 列表（仅首次执行时初始化）if (!vars.containsKey("ipListInitialized")) {    def ipList = ["192.168.1.1", "192.168.1.2", "192.168.1.3"];    vars.putObject("ipList", ipList);    vars.put("ipIndex", "0");    vars.put("ipListInitialized", "true");    log.info("IP 列表已初始化：" + ipList);}// 轮询逻辑def ipList = vars.getObject("ipList");int index = Integer.parseInt(vars.get("ipIndex"));int size = ipList.size();// 获取当前 IPString currentIp = ipList[index % size];vars.put("currentIp", currentIp);// 更新索引vars.put("ipIndex", String.valueOf((index + 1) % size));log.info("当前轮询 IP：" + currentIp);</code></pre><ol start="2"><li><strong>在 HTTP Request 中引用</strong>：<ul><li><strong>Server Name or IP</strong> 字段填写：<code>${currentIp}</code></li></ul></li></ol><h3 id="7-3-执行结果"><a href="#7-3-执行结果" class="headerlink" title="7.3 执行结果"></a>7.3 执行结果</h3><ul><li>第 1 次请求：使用 IP <code>192.168.1.1</code></li><li>第 2 次请求：使用 IP <code>192.168.1.2</code></li><li>第 3 次请求：使用 IP <code>192.168.1.3</code></li><li>第 4 次请求：循环使用 IP <code>192.168.1.1</code></li></ul><h2 id="8-总结"><a href="#8-总结" class="headerlink" title="8. 总结"></a>8. 总结</h2><p><code>vars.put()</code> 是 JMeter 中管理线程级变量的核心方法，通过本文的指南，您应已掌握：</p><ul><li>✅ <code>vars.put()</code> 的基本用法和类型转换</li><li>✅ <code>vars</code> 相关方法（如 <code>putObject()</code>、<code>get()</code>、<code>remove()</code>）的使用</li><li>✅ 常见使用场景（如数据传递、响应提取、复杂数据存储）</li><li>✅ 注意事项和性能优化建议</li></ul><p>合理使用 <code>vars</code> 对象，可大幅提升 JMeter 脚本的灵活性和可维护性，实现更复杂的测试场景。</p>]]></content>
      
      
      <categories>
          
          <category> 性能测试 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> JMeter </tag>
            
            <tag> 性能测试 </tag>
            
            <tag> 变量操作 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Hexo 单仓库双分支 配合action</title>
      <link href="/2026/01/30/Hexo%20%E5%8D%95%E4%BB%93%E5%BA%93%E5%8F%8C%E5%88%86%E6%94%AF%E4%BA%91%E5%B0%8F%E8%AE%B0/"/>
      <url>/2026/01/30/Hexo%20%E5%8D%95%E4%BB%93%E5%BA%93%E5%8F%8C%E5%88%86%E6%94%AF%E4%BA%91%E5%B0%8F%E8%AE%B0/</url>
      
        <content type="html"><![CDATA[<h1 id="Hexo-单仓库双分支：摆脱本地hexo、node"><a href="#Hexo-单仓库双分支：摆脱本地hexo、node" class="headerlink" title="Hexo 单仓库双分支：摆脱本地hexo、node"></a>Hexo 单仓库双分支：摆脱本地hexo、node</h1><p>本方案旨在实现一个<strong>完全脱离本地环境</strong>、随时随地可写作的现代化博客系统。你只需要一个 GitHub 账号，即可通过网页或任何 Git 客户端发布文章。</p><h2 id="1-核心架构：单仓库双分支"><a href="#1-核心架构：单仓库双分支" class="headerlink" title="1. 核心架构：单仓库双分支"></a>1. 核心架构：单仓库双分支</h2><p>同一个 GitHub 仓库的两个分支，实现“源码”与“网页”的分离：</p><table><thead><tr><th align="left">分支名</th><th align="left">作用</th><th align="left">内容</th><th align="left">权限</th></tr></thead><tbody><tr><td align="left"><strong><code>dev</code></strong></td><td align="left"><strong>后台 (源码)</strong></td><td align="left">Markdown 文章、配置文件、<code>package.json</code></td><td align="left"><strong>默认分支</strong>，你在此写作</td></tr><tr><td align="left"><strong><code>master</code></strong></td><td align="left"><strong>前台 (网页)</strong></td><td align="left">HTML/CSS/JS (由 Action 自动生成)</td><td align="left">仅由 Robot 推送，<strong>勿手动修改</strong></td></tr></tbody></table><hr><h2 id="2-初始环境搭建-只需做一次"><a href="#2-初始环境搭建-只需做一次" class="headerlink" title="2. 初始环境搭建 (只需做一次)"></a>2. 初始环境搭建 (只需做一次)</h2><h3 id="第一步：准备仓库"><a href="#第一步：准备仓库" class="headerlink" title="第一步：准备仓库"></a>第一步：准备仓库</h3><ol><li> 在 GitHub 创建一个新仓库，名称必须为：<code>你的用户名.github.io</code>。</li><li> 仓库设置为 <strong>Public</strong>（公开）。</li></ol><h3 id="第二步：初始化分支"><a href="#第二步：初始化分支" class="headerlink" title="第二步：初始化分支"></a>第二步：初始化分支</h3><p>在本地或者直接在 GitHub 网页上操作：</p><ol><li> 确保你当前处于一个干净的分支（建议命名为 <code>dev</code>）。</li><li> 将 Hexo 源码（<code>source/</code>, <code>themes/</code>, <code>_config.yml</code>, <code>package.json</code> 等）全部推送到 <code>dev</code>。</li><li> 在 GitHub 仓库设置 (<strong>Settings -&gt; General</strong>) 中，将 <strong>Default branch</strong> 修改为 **<code>dev</code>**。</li><li> (可选) 删除远程的 <code>master</code> 分支（如果有），让 Action 稍后自动重建一个干净的。</li></ol><h3 id="第三步：配置自动化-GitHub-Actions"><a href="#第三步：配置自动化-GitHub-Actions" class="headerlink" title="第三步：配置自动化 (GitHub Actions)"></a>第三步：配置自动化 (GitHub Actions)</h3><p>在 <code>dev</code> 分支根目录下，创建文件 <code>.github/workflows/deploy.yml</code>：</p><pre class="line-numbers language-yaml" data-language="yaml"><code class="language-yaml">name: Deploy Hexo Blogon:  push:    branches:      - dev  # 监听源码分支的变动permissions:  contents: write # 必须赋予写权限jobs:  build:    runs-on: ubuntu-latest    steps:      - name: Checkout Source        uses: actions/checkout@v4        with:          ref: dev          # 拉取源码          submodules: true  # 兼容主题 submodule          fetch-depth: 0      - name: Setup Node.js        uses: actions/setup-node@v4        with:          node-version: '20' # 使用现代 Node 版本      - name: Upgrade npm &amp; Install Hexo CLI        run: |          npm install -g npm@latest          npm install -g hexo-cli      - name: Install Dependencies        run: npm install --no-optional      - name: Build Hexo        run: hexo generate      - name: Deploy to Master        uses: peaceiris/actions-gh-pages@v3        with:          github_token: ${{ secrets.GITHUB_TOKEN }}          publish_dir: ./public          publish_branch: master  # 发布到 master 分支          commit_message: "Auto deploy: ${{ github.event.head_commit.message }}"</code></pre><h3 id="第四步：开启-GitHub-Pages"><a href="#第四步：开启-GitHub-Pages" class="headerlink" title="第四步：开启 GitHub Pages"></a>第四步：开启 GitHub Pages</h3><p>等待第一次 Action 跑通（显示绿色 ✅）后：</p><ol><li> 进入仓库 <strong>Settings -&gt; Pages</strong>。</li><li> <strong>Source</strong> 选择 <code>Deploy from a branch</code>。</li><li> <strong>Branch</strong> 选择 <code>master</code> 分支（根目录 <code>/root</code>）。</li><li> 保存。</li></ol><hr><h2 id="3-日常写作流程-完全云端化"><a href="#3-日常写作流程-完全云端化" class="headerlink" title="3. 日常写作流程 (完全云端化)"></a>3. 日常写作流程 (完全云端化)</h2><p>你可以选择以下任意一种方式写文章，<strong>无需本地安装 Node.js/Hexo</strong>。</p><h3 id="方式-A：GitHub-网页版-最简单"><a href="#方式-A：GitHub-网页版-最简单" class="headerlink" title="方式 A：GitHub 网页版 (最简单)"></a>方式 A：GitHub 网页版 (最简单)</h3><ol><li> 打开你的仓库首页（默认就是 <code>dev</code> 分支）。</li><li> 进入 <code>source/_posts/</code> 目录。</li><li> 点击 <strong>Add file -&gt; Create new file</strong>。</li><li>文件名填 <code>我的新文章.md</code>，内容写入： <pre class="line-numbers language-markdown" data-language="markdown"><code class="language-markdown">---title: 我的新文章date: 2024-03-21 12:00:00tags: [生活, 记录]---这里是正文内容...</code></pre></li><li> 点击底部的 <strong>Commit changes</strong>。</li><li> <strong>完成！</strong> 等待 1-2 分钟，你的博客 <code>https://你的用户名.github.io</code> 就会自动更新。</li></ol><h3 id="方式-B：VS-Code-网页版"><a href="#方式-B：VS-Code-网页版" class="headerlink" title="方式 B：VS Code 网页版"></a>方式 B：VS Code 网页版</h3><ol><li> 在你的仓库页面，直接按键盘上的 <strong><code>.</code> (句号)</strong> 键。</li><li> 这会打开一个网页版的 VS Code 编辑器。</li><li> 像在本地一样写文章、管理文件。</li><li> 写完后在左侧 Git 栏提交并推送。</li></ol><hr><h2 id="4-避坑指南"><a href="#4-避坑指南" class="headerlink" title="4. 避坑指南"></a>4. 避坑指南</h2><ol><li><p><strong>依赖管理</strong>：</p><ul><li>  确保 <code>package.json</code> 里的 <code>hexo</code> 版本是新的（建议 <code>^7.0.0</code>）。</li><li>  <strong>不要上传</strong> <code>package-lock.json</code>（除非你很确定环境一致），让 Action 每次自己计算依赖，可以避免很多 npm 兼容性报错。</li></ul></li><li><p><strong>图片处理</strong>：</p><ul><li>  在网页端写文章时，上传图片比较麻烦。</li><li>  建议使用 <strong>图床</strong>（如 SM.MS, Imgur, 或自己搭建的 OSS），在文章里直接用 Markdown 链接 <code>![](图片URL)</code> 引用。</li></ul></li><li><p><strong>主题修改</strong>：</p><ul><li>  如果需要修改主题配置（如菜单、侧边栏），直接编辑 <code>_config.yml</code> 或主题目录下的配置文件并提交即可，Action 会自动重新编译生效。</li></ul></li></ol>]]></content>
      
      
      <categories>
          
          <category> 杂记 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 杂记 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>JMeter CLI 参数详解</title>
      <link href="/2025/11/25/JMeter%20CLI%20%E5%8F%82%E6%95%B0%E8%AF%A6%E8%A7%A3/"/>
      <url>/2025/11/25/JMeter%20CLI%20%E5%8F%82%E6%95%B0%E8%AF%A6%E8%A7%A3/</url>
      
        <content type="html"><![CDATA[<h1 id="JMeter-CLI-命令行参数详解"><a href="#JMeter-CLI-命令行参数详解" class="headerlink" title="JMeter CLI 命令行参数详解"></a>JMeter CLI 命令行参数详解</h1><h2 id="1-概述"><a href="#1-概述" class="headerlink" title="1. 概述"></a>1. 概述</h2><p>JMeter 提供了两种运行模式：<strong>GUI 模式</strong>（图形界面）和 <strong>CLI 模式</strong>（命令行模式）。CLI 模式是生产环境执行性能测试的推荐方式，具有以下优势：</p><ul><li><strong>资源占用低</strong>：无图形界面，CPU 和内存占用大幅减少</li><li><strong>自动化友好</strong>：可集成到 CI/CD 流程中</li><li><strong>稳定性高</strong>：适合长时间运行的大规模压测</li><li><strong>远程执行</strong>：支持分布式测试</li></ul><blockquote><p>**tags: [最佳实践]**：GUI 模式仅用于编写和调试脚本，实际压测应使用 CLI 模式。</p></blockquote><h2 id="2-基本参数"><a href="#2-基本参数" class="headerlink" title="2. 基本参数"></a>2. 基本参数</h2><h3 id="2-1-核心运行参数"><a href="#2-1-核心运行参数" class="headerlink" title="2.1 核心运行参数"></a>2.1 核心运行参数</h3><table><thead><tr><th>参数</th><th>描述</th><th>示例</th></tr></thead><tbody><tr><td><code>-n</code></td><td>在命令行模式下运行 JMeter（必选）</td><td><code>jmeter -n -t test.jmx</code></td></tr><tr><td><code>-t</code></td><td>指定要运行的 JMeter 测试脚本路径（必选）</td><td><code>jmeter -n -t ./scripts/test.jmx</code></td></tr><tr><td><code>-l</code></td><td>指定记录测试结果的文件路径（.jtl 格式）</td><td><code>jmeter -n -t test.jmx -l result.jtl</code></td></tr></tbody></table><h3 id="2-2-基本命令示例"><a href="#2-2-基本命令示例" class="headerlink" title="2.2 基本命令示例"></a>2.2 基本命令示例</h3><p><strong>Windows 系统</strong>：</p><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash"># 基础运行命令jmeter.bat -n -t test.jmx -l result.jtl</code></pre><p><strong>Linux/Mac 系统</strong>：</p><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash"># 基础运行命令（需先赋予执行权限：chmod +x jmeter.sh）./jmeter.sh -n -t test.jmx -l result.jtl</code></pre><h2 id="3-服务器与代理设置"><a href="#3-服务器与代理设置" class="headerlink" title="3. 服务器与代理设置"></a>3. 服务器与代理设置</h2><h3 id="3-1-分布式测试参数"><a href="#3-1-分布式测试参数" class="headerlink" title="3.1 分布式测试参数"></a>3.1 分布式测试参数</h3><table><thead><tr><th>参数</th><th>描述</th><th>示例</th></tr></thead><tbody><tr><td><code>-s</code></td><td>以服务器方式运行 JMeter</td><td><code>jmeter -s</code></td></tr><tr><td><code>-r</code></td><td>启动远程服务器（在 jmeter.properties 中定义）</td><td><code>jmeter -n -t test.jmx -r -l result.jtl</code></td></tr><tr><td><code>-R</code></td><td>启动指定的远程服务器</td><td><code>jmeter -n -t test.jmx -R 192.168.1.101,192.168.1.102 -l result.jtl</code></td></tr></tbody></table><h3 id="3-2-代理服务器设置"><a href="#3-2-代理服务器设置" class="headerlink" title="3.2 代理服务器设置"></a>3.2 代理服务器设置</h3><table><thead><tr><th>参数</th><th>描述</th><th>示例</th></tr></thead><tbody><tr><td><code>-H</code></td><td>设置代理服务器 IP 地址</td><td><code>jmeter -n -t test.jmx -H 127.0.0.1 -P 8080 -l result.jtl</code></td></tr><tr><td><code>-P</code></td><td>设置代理服务器端口</td><td><code>jmeter -n -t test.jmx -H 127.0.0.1 -P 8080 -l result.jtl</code></td></tr><tr><td><code>-u</code></td><td>设置代理服务器用户名</td><td><code>jmeter -n -t test.jmx -H 127.0.0.1 -P 8080 -u username -a password -l result.jtl</code></td></tr><tr><td><code>-a</code></td><td>设置代理服务器密码</td><td><code>jmeter -n -t test.jmx -H 127.0.0.1 -P 8080 -u username -a password -l result.jtl</code></td></tr></tbody></table><h2 id="4-属性定义"><a href="#4-属性定义" class="headerlink" title="4. 属性定义"></a>4. 属性定义</h2><h3 id="4-1-属性参数"><a href="#4-1-属性参数" class="headerlink" title="4.1 属性参数"></a>4.1 属性参数</h3><table><thead><tr><th>参数</th><th>描述</th><th>示例</th></tr></thead><tbody><tr><td><code>-p</code></td><td>指定 JMeter 属性文件路径</td><td><code>jmeter -n -t test.jmx -p ./conf/jmeter.properties -l result.jtl</code></td></tr><tr><td><code>-J</code></td><td>定义额外的 JMeter 属性（线程级）</td><td><code>jmeter -n -t test.jmx -JthreadNum=100 -JrampUp=10 -l result.jtl</code></td></tr><tr><td><code>-G</code></td><td>定义全局属性（发送到所有服务器）</td><td><code>jmeter -n -t test.jmx -Gport=8080 -Genv=production -l result.jtl</code></td></tr><tr><td><code>-D</code></td><td>定义系统属性</td><td><code>jmeter -n -t test.jmx -Djava.home=/path/to/java -Duser.language=en -l result.jtl</code></td></tr><tr><td><code>-S</code></td><td>通过属性文件定义系统属性</td><td><code>jmeter -n -t test.jmx -S ./conf/system.properties -l result.jtl</code></td></tr></tbody></table><h3 id="4-2-属性使用示例"><a href="#4-2-属性使用示例" class="headerlink" title="4.2 属性使用示例"></a>4.2 属性使用示例</h3><p><strong>示例</strong>：通过命令行参数动态修改线程数和循环次数</p><ol><li><p><strong>在测试脚本中使用属性</strong>：</p><ul><li>线程组 → 线程数：<code>${__P(threadNum, 50)}</code></li><li>线程组 → 循环次数：<code>${__P(loopNum, 10)}</code></li></ul></li><li><p><strong>命令行传参</strong>：</p><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash"># 设置线程数为 200，循环次数为 50jmeter -n -t test.jmx -JthreadNum=200 -JloopNum=50 -l result.jtl</code></pre></li></ol><h2 id="5-日志与报告"><a href="#5-日志与报告" class="headerlink" title="5. 日志与报告"></a>5. 日志与报告</h2><h3 id="5-1-日志参数"><a href="#5-1-日志参数" class="headerlink" title="5.1 日志参数"></a>5.1 日志参数</h3><table><thead><tr><th>参数</th><th>描述</th><th>示例</th></tr></thead><tbody><tr><td><code>-L</code></td><td>定义日志级别（debug、info、error 等）</td><td><code>jmeter -n -t test.jmx -Ljmeter=INFO -Ljmeter.engine=DEBUG -l result.jtl</code></td></tr><tr><td><code>-j</code></td><td>指定 JMeter 运行日志文件路径</td><td><code>jmeter -n -t test.jmx -j jmeter.log -l result.jtl</code></td></tr></tbody></table><h3 id="5-2-报告参数"><a href="#5-2-报告参数" class="headerlink" title="5.2 报告参数"></a>5.2 报告参数</h3><table><thead><tr><th>参数</th><th>描述</th><th>示例</th></tr></thead><tbody><tr><td><code>-e</code></td><td>在负载测试后生成报告仪表板</td><td><code>jmeter -n -t test.jmx -l result.jtl -e -o report</code></td></tr><tr><td><code>-o</code></td><td>指定测试报告生成文件夹路径（必须为空）</td><td><code>jmeter -n -t test.jmx -l result.jtl -e -o ./reports</code></td></tr><tr><td><code>-g</code></td><td>仅从测试结果文件生成报告仪表板</td><td><code>jmeter -g result.jtl -o report</code></td></tr></tbody></table><h3 id="5-3-报告生成示例"><a href="#5-3-报告生成示例" class="headerlink" title="5.3 报告生成示例"></a>5.3 报告生成示例</h3><p><strong>示例 1</strong>：运行脚本并生成报告</p><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash"># Windowsjmeter.bat -n -t test.jmx -l result.jtl -e -o report# Linux./jmeter.sh -n -t test.jmx -l result.jtl -e -o report</code></pre><p><strong>示例 2</strong>：从已有结果文件生成报告</p><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash">jmeter -g result.jtl -o report</code></pre><h2 id="6-其他参数"><a href="#6-其他参数" class="headerlink" title="6. 其他参数"></a>6. 其他参数</h2><table><thead><tr><th>参数</th><th>描述</th><th>示例</th></tr></thead><tbody><tr><td><code>-d</code></td><td>指定 JMeter 的主目录路径</td><td><code>jmeter -d /opt/jmeter -n -t test.jmx</code></td></tr><tr><td><code>-h</code></td><td>列出 JMeter 提供的帮助文档</td><td><code>jmeter -h</code></td></tr></tbody></table><h2 id="7-常用命令组合"><a href="#7-常用命令组合" class="headerlink" title="7. 常用命令组合"></a>7. 常用命令组合</h2><h3 id="7-1-基础压测"><a href="#7-1-基础压测" class="headerlink" title="7.1 基础压测"></a>7.1 基础压测</h3><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash"># 基础运行，仅生成结果文件jmeter -n -t test.jmx -l result.jtl</code></pre><h3 id="7-2-压测并生成报告"><a href="#7-2-压测并生成报告" class="headerlink" title="7.2 压测并生成报告"></a>7.2 压测并生成报告</h3><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash"># 运行脚本并生成 HTML 报告jmeter -n -t test.jmx -l result.jtl -e -o report</code></pre><h3 id="7-3-分布式压测"><a href="#7-3-分布式压测" class="headerlink" title="7.3 分布式压测"></a>7.3 分布式压测</h3><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash"># 启动所有远程服务器执行测试jmeter -n -t test.jmx -r -l result.jtl# 启动指定远程服务器执行测试jmeter -n -t test.jmx -R 192.168.1.101,192.168.1.102 -l result.jtl</code></pre><h3 id="7-4-带参数的压测"><a href="#7-4-带参数的压测" class="headerlink" title="7.4 带参数的压测"></a>7.4 带参数的压测</h3><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash"># 带线程数、循环次数和环境参数的压测jmeter -n -t test.jmx -JthreadNum=100 -JloopNum=50 -Jenv=staging -l result.jtl</code></pre><h3 id="7-5-带代理的压测"><a href="#7-5-带代理的压测" class="headerlink" title="7.5 带代理的压测"></a>7.5 带代理的压测</h3><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash"># 通过代理服务器执行测试jmeter -n -t test.jmx -H 127.0.0.1 -P 8080 -u username -a password -l result.jtl</code></pre><h2 id="8-注意事项"><a href="#8-注意事项" class="headerlink" title="8. 注意事项"></a>8. 注意事项</h2><h3 id="8-1-文件和目录要求"><a href="#8-1-文件和目录要求" class="headerlink" title="8.1 文件和目录要求"></a>8.1 文件和目录要求</h3><blockquote><p>**tags: [警告]**：失败案例。</p></blockquote><ul><li><code>-l</code> 指定的结果文件（.jtl）必须不存在，否则会报错</li><li><code>-o</code> 指定的报告目录必须存在且为空，否则会报错</li><li>路径中包含空格时，需要用引号包围（如 <code>"C:\Program Files\test.jmx"</code>）</li></ul><h3 id="8-2-环境配置"><a href="#8-2-环境配置" class="headerlink" title="8.2 环境配置"></a>8.2 环境配置</h3><ul><li>确保已正确设置 <code>JAVA_HOME</code> 环境变量</li><li>确保 JMeter 目录已添加到 <code>PATH</code> 环境变量（可选）</li><li>对于大规模压测，建议在 Linux 服务器上执行</li></ul><h3 id="8-3-性能优化"><a href="#8-3-性能优化" class="headerlink" title="8.3 性能优化"></a>8.3 性能优化</h3><ul><li><p><strong>内存设置</strong>：推荐使用 <code>setenv</code> 文件（而非直接修改 <code>jmeter.bat</code>/<code>jmeter.sh</code>）来配置内存分配</p><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash"># 在 JMeter 安装目录的 bin 文件夹中创建 setenv.sh（Linux/Mac）或 setenv.bat（Windows）# setenv.sh 示例：设置初始堆内存为 1G，最大堆内存为 4GHEAP="-Xms1g -Xmx4g"export HEAP</code></pre></li><li><p><strong>GC 配置</strong>：在 <code>setenv</code> 文件中添加垃圾回收器配置，提高内存使用效率</p><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash"># setenv.sh 示例：使用 G1 垃圾回收器HEAP="-Xms1g -Xmx4g"GC="-XX:+UseG1GC -XX:MaxGCPauseMillis=200"export HEAPexport GC</code></pre></li></ul><blockquote><p>**tags: [最佳实践]**：使用 <code>setenv</code> 文件可以避免直接修改 JMeter 原始脚本，便于版本升级和维护。</p></blockquote><h3 id="8-4-命令行差异"><a href="#8-4-命令行差异" class="headerlink" title="8.4 命令行差异"></a>8.4 命令行差异</h3><ul><li><p><strong>Windows CMD</strong>：<code>-D</code> 等参数后不需要加空格</p><pre class="line-numbers language-cmd" data-language="cmd"><code class="language-cmd">jmeter.bat -n -t test.jmx -DthreadNum=100 -l result.jtl</code></pre></li><li><p><strong>Windows PowerShell</strong>：<code>-D</code> 等参数后需要加空格</p><pre class="line-numbers language-powershell" data-language="powershell"><code class="language-powershell">jmeter.bat -n -t test.jmx -D threadNum=100 -l result.jtl</code></pre></li><li><p><strong>Linux/Mac</strong>：使用 <code>./jmeter.sh</code> 执行，需赋予执行权限</p><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash">chmod +x jmeter.sh./jmeter.sh -n -t test.jmx -l result.jtl</code></pre></li></ul><h2 id="9-故障排查"><a href="#9-故障排查" class="headerlink" title="9. 故障排查"></a>9. 故障排查</h2><h3 id="9-1-常见错误及解决方案"><a href="#9-1-常见错误及解决方案" class="headerlink" title="9.1 常见错误及解决方案"></a>9.1 常见错误及解决方案</h3><table><thead><tr><th>错误信息</th><th>原因</th><th>解决方案</th></tr></thead><tbody><tr><td><code>Results file ... is not empty</code></td><td>结果文件已存在</td><td>删除已存在的 .jtl 文件或使用新的文件名</td></tr><tr><td><code>Report directory ... is not empty</code></td><td>报告目录不为空</td><td>清空报告目录或使用新的目录名</td></tr><tr><td><code>Out of Memory Error</code></td><td>内存不足</td><td>增加 JMeter 的堆内存分配</td></tr><tr><td><code>Connection refused to host</code></td><td>远程服务器连接失败</td><td>检查网络连接、防火墙设置和远程服务器状态</td></tr><tr><td><code>Cannot find test plan</code></td><td>测试脚本路径错误</td><td>检查脚本路径是否正确，确保文件存在</td></tr></tbody></table><h3 id="9-2-调试技巧"><a href="#9-2-调试技巧" class="headerlink" title="9.2 调试技巧"></a>9.2 调试技巧</h3><ul><li><p><strong>查看帮助</strong>：使用 <code>-h</code> 参数查看完整的命令行帮助</p><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash">jmeter -h</code></pre></li><li><p><strong>生成详细日志</strong>：使用 <code>-j</code> 参数生成运行日志，便于排查问题</p><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash">jmeter -n -t test.jmx -j jmeter.log -l result.jtl</code></pre></li><li><p><strong>测试模式</strong>：先在本地小规模测试，确认脚本和参数正确后再进行大规模压测</p><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash"># 小规模测试：10 线程，1 循环jmeter -n -t test.jmx -JthreadNum=10 -JloopNum=1 -l result.jtl</code></pre></li></ul><h2 id="10-高级用法"><a href="#10-高级用法" class="headerlink" title="10. 高级用法"></a>10. 高级用法</h2><h3 id="10-1-批量执行脚本"><a href="#10-1-批量执行脚本" class="headerlink" title="10.1 批量执行脚本"></a>10.1 批量执行脚本</h3><p><strong>示例</strong>：使用 Shell 脚本批量执行多个 JMeter 脚本</p><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash">#!/bin/bash# 脚本目录SCRIPT_DIR="./scripts"# 结果目录RESULT_DIR="./results"# 报告目录REPORT_DIR="./reports"# 创建目录mkdir -p $RESULT_DIR $REPORT_DIR# 批量执行脚本for script in "$SCRIPT_DIR"/*.jmx; do    # 获取脚本名称（不含路径和扩展名）    script_name=$(basename "$script" .jmx)        # 执行脚本    echo "Running script: $script_name"    jmeter -n -t "$script" -l "$RESULT_DIR/${script_name}.jtl" -e -o "$REPORT_DIR/${script_name}"        echo "Script $script_name completed"    echo "----------------------------------"doneecho "All scripts completed!"</code></pre><h3 id="10-2-集成到-CI-CD-流程"><a href="#10-2-集成到-CI-CD-流程" class="headerlink" title="10.2 集成到 CI/CD 流程"></a>10.2 集成到 CI/CD 流程</h3><p><strong>示例</strong>：在 Jenkins 中执行 JMeter 压测</p><ol><li><p><strong>Jenkins 任务配置</strong>：</p><ul><li>源码管理：Git 仓库</li><li>构建步骤：执行 shell 命令</li></ul></li><li><p><strong>构建脚本</strong>：</p><pre class="line-numbers language-bash" data-language="bash"><code class="language-bash"># 安装 JMeter（如果需要）# 执行压测./apache-jmeter-5.5/bin/jmeter.sh -n -t test.jmx -l result.jtl -e -o report# 归档报告zip -r jmeter-report.zip report/</code></pre></li><li><p><strong>构建后操作</strong>：</p><ul><li>归档制品：<code>jmeter-report.zip</code></li><li>发布 HTML 报告：使用 HTML Publisher 插件</li></ul></li></ol>]]></content>
      
      
      <categories>
          
          <category> 性能测试 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> JMeter </tag>
            
            <tag> 性能测试 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>线程包括哪些状态，状态之间是如何变化</title>
      <link href="/2025/01/06/%E7%BA%BF%E7%A8%8B%E5%8C%85%E6%8B%AC%E5%93%AA%E4%BA%9B%E7%8A%B6%E6%80%81%EF%BC%8C%E7%8A%B6%E6%80%81%E4%B9%8B%E9%97%B4%E6%98%AF%E5%A6%82%E4%BD%95%E5%8F%98%E5%8C%96/"/>
      <url>/2025/01/06/%E7%BA%BF%E7%A8%8B%E5%8C%85%E6%8B%AC%E5%93%AA%E4%BA%9B%E7%8A%B6%E6%80%81%EF%BC%8C%E7%8A%B6%E6%80%81%E4%B9%8B%E9%97%B4%E6%98%AF%E5%A6%82%E4%BD%95%E5%8F%98%E5%8C%96/</url>
      
        <content type="html"><![CDATA[<h1 id="线程包括哪些状态，状态之间是如何变化"><a href="#线程包括哪些状态，状态之间是如何变化" class="headerlink" title="线程包括哪些状态，状态之间是如何变化?"></a>线程包括哪些状态，状态之间是如何变化?</h1><h2 id="1-线程状态-六种状态"><a href="#1-线程状态-六种状态" class="headerlink" title="1. 线程状态-六种状态"></a>1. 线程状态-六种状态</h2><blockquote><p>线程的状态可以参考JDK中的Thread类中的枚举State,存在<strong>六种状态</strong></p></blockquote><pre class="line-numbers language-java" data-language="java"><code class="language-java">  public enum State {//尚未启动的线程的线程状态  NEW,  //可运行线程的线程状态  RUNNABLE,//线程阻塞等待监视器锁的线程状态  BLOCKED,//等待线程的线程状态  WAITING,//具有指定等待时间的等待线程的线程状态（有限等待）  TIMED_WAITING, //已终止线程的线程状态。线程已完成执行  TERMINATED;}</code></pre><p><img src="https://r2.022032.xyz/yimouz/2026/01/2fdb8ef77376eb11c1cce6076e62efc7.png" alt="线程状态转换图"></p><p><strong>六种状态介绍:</strong></p><p><img src="https://r2.022032.xyz/yimouz/2026/01/839854b08e1175a7e13158bf2ec28662.png" alt="线程状态介绍"></p><p><strong>新建(NEW)</strong></p><blockquote><ul><li>当一个线程对象被创建，但还未调用 start 方法时处于<strong>新建</strong>状态</li><li>此时未与操作系统底层线程关联</li></ul></blockquote><p><strong>可运行(RUNNABLE)</strong>:</p><blockquote><ul><li>调用了 start 方法，就会由<strong>新建</strong>进入<strong>可运行</strong></li><li>此时与底层线程关联，由操作系统调度执行</li></ul></blockquote><p><strong>终结(TERMINATED)</strong></p><blockquote><ul><li>线程内代码已经执行完毕，由<strong>可运行</strong>进入<strong>终结</strong></li></ul></blockquote><blockquote><p>-此时会取消与底层线程关联</p></blockquote><p><strong>阻塞(BLOCKED)</strong></p><blockquote><ul><li>当获取锁失败后，由<strong>可运行</strong>进入 Monitor 的阻塞队列<strong>阻塞</strong>，此时不占用 cpu 时间</li><li>当持锁线程释放锁时，会按照一定规则唤醒阻塞队列中的<strong>阻塞</strong>线程，唤醒后的线程进入<strong>可运行</strong>状态</li></ul></blockquote><p><strong>等待(WAITING)</strong></p><blockquote><ul><li>当获取锁成功后，但由于条件不满足，调用了 wait() 方法，此时从<strong>可运行</strong>状态释放锁进入 Monitor 等待集合<strong>等待</strong>，同样不占用 cpu 时间</li><li>当其它持锁线程调用 notify() 或 notifyAll() 方法，会按照一定规则唤醒等待集合中的<strong>等待</strong>线程，恢复为<strong>可运行</strong>状态</li></ul></blockquote><p><strong>有时限等待(TIMED_WAITING)</strong></p><blockquote><ul><li>当获取锁成功后，但由于条件不满足，调用了 wait(long) 方法，此时从<strong>可运行</strong>状态释放锁进入 Monitor 等待集合进行<strong>有时限等待</strong>，同样不占用 cpu 时间</li><li>当其它持锁线程调用 notify() 或 notifyAll() 方法，会按照一定规则唤醒等待集合中的<strong>有时限等待</strong>线程，恢复为<strong>可运行</strong>状态，并重新去竞争锁</li><li>如果等待超时，也会从<strong>有时限等待</strong>状态恢复为<strong>可运行</strong>状态，并重新去竞争锁</li><li>还有一种情况是调用 sleep(long) 方法也会从<strong>可运行</strong>状态进入<strong>有时限等待</strong>状态，但与 Monitor 无关，不需要主动唤醒，超时时间到自然恢复为<strong>可运行</strong>状态</li></ul></blockquote><p><em><strong>其它情况（只需了解）</strong></em></p><blockquote><ul><li>可以用 interrupt() 方法打断<strong>等待</strong>、<strong>有时限等待</strong>的线程，让它们恢复为<strong>可运行</strong>状态</li><li>park，unpark 等方法也可以让线程等待和唤醒</li></ul></blockquote><h2 id="2-线程状态-五种状态"><a href="#2-线程状态-五种状态" class="headerlink" title="2. 线程状态-五种状态"></a>2. 线程状态-五种状态</h2><p><strong>五种状态的说法来自于操作系统层面的划分</strong></p><p><img src="https://r2.022032.xyz/yimouz/2026/01/b3acf16e360c62b80baaefbc49e9097b.png" alt="操作系统线程状态图"></p><blockquote><ul><li><p><strong>运行态</strong>：分到 cpu 时间，能真正执行线程内代码的</p></li><li><p><strong>就绪态</strong>：有资格分到 cpu 时间，但还未轮到它的</p></li><li><p><strong>阻塞态</strong>：没资格分到 cpu 时间的</p></li><li><ul><li>涵盖了 java 状态中提到的<strong>阻塞</strong>、<strong>等待</strong>、<strong>有时限等待</strong></li><li>多出了阻塞 I/O，指线程在调用阻塞 I/O 时，实际活由 I/O 设备完成，此时线程无事可做，只能干等</li></ul></li><li><p><strong>新建与终结态</strong>：与 java 中同名状态类似，不再啰嗦</p></li></ul></blockquote><h2 id="3-wait和sleep方法的不同？"><a href="#3-wait和sleep方法的不同？" class="headerlink" title="3. wait和sleep方法的不同？"></a>3. wait和sleep方法的不同？</h2><p><strong>共同点</strong></p><blockquote><p>wait() ，wait(long) 和 sleep(long) 的效果都是让当前线程暂时放弃 CPU 的使用权，进入阻塞状态</p></blockquote><p><strong>不同点</strong></p><p>1.方法归属不同</p><blockquote><ul><li>sleep(long) 是 Thread 的静态方法</li><li>而 wait()，wait(long) 都是 Object 的成员方法，每个对象都有 2.醒来时机不同</li><li>执行 sleep(long) 和 wait(long) 的线程都会在等待相应毫秒后醒来</li><li>wait(long) 和 wait() 还可以被 notify 唤醒，wait() 如果不唤醒就一直等下去</li><li>它们都可以被打断唤醒</li></ul></blockquote><ol><li>锁特性不同（重点）</li></ol><blockquote><ul><li>wait 方法的调用必须先获取 wait 对象的锁，而 sleep 则无此限制</li><li>wait 方法执行后会释放对象锁，允许其它线程获得该对象锁（我放弃 cpu，但你们还可以用）</li><li>而 sleep 如果在 synchronized 代码块中执行，并不会释放对象锁（我放弃 cpu，你们也用不了）</li></ul></blockquote><h2 id="3-线程状态面试题"><a href="#3-线程状态面试题" class="headerlink" title="3. 线程状态面试题"></a>3. 线程状态面试题</h2><p><strong>面试官</strong>：线程包括哪些状态，状态之间是如何变化的？</p><p><strong>候选人：</strong></p><blockquote><p>在JDK中的Thread类中的枚举State里面定义了6中线程的状态分别是：新建、可运行、终结、阻塞、等待和有时限等待六种。</p><p>关于线程的状态切换情况比较多。我分别介绍一下</p><p>当一个线程对象被创建，但还未调用 start 方法时处于<strong>新建</strong>状态，调用了 start 方法，就会由<strong>新建</strong>进入<strong>可运行</strong>状态。如果线程内代码已经执行完毕，由<strong>可运行</strong>进入<strong>终结</strong>状态。当然这些是一个线程正常执行情况。</p><p>如果线程获取锁失败后，由<strong>可运行</strong>进入 Monitor 的阻塞队列<strong>阻塞</strong>，只有当持锁线程释放锁时，会按照一定规则唤醒阻塞队列中的<strong>阻塞</strong>线程，唤醒后的线程进入<strong>可运行</strong>状态</p><p>如果线程获取锁成功后，但由于条件不满足，调用了 wait() 方法，此时从<strong>可运行</strong>状态释放锁<strong>等待</strong>状态，当其它持锁线程调用 notify() 或 notifyAll() 方法，会恢复为<strong>可运行</strong>状态</p><p>还有一种情况是调用 sleep(long) 方法也会从<strong>可运行</strong>状态进入<strong>有时限等待</strong>状态，不需要主动唤醒，超时时间到自然恢复为<strong>可运行</strong>状态</p></blockquote>]]></content>
      
      
      <categories>
          
          <category> 性能笔记 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 线程池 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>JMeter-prev</title>
      <link href="/2024/12/03/%E4%BD%BF%E7%94%A8%20JMeter%20%E4%B8%AD%E7%9A%84%20%60prev%60%E5%AF%B9%E8%B1%A1/"/>
      <url>/2024/12/03/%E4%BD%BF%E7%94%A8%20JMeter%20%E4%B8%AD%E7%9A%84%20%60prev%60%E5%AF%B9%E8%B1%A1/</url>
      
        <content type="html"><![CDATA[<p> JMeter 中，<code>prev</code> 对象是一个非常重要的变量，通常用于在 JSR223 Sampler 或者 BeanShell Sampler 中获取上一个 Sampler 的结果。记录下 <code>prev</code> 对象的常用 API。</p><h3 id="什么是-prev-对象？"><a href="#什么是-prev-对象？" class="headerlink" title="什么是 prev 对象？"></a>什么是 <code>prev</code> 对象？</h3><p><code>prev</code> 对象是一个 <code>SampleResult</code> 类型的实例，它包含了上一个 Sampler 的所有信息和结果。通过使用 <code>prev</code> 对象，我们可以访问上一个请求的响应数据、响应时间、响应代码等信息。</p><h3 id="常用的-prev-API"><a href="#常用的-prev-API" class="headerlink" title="常用的 prev API"></a>常用的 <code>prev</code> API</h3><p>下面是 <code>prev</code> 对象的一些常用 API 和属性：</p><ol><li><p><strong>getTime()</strong></p><ul><li>返回采样器的响应时间（以毫秒为单位）。<pre class="line-numbers language-java" data-language="java"><code class="language-java">long responseTime = prev.getTime();</code></pre></li></ul></li><li><p><strong>getResponseCode()</strong></p><ul><li>返回采样器的响应代码（通常是 HTTP 状态码）。<pre class="line-numbers language-java" data-language="java"><code class="language-java">String responseCode = prev.getResponseCode();</code></pre></li></ul></li><li><p><strong>getResponseMessage()</strong></p><ul><li>返回采样器的响应消息。<pre class="line-numbers language-java" data-language="java"><code class="language-java">String responseMessage = prev.getResponseMessage();</code></pre></li></ul></li><li><p><strong>getResponseDataAsString()</strong></p><ul><li>以字符串形式返回响应数据。<pre class="line-numbers language-java" data-language="java"><code class="language-java">String responseData = prev.getResponseDataAsString();</code></pre></li></ul></li><li><p><strong>getResponseHeaders()</strong></p><ul><li>返回响应头信息。<pre class="line-numbers language-java" data-language="java"><code class="language-java">String responseHeaders = prev.getResponseHeaders();</code></pre></li></ul></li><li><p><strong>getRequestHeaders()</strong></p><ul><li>返回请求头信息。<pre class="line-numbers language-java" data-language="java"><code class="language-java">String requestHeaders = prev.getRequestHeaders();</code></pre></li></ul></li><li><p><strong>getSamplerData()</strong></p><ul><li>返回采样器的请求数据。<pre class="line-numbers language-java" data-language="java"><code class="language-java">String samplerData = prev.getSamplerData();</code></pre></li></ul></li><li><p><strong>getSampleLabel()</strong></p><ul><li>返回采样器的标签。<pre class="line-numbers language-java" data-language="java"><code class="language-java">String sampleLabel = prev.getSampleLabel();</code></pre></li></ul></li><li><p><strong>isSuccessful()</strong></p><ul><li>返回采样器的执行结果是否成功。<pre class="line-numbers language-java" data-language="java"><code class="language-java">boolean isSuccess = prev.isSuccessful();</code></pre></li></ul></li><li><p><strong>getURL()</strong></p><ul><li>返回采样器的请求 URL。<pre class="line-numbers language-java" data-language="java"><code class="language-java">String url = prev.getURL();</code></pre></li></ul></li><li><p><strong>getStartTime()</strong></p><ul><li>返回采样器开始时间（以毫秒为单位）。<pre class="line-numbers language-java" data-language="java"><code class="language-java">long startTime = prev.getStartTime();</code></pre></li></ul></li><li><p><strong>getEndTime()</strong></p><ul><li>返回采样器结束时间（以毫秒为单位）。<pre class="line-numbers language-java" data-language="java"><code class="language-java">long endTime = prev.getEndTime();</code></pre></li></ul></li></ol><h3 id="使用示例"><a href="#使用示例" class="headerlink" title="使用示例"></a>使用示例</h3><p>假设我们在测试一个 RESTful API，并且需要在每个请求之后检查响应数据是否包含特定的字符串。我们可以使用 JSR223 Sampler 来实现这个功能。以下是一个示例脚本：</p><pre class="line-numbers language-java" data-language="java"><code class="language-java">// 获取上一个采样器的响应数据String responseData = prev.getResponseDataAsString();// 检查响应数据是否包含特定字符串if (responseData.contains("code")) {    // 如果包含，记录日志并继续    log.info("Response contains the expected string.");} else {    // 如果不包含，记录日志并标记测试失败    log.error("Response does not contain the expected string.");    AssertionResult.setFailure(true);    AssertionResult.setFailureMessage("Response does not contain the expected string.");}</code></pre><p>就算是随便记录也希望能够帮助我们更好理解，不要健忘。</p>]]></content>
      
      
      <categories>
          
          <category> 性能笔记 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> JMeter </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title></title>
      <link href="/2024/04/22/%E5%BF%AB%E9%80%9F%E5%BC%80%E5%A7%8B%E4%B8%80%E4%B8%AA%E6%96%B0%E7%9A%84xui%E9%9D%A2%E6%9D%BF/"/>
      <url>/2024/04/22/%E5%BF%AB%E9%80%9F%E5%BC%80%E5%A7%8B%E4%B8%80%E4%B8%AA%E6%96%B0%E7%9A%84xui%E9%9D%A2%E6%9D%BF/</url>
      
        <content type="html"><![CDATA[<p><code>yum update -y</code><br>等待结束<br><code>bash &lt;(curl -Ls https://raw.githubusercontent.com/vaxilu/x-ui/master/install.sh)</code><br>交互下等待结束<br><code>x-ui</code><br>面板可以更新下bbr<br>centos开放面板端口<br><code>firewall-cmd --zone=public --add-port=18000/tcp --permanent</code><br><code>firewall-cmd --reload</code></p>]]></content>
      
      
      
        <tags>
            
            <tag> 养鸡技巧 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>JMeter POST 请求缺失 Header 需要手动添加</title>
      <link href="/2021/06/10/JMeter%20POST%20%E8%AF%B7%E6%B1%82%E7%BC%BA%E5%A4%B1%20Header%20%E7%9A%84%E8%A7%A3%E5%86%B3%E6%96%B9%E6%A1%88/"/>
      <url>/2021/06/10/JMeter%20POST%20%E8%AF%B7%E6%B1%82%E7%BC%BA%E5%A4%B1%20Header%20%E7%9A%84%E8%A7%A3%E5%86%B3%E6%96%B9%E6%A1%88/</url>
      
        <content type="html"><![CDATA[<h1 id="JMeter-POST-请求缺失-Header-的解决方案"><a href="#JMeter-POST-请求缺失-Header-的解决方案" class="headerlink" title="JMeter POST 请求缺失 Header 的解决方案"></a>JMeter POST 请求缺失 Header 的解决方案</h1><h2 id="1-问题背景"><a href="#1-问题背景" class="headerlink" title="1. 问题背景"></a>1. 问题背景</h2><p>在进行接口测试（特别是 POST 请求）时，如果没有显式添加 <code>Content-Type</code> 头，服务端往往无法正确解析请求体，从而返回 <code>415 Unsupported Media Type</code> 或 <code>400 Bad Request</code> 错误。<br>最常见的场景是发送 JSON 数据，但未设置 <code>Content-Type: application/json</code>。</p><h2 id="2-核心解决方案：使用-HTTP-Header-Manager"><a href="#2-核心解决方案：使用-HTTP-Header-Manager" class="headerlink" title="2. 核心解决方案：使用 HTTP Header Manager"></a>2. 核心解决方案：使用 HTTP Header Manager</h2><p>这是解决该问题最标准、最灵活的方法，适用于 JSON、XML 等各种数据格式。</p><h3 id="操作步骤"><a href="#操作步骤" class="headerlink" title="操作步骤"></a>操作步骤</h3><ol><li><strong>添加组件</strong>：<ul><li>  选中 <strong>Test Plan (测试计划)</strong> 或 **Thread Group (线程组)**。</li><li>  右键菜单选择：<code>Add</code> -&gt; <code>Config Element</code> -&gt; <code>HTTP Header Manager</code> (HTTP信息头管理器)。</li></ul></li><li><strong>配置参数</strong>：<ul><li>  点击底部的 <code>Add</code> 按钮新增一行。</li><li>  <strong>Name</strong>: <code>Content-Type</code></li><li>  <strong>Value</strong>: <code>application/json</code> (如果是其他格式，填写对应的值，如 <code>application/xml</code>)</li></ul></li></ol><h3 id="作用范围说明"><a href="#作用范围说明" class="headerlink" title="作用范围说明"></a>作用范围说明</h3><ul><li>  <strong>全局生效</strong>：如果在 Test Plan 根节点下添加，所有请求都会默认带上该 Header。</li><li>  <strong>局部生效</strong>：如果在某个 Thread Group 或具体的 HTTP Request 下添加，仅对该范围内的请求生效。</li></ul><h2 id="3-常见误区：修改-jmeter-properties"><a href="#3-常见误区：修改-jmeter-properties" class="headerlink" title="3. 常见误区：修改 jmeter.properties"></a>3. 常见误区：修改 jmeter.properties</h2><p><strong>注意</strong>：修改配置文件中的 <code>post_add_content_type_if_missing</code> 无法解决 JSON 格式的 Header 缺失问题。</p><ul><li>  <strong>局限性</strong>：该配置项开启后，<strong>只能</strong> 自动补全 <code>application/x-www-form-urlencoded</code>。</li><li>  <strong>无效操作</strong>：将该配置项的值改为 <code>json</code> 或 <code>true</code> 都无法让 JMeter 自动添加 <code>application/json</code>。</li><li>  <strong>结论</strong>：请完全忽略此配置项，始终使用 <strong>HTTP Header Manager</strong> 来管理请求头。</li></ul><h3 id="补充说明：隐式行为-Implicit-Behavior"><a href="#补充说明：隐式行为-Implicit-Behavior" class="headerlink" title="补充说明：隐式行为 (Implicit Behavior)"></a>补充说明：隐式行为 (Implicit Behavior)</h3><p>如果开启了该配置 (<code>true</code>)，其补全行为是 <strong>隐式的</strong>：</p><ol><li> <strong>GUI 不可见</strong>：你<strong>不会</strong>在测试计划界面看到任何自动生成的 Header Manager 组件。</li><li> <strong>结果可见</strong>：只有在运行测试后，查看 <strong>View Results Tree (查看结果树)</strong> -&gt; <strong>Request</strong> -&gt; <strong>Request Headers</strong> 时，才会发现 <code>Content-Type: application/x-www-form-urlencoded</code> 被默默添加到了请求中。</li></ol><h3 id="补充说明：优先级与冲突-Priority"><a href="#补充说明：优先级与冲突-Priority" class="headerlink" title="补充说明：优先级与冲突 (Priority)"></a>补充说明：优先级与冲突 (Priority)</h3><p>如果开启了自动补全，同时你又手动设置了 Header，结果是：<strong>以你的手动设置为准</strong>。<br>该配置项的名称 <code>..._if_missing</code> 揭示了其逻辑：</p><ul><li><strong>检查机制</strong>：JMeter 发送请求前会检查是否已有 <code>Content-Type</code>。</li><li><strong>用户优先</strong>：只要检测到你设置了该 Header（无论值是 json、xml 还是其他），隐式补全行为就<strong>直接跳过</strong>。</li><li><strong>最终结果</strong>：<strong>不会覆盖</strong>，也<strong>不会出现两个 Header</strong>。你的设置完全生效，隐式行为完全失效。</li></ul><h2 id="4-常见-HTTP-Header-及使用场景指南"><a href="#4-常见-HTTP-Header-及使用场景指南" class="headerlink" title="4. 常见 HTTP Header 及使用场景指南"></a>4. 常见 HTTP Header 及使用场景指南</h2><p>在接口测试中，正确设置 Header 是请求成功的关键。以下是高频使用的 Header 及其对应值。</p><h3 id="4-1-数据格式类-Content-Type"><a href="#4-1-数据格式类-Content-Type" class="headerlink" title="4.1 数据格式类 (Content-Type)"></a>4.1 数据格式类 (Content-Type)</h3><p>告诉服务端<strong>你发送的数据是什么格式</strong>。</p><table><thead><tr><th align="left">场景</th><th align="left">Header 值</th><th align="left">说明</th></tr></thead><tbody><tr><td align="left"><strong>JSON 接口</strong> (最常见)</td><td align="left"><code>application/json</code></td><td align="left">RESTful API 标准格式</td></tr><tr><td align="left"><strong>表单提交</strong></td><td align="left"><code>application/x-www-form-urlencoded</code></td><td align="left">类似 HTML <code>&lt;form&gt;</code> 提交，键值对形式</td></tr><tr><td align="left"><strong>文件上传</strong></td><td align="left"><code>multipart/form-data</code></td><td align="left"><strong>注意</strong>：通常不需要手动设置，勾选 JMeter “Use multipart/form-data” 即可自动处理</td></tr><tr><td align="left"><strong>XML 接口</strong></td><td align="left"><code>application/xml</code> 或 <code>text/xml</code></td><td align="left">旧式 SOAP 或 XML API</td></tr></tbody></table><h3 id="4-2-认证鉴权类-Authorization"><a href="#4-2-认证鉴权类-Authorization" class="headerlink" title="4.2 认证鉴权类 (Authorization)"></a>4.2 认证鉴权类 (Authorization)</h3><p>告诉服务端<strong>你是谁</strong>。</p><table><thead><tr><th align="left">场景</th><th align="left">Header 值示例</th><th align="left">说明</th></tr></thead><tbody><tr><td align="left"><strong>Token 认证</strong> (JWT/OAuth)</td><td align="left"><code>Bearer &lt;your_token_string&gt;</code></td><td align="left">最通用的现代 API 认证方式</td></tr><tr><td align="left"><strong>Basic 认证</strong></td><td align="left"><code>Basic &lt;base64_user_pass&gt;</code></td><td align="left">较旧，用户名密码 Base64 编码</td></tr><tr><td align="left"><strong>自定义 Key</strong></td><td align="left"><code>X-API-KEY: abc12345</code></td><td align="left">部分服务商特定的 Header 名称</td></tr></tbody></table><h3 id="4-3-客户端协商类"><a href="#4-3-客户端协商类" class="headerlink" title="4.3 客户端协商类"></a>4.3 客户端协商类</h3><p>告诉服务端<strong>你想要什么</strong>或<strong>你用什么设备</strong>。</p><table><thead><tr><th align="left">Header 名称</th><th align="left">常见值</th><th align="left">说明</th></tr></thead><tbody><tr><td align="left"><strong>Accept</strong></td><td align="left"><code>application/json</code></td><td align="left">期望服务端返回 JSON 格式数据</td></tr><tr><td align="left"><strong>User-Agent</strong></td><td align="left"><code>Mozilla/5.0 ...</code></td><td align="left">模拟浏览器（Chrome/Safari）或手机（Android/iOS），防止被识别为爬虫/机器人</td></tr><tr><td align="left"><strong>Referer</strong></td><td align="left"><code>https://www.example.com/login</code></td><td align="left">来源页面，防盗链检查常需要此字段</td></tr></tbody></table><h3 id="4-4-必备-Header-清单-Checklist"><a href="#4-4-必备-Header-清单-Checklist" class="headerlink" title="4.4 必备 Header 清单 (Checklist)"></a>4.4 必备 Header 清单 (Checklist)</h3><h4 id="针对-REST-API-接口测试："><a href="#针对-REST-API-接口测试：" class="headerlink" title="针对 REST API 接口测试："></a>针对 REST API 接口测试：</h4><ul><li><input checked="" disabled="" type="checkbox"> <strong>Content-Type</strong>: <code>application/json</code> (如果是 POST/PUT)</li><li><input checked="" disabled="" type="checkbox"> <strong>Authorization</strong>: <code>Bearer ...</code> (如果需要登录)</li><li><input disabled="" type="checkbox"> <strong>Accept</strong>: <code>application/json</code> (推荐加上)</li></ul><h4 id="针对-模拟浏览器行为："><a href="#针对-模拟浏览器行为：" class="headerlink" title="针对 模拟浏览器行为："></a>针对 模拟浏览器行为：</h4><ul><li><input checked="" disabled="" type="checkbox"> <strong>User-Agent</strong>: 必须模拟真实浏览器字符串</li><li><input checked="" disabled="" type="checkbox"> <strong>Referer/Origin</strong>: 涉及跨域或安全检查时必须</li><li><input checked="" disabled="" type="checkbox"> <strong>Cookie</strong>: 详见下方说明（推荐使用 Cookie Manager，但也支持手动添加）</li></ul><h3 id="4-5-特殊-Header：Cookie-的处理"><a href="#4-5-特殊-Header：Cookie-的处理" class="headerlink" title="4.5 特殊 Header：Cookie 的处理"></a>4.5 特殊 Header：Cookie 的处理</h3><p>虽然 Cookie 本质上也是 Header (<code>Cookie: name=value</code>)，但 JMeter 提供了两种处理方式：</p><h4 id="方式一：使用-HTTP-Cookie-Manager-强烈推荐"><a href="#方式一：使用-HTTP-Cookie-Manager-强烈推荐" class="headerlink" title="方式一：使用 HTTP Cookie Manager (强烈推荐)"></a>方式一：使用 HTTP Cookie Manager (强烈推荐)</h4><p>这是最接近真实浏览器行为的方式。</p><ul><li>  <strong>自动维护</strong>：自动解析服务端返回的 <code>Set-Cookie</code> 并在后续请求中携带，无需人工干预。</li><li>  <strong>手动添加</strong>：支持在组件界面下方 “User-Defined Cookies” 表格中添加自定义 Cookie（如预埋 Token）。</li></ul><h4 id="方式二：使用-HTTP-Header-Manager-手动强注"><a href="#方式二：使用-HTTP-Header-Manager-手动强注" class="headerlink" title="方式二：使用 HTTP Header Manager (手动强注)"></a>方式二：使用 HTTP Header Manager (手动强注)</h4><ul><li>  <strong>操作</strong>：直接添加 Name=<code>Cookie</code>, Value=<code>session_id=xyz123</code>。</li><li>  <strong>特点</strong>：简单粗暴，强制生效。</li><li>  <strong>缺点</strong>：<strong>不会</strong>自动更新服务端下发的新 Cookie，<strong>不会</strong>自动维护 Session 上下文。仅适用于简单的单次请求调试或固定 Cookie 场景。</li></ul>]]></content>
      
      
      <categories>
          
          <category> JMeter </category>
          
      </categories>
      
      
        <tags>
            
            <tag> JMeter </tag>
            
            <tag> HTTP Header </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>JMeter响应中文乱码解决方案</title>
      <link href="/2021/06/08/JMeter%E5%93%8D%E5%BA%94%E4%B8%AD%E6%96%87%E4%B9%B1%E7%A0%81%E8%A7%A3%E5%86%B3%E6%96%B9%E6%A1%88/"/>
      <url>/2021/06/08/JMeter%E5%93%8D%E5%BA%94%E4%B8%AD%E6%96%87%E4%B9%B1%E7%A0%81%E8%A7%A3%E5%86%B3%E6%96%B9%E6%A1%88/</url>
      
        <content type="html"><![CDATA[<h1 id="JMeter-响应中文乱码解决方案"><a href="#JMeter-响应中文乱码解决方案" class="headerlink" title="JMeter 响应中文乱码解决方案"></a>JMeter 响应中文乱码解决方案</h1><p>JMeter 响应数据出现中文乱码，通常是因为 JMeter 默认使用 <code>ISO-8859-1</code> 编码，而接口返回的数据是 <code>UTF-8</code> 编码。</p><p>以下是几种常用的解决方案，按<strong>推荐程度</strong>排序：</p><h3 id="1-修改配置文件（全局生效，最推荐）"><a href="#1-修改配置文件（全局生效，最推荐）" class="headerlink" title="1. 修改配置文件（全局生效，最推荐）"></a>1. 修改配置文件（全局生效，最推荐）</h3><p>这是最彻底的解决方法，修改一次后对所有脚本生效。</p><ol><li> 进入 JMeter 安装目录的 <code>bin</code> 文件夹。</li><li> 找到并打开 <code>jmeter.properties</code> 文件。</li><li> 搜索关键字 <code>sampleresult.default.encoding</code>。</li><li>找到该行，去掉行首的 <code>#</code> 号，并将值修改为 <code>UTF-8</code>： <pre class="line-numbers language-properties" data-language="properties"><code class="language-properties"># 修改前#sampleresult.default.encoding=ISO-8859-1# 修改后sampleresult.default.encoding=UTF-8</code></pre></li><li> <strong>保存文件并重启 JMeter</strong>。</li></ol><hr><h3 id="2-在-HTTP-请求中指定编码（局部生效）"><a href="#2-在-HTTP-请求中指定编码（局部生效）" class="headerlink" title="2. 在 HTTP 请求中指定编码（局部生效）"></a>2. 在 HTTP 请求中指定编码（局部生效）</h3><p>如果你没有权限修改配置文件，或者只想针对某个特定接口生效：</p><ol><li> 在 JMeter 界面中选中你的 **HTTP Request (HTTP 请求)**。</li><li> 找到 <strong>Content encoding (内容编码)</strong> 字段。</li><li> 手动输入 <code>utf-8</code>（不区分大小写）。</li></ol><hr><h3 id="3-使用-BeanShell-后置处理器（脚本强制转换）"><a href="#3-使用-BeanShell-后置处理器（脚本强制转换）" class="headerlink" title="3. 使用 BeanShell 后置处理器（脚本强制转换）"></a>3. 使用 BeanShell 后置处理器（脚本强制转换）</h3><p>如果以上方法都无效，可以使用脚本强制修改响应数据的编码：</p><ol><li> 在对应的 HTTP 请求节点上右键，选择 <strong>Add (添加)</strong> -&gt; <strong>Post Processors (后置处理器)</strong> -&gt; <strong>BeanShell PostProcessor</strong>。</li><li>在 <strong>Script</strong> 区域输入以下代码： <pre class="line-numbers language-java" data-language="java"><code class="language-java">prev.setDataEncoding("UTF-8");</code></pre></li></ol><hr><h3 id="4-针对请求体（Body）乱码"><a href="#4-针对请求体（Body）乱码" class="headerlink" title="4. 针对请求体（Body）乱码"></a>4. 针对请求体（Body）乱码</h3><p>如果是你发送给服务器的中文变成乱码（而不是接收乱码），请检查：</p><ul><li>  <strong>HTTP 请求头</strong>：确保添加了 <code>Content-Type: application/json;charset=UTF-8</code>。</li><li>  <strong>勾选编码</strong>：在 HTTP 请求的 Parameters 中，勾选 “URL Encode”。</li></ul>]]></content>
      
      
      <categories>
          
          <category> JMeter </category>
          
      </categories>
      
      
        <tags>
            
            <tag> JMeter </tag>
            
            <tag> 性能测试 </tag>
            
            <tag> 乱码处理 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>性能笔记-几个瓶颈</title>
      <link href="/2020/05/26/%E6%80%A7%E8%83%BD%E7%AC%94%E8%AE%B0-%E5%87%A0%E4%B8%AA%E7%93%B6%E9%A2%88/"/>
      <url>/2020/05/26/%E6%80%A7%E8%83%BD%E7%AC%94%E8%AE%B0-%E5%87%A0%E4%B8%AA%E7%93%B6%E9%A2%88/</url>
      
        <content type="html"><![CDATA[<h1 id="性能测试的几个瓶颈术语"><a href="#性能测试的几个瓶颈术语" class="headerlink" title="性能测试的几个瓶颈术语"></a>性能测试的几个瓶颈术语</h1><h2 id="软件瓶颈"><a href="#软件瓶颈" class="headerlink" title="软件瓶颈"></a>软件瓶颈</h2><h3 id="应用软件瓶颈"><a href="#应用软件瓶颈" class="headerlink" title="应用软件瓶颈"></a>应用软件瓶颈</h3><p>web服务器包括数据库等中间件导致的响应时间增加</p><h3 id="应用程序瓶颈"><a href="#应用程序瓶颈" class="headerlink" title="应用程序瓶颈"></a>应用程序瓶颈</h3><p>处理问题只能串行不能并行导致的响应时间增加</p><h2 id="硬件瓶颈"><a href="#硬件瓶颈" class="headerlink" title="硬件瓶颈"></a>硬件瓶颈</h2><p>cpu ram 持续利用率过高超过95%</p><h2 id="操作系统瓶颈"><a href="#操作系统瓶颈" class="headerlink" title="操作系统瓶颈"></a>操作系统瓶颈</h2><p>例如win系虚拟内存的设置交互</p><h2 id="网络设备瓶颈"><a href="#网络设备瓶颈" class="headerlink" title="网络设备瓶颈"></a>网络设备瓶颈</h2><p>负载均衡 防火墙 交换机 相关</p>]]></content>
      
      
      <categories>
          
          <category> 性能笔记 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 杂记 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>使用GIT远程仓管理hexo文档</title>
      <link href="/2020/05/22/%E4%BD%BF%E7%94%A8GIT%E8%BF%9C%E7%A8%8B%E4%BB%93%E7%AE%A1%E7%90%86hexo%E6%96%87%E6%A1%A3/"/>
      <url>/2020/05/22/%E4%BD%BF%E7%94%A8GIT%E8%BF%9C%E7%A8%8B%E4%BB%93%E7%AE%A1%E7%90%86hexo%E6%96%87%E6%A1%A3/</url>
      
        <content type="html"><![CDATA[<h3 id="Hexo简单的命令"><a href="#Hexo简单的命令" class="headerlink" title="Hexo简单的命令"></a>Hexo简单的命令</h3><ol><li> 终端执行<code>hexo new 新文章</code>命令，此时本地<code>source/_post</code>目录下会新建<code>新文章.md</code>文件</li><li> 打开 <code>新文章.md</code>编写完成后，终端执行<code>hexo g</code>编译</li><li> 终端执行<code>hexo s</code>本地预览</li><li> 终端执行<code>hexo d</code>推送到远程仓<strong>master</strong>分支</li></ol><h3 id="场景备份的建议"><a href="#场景备份的建议" class="headerlink" title="场景备份的建议"></a>场景备份的建议</h3><ul><li>个人pc比较多,有时候不是专门用一台机器来做一件事,公司家庭暂住地等等等等,多台pc的使用场景</li><li>硬盘损坏,</li><li>电脑丢失</li><li>如果你还算比较珍你的部落格,不管是什么情况,都建议进行备份,鸡蛋不在一个篮子里,有备自然无患<br>有些人选择新建一个远程仓来进行拉取上传,有些人选择每次用u盘热拷数据,而我选择在原来远程仓基础上新建一个分支进行源码上传,原因嘛,自然是怎么方便怎么来.</li></ul><h4 id="新建git分支"><a href="#新建git分支" class="headerlink" title="新建git分支"></a>新建git分支</h4><p>终端执行<code>git branch</code><br>查看分支情况,如果没有意外你只有一个<strong>master</strong>分支<br>终端执行<code>git checkout -b dev</code><br>创建并切换到<strong>dev</strong>分支,其中<strong>dev</strong>是你的自定义分支名<br>再次执行<code>git branch</code><br>你可以看到列出的分支包含<strong>dev/master</strong><br>当前分支带有*号</p><h4 id="发布时选择的分支"><a href="#发布时选择的分支" class="headerlink" title="发布时选择的分支"></a>发布时选择的分支</h4><p>github仓库初始化生成一个master分支，这没什么特殊含义,只是init的时候就叫这个而已.<br>不过这对hexo来讲,生成的静态页面也是默认在master分支,如非魔改框架,请务必留着.<br>我这里选择<strong>dev</strong>分支作为我的源码托管分支,<br>你可以在克隆项目的时候指定分支<code>git clone -b dev xxurl</code>如果你正好也感兴趣,我会在后面描述  </p><h4 id="本地与远程仓库关联"><a href="#本地与远程仓库关联" class="headerlink" title="本地与远程仓库关联"></a>本地与远程仓库关联</h4><p>终端执行<code class="shell  language-shell">git remote add origin <a href="mailto:git@github.com">git@github.com</a>:yimouz/yimouz.github.io.git</code>  </p><blockquote><p>这里的origin只是一个易标识,你可以修改,比如hexo,后面这一串是仓库ssh地址,请不要错误关联到我的仓库,错误关联但没有配对的密钥自然没有用.除了ssh还可以选择http方式,具体看你个人选择.</p></blockquote><h4 id="推送到远程仓库"><a href="#推送到远程仓库" class="headerlink" title="推送到远程仓库"></a>推送到远程仓库</h4><p>推送到dev分支。<br>本地有许多文件,保留文章跟一些你觉得重要的，其他无关的文件并不希望推送，<br>使用<code>.gitignore</code>文件吧<br>把这些不想要的推送的保存到你ignore,比如如下一些:<br>.DS_Store<br>Thumbs.db<br>db.json<br><em>.log<br>node_modules/<br>public/<br>.deploy</em>/<br>添加完就可以试一下推送了<br>终端执行：<br> </p><pre class="line-numbers language-none"><code class="language-none">git add .git commit -m '你的提交信息'git push hexo dev</code></pre><p></p><h4 id="删除dev分支的某些文件"><a href="#删除dev分支的某些文件" class="headerlink" title="删除dev分支的某些文件"></a>删除dev分支的某些文件</h4><p>dev继承自master会带上master已经存在的分支内容<br>类似public<br>如果你跟我一样看着他们常常难受<br>不要犹豫,本地环境.<br>按下你的delete fireout it<br>删除完,终端执行<br>当然不删也可以<br> </p><pre class="line-numbers language-none"><code class="language-none">git add .git commit -m '同样填写你的注释信息'git push hexo dev</code></pre><br>你会发现.ignore里携带的都没有带上去了.<br>这就是ignore<p></p><h3 id="多pc"><a href="#多pc" class="headerlink" title="多pc"></a>多pc</h3><p>回到这个问题,新电脑了怎么办,硬盘坏了怎么办  </p><ol><li>下载git </li><li>安装node npm </li><li>克隆你的分支<code>git clone -b dev 你的url</code>   </li><li>初始化init</li><li>密钥</li><li>关联到你的远程仓</li></ol><blockquote><p>当然不要忘记<br> </p><pre class="line-numbers language-none"><code class="language-none">npm install hexo   npm install hexo-deployer-git -save</code></pre><br>是不是又回到了熟悉的环境<p></p></blockquote><h3 id="好了知道你有多pc使用习惯了-git就非常适合这种多端协作的场景-你要知道他是干嘛的"><a href="#好了知道你有多pc使用习惯了-git就非常适合这种多端协作的场景-你要知道他是干嘛的" class="headerlink" title="好了知道你有多pc使用习惯了,git就非常适合这种多端协作的场景,你要知道他是干嘛的"></a>好了知道你有多pc使用习惯了,git就非常适合这种多端协作的场景,你要知道他是干嘛的</h3><p>在公司划水,写写博客 一顿操作在push之后进入贤者模式<br>晚上或者周末在家,心潮澎拜,idea遍及<br>马不停蹄打开git</p><pre class="line-numbers language-none"><code class="language-none">/git pull &lt;远程仓ssh&gt; &lt;远程分支名&gt;:&lt;本地分支名&gt;   git pull git@github.com:yimouz/yimouz.github.io.git dev:dev/拉取并合并到你的本地环境  </code></pre><blockquote><p>至此就结束了!</p></blockquote>]]></content>
      
      
      <categories>
          
          <category> 杂记 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> hexo </tag>
            
        </tags>
      
    </entry>
    
    
  
  
</search>
